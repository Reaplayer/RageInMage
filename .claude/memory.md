# Claude Code Memory — RageInMage

> This file persists across sessions. Claude Code reads it via the CLAUDE.md pointer.
> Update this file at the END of each session with current status.
> Last updated: 2026-07-28. **NEWEST (2026-07-28): ✅ MOMENTUM DIAGNOSED — THE GAUGE WAS NEVER BROKEN. The three stage GEs are simply UNASSIGNED on the native MomentumComponent (`Stage1/2/3Effect = None`).** Reaper reported "Momentum still doesn't seem to be gaining at all"; PIE-verified via `playtest_*` + `GetAll` that the gauge climbs **exactly +10.0/sec while moving, 0 → 113 over 12s** (samples: 1.83 / 11.86 / 21.98 / 31.98 / 42.12 / 52.25 / 62.41 / 72.56 / 82.70 / 92.72 / 102.87 / 113.03). It crossed Stage1(35), Stage2(70) and Stage3(105) and **`MaxWalkSpeed` never moved off 262.5** → nothing downstream fires, so it FEELS like no gain. Control that proves the asymmetry: `GetAll ImmovableMassComponent Stage1Effect` → `GE_ImmovableMass_Stage1_C` (assigned, which is why ImmovableMass "works"), `GetAll MomentumComponent Stage1Effect/2/3` → **None**. C++ (`MomentumComponent.cpp`, the ASC-resolution fix, the AirMage gate in `InitPlayerAbilityActorInfo`), `CharacterClass=AirMage`, and the 3 GE assets themselves are all CORRECT — do NOT go re-editing them. **THE ONLY FIX: open `BP_RageInMagePlayerCharacter` → select the native `MomentumComponent` → Details → category `Momentum|Stages` → set Stage 1/2/3 Effect to `GE_Momentum_Stage1/2/3` (`/Game/Blueprints/AbilitySystem/Player/Mechanics/Momentum/`) → compile + save.** ⚠️ **BRIDGE LIMITATION (confirmed, extends the 2026-07-22 note): native default subobjects on a Blueprint CDO are UNREACHABLE from Lua** — `bp:get/set('MomentumComponent', ...)` → "target not found", and every alias tried (`self`,`'Comp.Prop'`, `'CDO:Comp'`, full `Default__..._C:Comp` object path) fails; `bp.components` only lists SCS components (MotionWarping). So assigning ANY property on ImmovableMass/Momentum components is a Reaper-by-hand step. **BUT you CAN verify it at runtime: `playtest_console('GetAll <ComponentClass> <Property>')` then read `Saved/Logs/RageInMage.log` — GetAll prints native subobject props AND `FGameplayAttributeData` as `(BaseValue=,CurrentValue=)`.** GE_Momentum_Stage1/2/3 audited and CORRECT: Infinite, grant `MechanicsStage.Momentum.1/2/3`, MovementSpeed **MultiplyCompound** ×1.1/1.2/1.3 (correct choice — MovementSpeed has BaseValue=0/CurrentValue=0.7, so MultiplyCompound gives a true +10/20/30%, unlike the old "Op=Add +0.10" note which would have been +14%), AttackSpeed Add +0.05/0.15/0.25, and Stage2/3 carry CooldownReduction +10/+20. `CooldownReduction` is now FULLY wired in C++ (`URageInMageGameplayAbility::GetEffectiveCooldownReduction`, `bAffectedByCooldownReduction`, `MaxCooldownReductionPercent=80`) so the deferred CDR tier is LIVE once the stage GEs apply. Minor flag: the CooldownReduction modifier in Stage2/3 serializes without `AttributeName`/`AttributeOwner` (MovementSpeed/AttackSpeed have both) — resolves fine today, but re-pick it from the dropdown if CDR ever misbehaves. Suggested-but-NOT-done (needs a rebuild, Reaper's call): a warn-on-null log in `ActivateMomentum()` — this same silent-null-GE failure mode has now cost two debugging sessions.
>
> Prior NEWEST (2026-07-26, session 3): 🎉 EARTH IS 6/6 — THE 5TH COMPLETED SCHOOL. The rebuild landed (DLL 19:50:31) and ALL SIX Earth spells are now built, registered and castable.** Verified live via bridge: `RageInMageRockSolidAbility`(38) / `JaggedTerraFormAbility`(43) / `BoulderAbility`(44) / `RockWall`(64) / `SpikeField`(63) / `Boulder`(73) all resolve, and `RiddenBoulder`/`BeginBoulderRide` are in the PlayerController. **Built this session (all compile clean, 0 errors, saved):** `GA_RockSolid`+`BP_RockWall`, `GA_JaggedTerraForm`+`BP_SpikeField`, `GA_TheBoulder`+`BP_Boulder` — see the "EARTH 6/6 — FINAL THREE BUILT" section below for every default, the graph shapes, and the 4 remaining manual/polish items. **INPUT SLOT CONVENTION CONFIRMED + FOLLOWED (matches Air exactly): LMB=Primary, RMB=Secondary, 1=Movement, 2=Defensive, 3=CrowdControl, 4=Ultimate.** Earth now: LMB Slingrock / RMB JaggedTerraForm / 1 ChargingBull / 2 RockSolid / 3 GemJail / 4 TheBoulder — no collisions, all 6 registered in `DA_CharacterClassInfo{EarthMage}.StartupAbilities` (round-trip verified), `BP_RageInMagePlayerCharacter.CharacterClass` still **EarthMage** so they're granted. ⚠️ NOTHING IS PIE-TESTED YET.
>
> Prior NEWEST (2026-07-26, session 2): ✅ DUAL-STICK AIM IS BUILT AND COMPILED — but the session that built it ENDED IN AN EDITOR CRASH (Live Coding), so it was never written down until now. Nothing was lost. See the "DUAL-STICK AIM" section below for the full state + the crash post-mortem.** Short version: `URageInMageAbilityTask_DualAimMode` + the PlayerController half are complete and live in `UnrealEditor-RageInMage.dll` (full rebuild at 05:38:49 landed AFTER the crash — symbols verified present in the DLL).
> (Session-2 note, now RESOLVED: the last three Earth spells were C++-complete awaiting one rebuild. That rebuild landed — see the session-3 entry above.)
>
> Prior NEWEST (2026-07-26, session 1): **✅ SLINGROCK BUILT & REGISTERED — Earth is now 3/6.** Full rebuild had landed before this session and the pending in-editor GE granted-tags work was done by Reaper. See the "SLINGROCK" section below. Also this session: new **`Condition.DamageImmune`** tag added and **`Condition.Immune` REDEFINED** to mean blanket immunity (Reaper's call: "Immune is just immune to everything", DamageImmune is damage-only) — both are still INERT (no C++ consumer; `Condition.Untouchable` is also inert and overlaps semantically, flagged to Reaper). ⏳ **OPEN QUESTION PUT TO REAPER, NOT YET ANSWERED:** whether to add `Condition.Immune` to `BlockedByConditions` on all 7 filled ConditionInfo rows now that it means blanket immunity — that would make it a working primitive with zero C++ (ApplyConditionFromData already checks that field). **⚠️ LIVE CODING NOW HANDLES NEW UPROPERTYs/STRUCT MEMBERS FINE** — hot-patched both the tag struct member AND 3 new UPROPERTYs on RageInMageProjectileSpell this session (verified 37→40 props via `class_properties`); UBT ran UHT and reported Succeeded. Stop assuming header changes need a full editor-closed rebuild — try `exec_command('LiveCoding.Compile')` first, then verify with `class_properties`.
>
> Prior NEWEST (2026-07-24): ✅ IMMOVABLE MASS IS LIVE AND PIE-CONFIRMED — Reaper: "ImmovableMass works now with all its perks and restraints".** The blocker was a phantom pawn-owned ASC, not the stance code — full diagnosis in the "STRAY PAWN-OWNED ASC" section below. Fixed in 2 places (C++ ASC resolution + a stale Blueprint CDO reference), both landed via Live Coding + asset save, no full rebuild needed.
>
> Prior NEWEST (2026-07-22): ✅ REBUILD LANDED — `Slow` attribute + stance-scalar foundation + Charging Bull ALL LIVE AND COMPILED.** Verified via bridge: `class_properties('RageInMageChargingBullAbility')` → 45 entries, `RageInMageAttributeSet` filter 'Slow' → 1 entry. **CHARGING BULL IS FULLY BUILT & CASTABLE** (1st Earth spell done): `GA_ChargingBull` created at `/Game/Blueprints/AbilitySystem/Player/Abilities/Earth/ChargingBull/` (parent `RageInMageChargingBullAbility`), defaults set (InputTag.1/Movement, Ability.Earth.ChargingBull, GE_Damage, DamageTypeTags={Bludgeoning=25}, **ActivationOwnedTags=Bludgeoning** load-bearing, bUseHoldToAim=False, CharacterSpeed=3000, DashDistance=700, KnockbackStrength=1200), EventGraph wired (ActivateAbility→CommitAbility→Branch(RV); True→BeginChargingBull **[then left UNCONNECTED — C++ self-ends]**; False→EndAbility; OnEndAbility→EndAbility — 7 nodes, all `enabled`, compiles clean), and **registered as EarthMage's 1st StartupAbility** in DA_CharacterClassInfo (round-trip verified). **`BP_RageInMagePlayerCharacter.CharacterClass` flipped AirMage→EarthMage** (compiled+saved) so the stance activates and Earth abilities are granted — ⚠️ this means the 6 AIR spells are no longer granted; flip back to `AirMage` to test Air again. Safe because Secondary/Vital/Resistance/ConditionInfo GEs are **root-level** on DA_CharacterClassInfo (shared by all classes), and EarthMage's row is structurally identical to AirMage's. **Cost/Cooldown GEs deliberately NOT set (both None) = spammable, ideal for testing.** ⚠️ CANNOT VERIFY via bridge (native components unreachable): whether Reaper assigned Stage1/2/3Effect + ConvertedSlowEffect on the ImmovableMassComponent in BP_RageInMagePlayerCharacter — the "charge is slower at high stance" half of the test needs the stage GEs (MovementSpeed self-slow) assigned. **PENDING TEST:** stand still → gauge to Stage 2-3 → Charging Bull should travel SHORTER/SLOWER but hit HARDER + knock further vs a Stage-0 charge. **NEXT: rename tags Ability/Cooldown.Earth.JewelFistShatter → JaggedTerraForm (C++, needs rebuild — batch it), then Gem Jail → Slingrock → dual-stick aim → Rock Solid → JaggedTerraForm → Boulder.**
> **BRIDGE LEARNINGS (2026-07-22, correcting/extending earlier notes):** `find_assets(path, opts)` is PATH-based — use `find_assets('/Game', {pattern='CT_ManaCost', recursive=true})`, NOT a bare name. `get_sub_paths(base, recursive)` lists folders. **DataAssets use SINGLE-ARG `a:get(prop)` / `a:set(prop, val)` (NO 'self' target)** — Blueprints use the two-arg `a:get('self', prop)`. Map sub-path `CharacterClassInformation{EarthMage}.StartupAbilities` works for BOTH get and set on the DataAsset, and READS BACK FINE (array of class refs in `("ref","ref")` form). NOTE: SecondaryAttributes/VitalAttributes/ResistanceAttributes/ConditionInfo are **root-level** DataAsset props, NOT per-class map fields (per-class = PrimaryAttributes/StartupAbilities/XPReward/ProgressBarColor). Graph authoring works well: `find_nodes(query, asset_path, graph_name, max)` → pass the RESULT TABLE (has `_spawner_id`) to `add_node(path, graph, node, x, y)` → returns `{handle, pins_in, pins_out}` with exact pin names → `connect(from_handle, from_pin, to_handle, to_pin)`. Node name is **`CommitAbility`** (no space — "Commit Ability" returns 0 results). Branch out-pins are display-named **`True`/`False`** (raw then/else). Newly-created ability BPs ship 2 `enabled_state=disabled` stub events (Event ActivateAbility / Event OnEndAbility) — they **auto-enable once you connect them** (verified). `read_graph` returns `{nodes={...}, graph_name=...}` — iterate `res.nodes`, fields are `name/handle/enabled_state/pins_in/pins_out`. To dump data use `error("\n"..text)` (bare returns aren't surfaced).
>
> Prior NEWEST (2026-07-21): **DEDICATED `Slow` ATTRIBUTE ADDED — C++ WRITTEN, NOT YET COMPILED (needs Reaper close-editor + full Build.bat: new REPLICATED attribute = not hot-patchable).** Reaper's architecture call: split the slow channel off MovementSpeed. New `Slow` FGameplayAttributeData on `URageInMageAttributeSet` (replicated + OnRep_Slow + ATTRIBUTE_ACCESSORS + DOREPLIFETIME) — an accumulated slow **PERCENT**, default **0** (so NO init needed, nothing freezes). `ARageInMageCharacterBase::UpdateMovementSpeed` now = `BaseWalkSpeed * GetMovementSpeed() * FMath::Clamp(1 - GetSlow()/100, 0, 1)`; `BindMovementSpeedDelegate` also binds the Slow attr change → same `OnMovementSpeedAttributeChanged` handler (live speed update). **CONVENTION (load-bearing, supersedes the old SetByCaller-pool note): REAL slows (enemy slows, converted CC, future Earth slows) → additive `Slow` modifier (+percent) + GRANT `Condition.Slowed`. "Exceptions" (stance self-slow, item slows, Momentum move buffs) → stay on `MovementSpeed`.** `ImmovableMassComponent` SIMPLIFIED: deleted `SlowPool`/`AddSlowToPool`/`PoolTotal`/`OnEffectApplied`/`OnEffectRemoved` — overload is now `OnSlowChanged` (bound to the Slow attr) → `CheckOverload()` shatters when `Slow >= SlowOverloadThreshold` (100; changed `>` to `>=` so it can't stick frozen-but-unshattered at exactly 100%). `Shatter()` now clears slows via `RemoveActiveEffectsWithGrantedTags(Condition.Slowed)` (self-slow on MovementSpeed survives). `HandleHardCC` still applies `ConvertedSlowEffect` with `AssignTagSetByCallerMagnitude(Condition.Slowed, positivePercent)` — no manual pool call (the Slow delegate runs CheckOverload). **REAPER MANUAL STEPS (post-rebuild):** (1) re-author `GE_ImmovableMass_ConvertedSlow`: modifier MovementSpeed→**Slow**, Op=Add, Magnitude=SetByCaller(Condition.Slowed), + add Grant-Tags-to-Target-Actor granting `Condition.Slowed`; keep HasDuration + duration Reaper set. (2) Any future real-slow GE follows the same convention. Files: `RageInMageAttributeSet.h/.cpp`, `Character/RageInMageCharacterBase.cpp`, `AbilitySystem/Components/ImmovableMassComponent.h/.cpp`. **THEN: back to Phase 3 = the 6 Earth spells (Reaper about to give the designs).**
>
> Prior NEWEST (2026-07-14): **EARTH MAGE — IMMOVABLE MASS MECHANIC, PHASE 1 C++ WRITTEN (NOT yet compiled — editor was open/Live Coding locked, Build.bat refused). Needs Reaper to close editor + full Build.bat rebuild (new class + new members, NOT hot-patchable).** Design fully locked with Reaper (see the dedicated "## Earth Mage School — Immovable Mass" section below for the complete spec + all 7 answered design decisions). Built this session: (1) new `State.Dashing` gameplay tag (Public/Private RageInMageGameplayTag) — dash abilities grant it, drives increased gauge decay. (2) new **`UImmovableMassComponent`** (`Public/AbilitySystem/Components/ImmovableMassComponent.h` + `Private/.../ImmovableMassComponent.cpp`) — the whole passive juggernaut-stance system: 0-120 gauge (time-based charge while still after 1s delay, DISTANCE-based decay while moving, ×mult while State.Dashing), stages 0-3 at 30/60/90, bidirectional stage-GE swap, hard-CC→slow conversion (60/40/30%), Stage-3 ignore-first-hard-CC (20s real-time cooldown, never reset by re-entry), additive enemy-slow overload pool (>100% → SHATTER: remove slows, reset gauge, 5s no-charge vulnerability lockout), knockback resist (30/60/immune) + airborne = hard CC. Server-authoritative; mirrors gauge to the `ImmovableMass` attribute for UI. Self-gates: created on ALL players but only `ActivateStance()`'d for `ECharacterClass::EarthMage`. (3) Hooked `ARageInMageCharacterBase::ApplyKnockbackImpulse` — queries the component (Blocked=no launch / Reduced=×knockback-mult / Passthrough). (4) GE-based CC interception + enemy-slow tracking bound inside the component via ASC delegates (`RegisterGameplayTagEvent` per hard-CC tag; `OnGameplayEffectAppliedDelegateToSelf`/`OnAnyGameplayEffectRemovedDelegate` for the slow pool, reading slow % from SetByCaller[Condition.Slowed]). (5) Component created on `ARageInMagePlayerCharacter` (constructor) + activated in `InitPlayerAbilityActorInfo` (EarthMage-gated). **PHASE 2 (after rebuild, do via NeoStack bridge): create GE_ImmovableMass_Stage1/2/3 (self-slow -0.15/-0.30/-0.50 additive MovementSpeed + Resistance_Damage 20/40/60) + GE_ImmovableMass_ConvertedSlow (MovementSpeed slow, magnitude from SetByCaller Condition.Slowed = percent, additive; short duration), assign all 4 on the component defaults in BP_RageInMagePlayerCharacter, PIE-test the stance in isolation. THEN Phase 3 = the 6 Earth spells.** ⚠️ CHECK before Phase 2/testing: how the player's `CharacterClass` gets set to EarthMage (one shared BP_RageInMagePlayerCharacter vs per-class child BPs) — the stance won't activate unless CharacterClass==EarthMage at InitPlayerAbilityActorInfo.
>
> Prior NEWEST (2026-07-11): **AIR SCHOOL IS NOW 6/6 COMPLETE.** Catapult Launch + Dance of the Wind Dragon C++ was already compiled (Reaper's earlier Ctrl+Alt+F11 landed it — confirmed via `class_properties`, 46/43 entries). This session built both remaining GA Blueprints (`GA_CatapultLaunch`, `GA_DanceOfTheWindDragon`) from scratch — defaults set, EventGraph wired identically to the proven Commit→Branch→Begin[unconnected]/EndAbility, OnEndAbility→EndAbility template (verified by reading `GA_MountainVortex`'s graph first), both compile clean (0 errors), and both registered as AirMage's 5th/6th StartupAbilities via the direct sub-path `CharacterClassInformation{AirMage}.StartupAbilities` setter (no whole-struct rewrite needed). Values chosen (placeholders, untuned): CatapultLaunch — InputTag.RMB/Secondary, Bludgeoning=25, CharacterSpeed=2500, DashDistance=600, ThrowStrength=1800. DanceOfTheWindDragon — InputTag.4/Ultimate, Bludgeoning=30/hit, Duration=3, ZoneRadius=350, NumAttacks=7(default), AfterimageActorClass=None (no cosmetic actor yet). **Both CooldownGameplayEffectClass left None** — same recurring manual-step block as BlueSky/MountainVortex: `GE_Cooldown_CatapultLaunch`/`GE_Cooldown_DanceOfTheWindDragon` grant NO tags yet (confirmed empty via `GEComponents` read), Reaper must add "Grant Tags to Target Actor" (`Cooldown.Air.CatapultLaunch` / `Cooldown.Air.DanceOfTheWindDragon`) in-editor, then I/Reaper assign them as each GA's CooldownGameplayEffectClass.
> **AIR SCHOOL FULLY DONE & PIE-CONFIRMED 2026-07-14 (Reaper):** all cooldown tags sorted in-editor for the 4 pending spells (BlueSkyWhirlwind, MountainVortex, CatapultLaunch, DanceOfTheWindDragon) PLUS a newly-added one for SwiftGust (it had none before), each `GE_Cooldown_*` now grants its `Cooldown.Air.*` tag and is assigned as the GA's CooldownGameplayEffectClass. Reaper PIE-tested — "things seem to be working quite well." Air school = 6/6 built, wired, cooldowns live, playtest-confirmed. **NEXT DIRECTION: build the EARTH SCHOOL** (5th completed school) so we have 5 done, then pivot to animations / arena maps / UI. Earth spell design incoming from Reaper.
> **TOP OPEN ITEMS now:** (1) **EARTH SCHOOL — new build target** (info from Reaper pending; see Earth section once started). (2) Dash-fix rebuild (ZipNZap/CleanWindDash "doesn't dash" + pass-through collision) still not PIE-tested. **(The "no damage anywhere" bug is CLOSED — Reaper confirmed 2026-07-11 it was a stale editor/Live-Coding state, fixed by closing+rebuilding, not a real code bug. See its section below for the full note.)**
> Prior header (2026-07-06, Air-school + dash-fix session): Clean Wind Dash FULLY BUILT & COMPILED, dash-fix rebuild landed, SparkingSphere/StaticMegaSurge/gamepad virtual cursor confirmed working. Prior controller detail below (newest first):
>   A. **Controller was fully broken; now working.** Two regressions FIXED & PIE-confirmed by Reaper: (1) **bootstrap deadlock** — `BeginPlay` only added `IMC_MAKContext` (no gamepad keys); the controller context was gated behind gamepad-detection that could never fire because the only detector (right-stick Look) lived inside the un-added context. Fix: add `ControllerMageContext` in BeginPlay + keep both IMCs permanently active (disjoint keys), `UpdateControllerIMC()` now a no-op. (2) **chord spells aimed but never fired** — chord-triggered actions emit `ETriggerEvent::Canceled` not `Completed` on release; `BindAbilityActions` only bound Completed. Fix: also bind Canceled→ReleasedFunc.
>   B. **Gamepad spell-aim MODAL velocity virtual cursor — PIE-CONFIRMED WORKING & LOVED by Reaper 2026-07-06** ("AWESOME!!!!"). One tweak this session: `BeginGamepadAim` now seeds `AimCursorOffset = ZeroVector` so the cursor starts ON the character and the stick pushes it outward (was seeding at max range in the facing dir; Reaper wanted start-at-character). Function-body-only edit, hot-patchable — but rides along with the dash-ability rebuild anyway. Only while aiming a spell, the right stick drives a persistent virtual cursor (velocity-integrated, caster-relative offset, no snapback, Z-traced onto objects); normal movement keeps the original direction+snapback for tight turning/dodging. Driven by `bAimingSpell` flag toggled from the `AimMode` task's Activate/OnDestroy. **Added new members (`AimCursorOffset`, `AimCursorSpeed`, `bAimingSpell`) → Live Coding may reject with C2065; if so, full `Build.bat` rebuild.** Tune `AimCursorSpeed` (default 1500 u/s) on `BP_RageInMagePlayerController` to taste.
>   Everything below is PRIOR (2026-07-02) session state, unchanged this session:
>   0. **ZipNZap + Static Megasurge Blueprint assets BUILT (the final 2 Lightning spells) — Lightning school is now 6/6 authored.** Their C++ was already compiled/loaded (landed in a prior rebuild — verified both ability classes + `Get/SetBaseWalkSpeed` live, NO rebuild was needed this session). **ZipNZap = FULLY DONE** (GA + cost GE + curve row + graph, compiles clean, registered 5th). **Static Megasurge = DONE — both by-hand GEComponent tag steps completed by Reaper 2026-07-06** (`GE_Cooldown_StaticMegaSurge` now grants `Cooldown.Lightning.StaticMegaSurge`, `GE_StaticMegaSurge_Buff` grants `Status.Reflecting`). Both GAs registered in `DA_CharacterClassInfo` (LightningMage now has 6 StartupAbilities). Neither new spell PIE-tested. NOTE: StaticMegaSurge's damage-reduction half stays inert until Reaper finishes the `Resistance_Damage` wiring in `ExecCalc_Damage`.
>   1. **SparkingSphere over-damage fix — DONE & PIE-CONFIRMED WORKING by Reaper 2026-07-06** ("damages properly like it should"). The per-target cooldown model (below) shipped in a rebuild and behaves correctly.
>   2. **Horde spawner `BP_EnemySpawner`** — fully BUILT (waves, wave-on-clear, MaxAlive cap), compiles clean, seeded with Ghoul+Goblin_Spear. NOT yet placed in a level / PIE-tested; needs a NavMesh in the map to spawn.
>   3. **Player `HasAuthority()` guard** on `InitializeDefaultAttributes()` — DONE, hot-patched via Live Coding.
>   4. **Enemy movement-speed + Ghoul-not-attacking bugs** — both RESOLVED & PIE-confirmed by Reaper (stale-CDO + stale-placed-instance cluster; re-placing the Ghoul instance was the final fix).
>   Prior (2026-07-01): temporary per-actor MovementSpeed on-screen debug instrumentation added to `ARageInMageCharacterBase` (still present). ZipNZap + Static Megasurge (Lightning Movement/Defensive) remain UNBUILT/pending as before.

## STRAY PAWN-OWNED ASC — the phantom that broke ImmovableMass + Momentum (FOUND & FIXED 2026-07-24)
**Symptom:** on PIE, an ensure fired at `GameplayEffect.cpp:3994` — `FActiveGameplayEffectsContainer::SetAttributeBaseValue: Unable to get attribute set for attribute ImmovableMass`. Stack: `PossessedBy` → `InitPlayerAbilityActorInfo` → `ImmovableMassComponent::ActivateStance` → `SetGauge(0)`. Non-fatal (PIE kept running), only visible because Reaper was attached in the debugger.
**Root cause (2 layers):**
1. `BP_RageInMagePlayerCharacter`'s **CDO carried an orphaned `RageInMageAbilitySystemComponent` default subobject**, with the inherited `AbilitySystemComponent` UPROPERTY pointing at it. Left over from when `ARageInMagePlayerCharacter`'s constructor still created a pawn-side ASC (the `// NOTE: Do NOT create an ASC here` comment at `RageInMagePlayerCharacter.cpp:46` marks its removal). `AbilitySystemComponent` is a **plain `UPROPERTY()`** on `RageInMageCharacterBase.h:151` — NOT Transient — so the reference serialized into the Blueprint and outlived the C++ that created it. Every spawn re-instantiated a phantom ASC on the pawn with **SpawnedAttributes = 0**.
2. `ActivateStance()`/`ActivateMomentum()` opened with `OwnerCharacter->FindComponentByClass<UAbilitySystemComponent>()` and only fell back to the interface if that returned null — so they grabbed the phantom and never reached the PlayerState's real ASC.
**FIX (both applied):** (a) deleted the `FindComponentByClass` first-pass in `ImmovableMassComponent.cpp` + `MomentumComponent.cpp` — `UAbilitySystemBlueprintLibrary::GetAbilitySystemComponent()` **already tries `IAbilitySystemInterface` first and falls back to a component search internally**, so the old ladder inverted the correct priority. This is the durable fix. (b) cleared the stale CDO ref via bridge `bp:set('self','AbilitySystemComponent','None')` + compile + save — which also evicted the orphaned subobject from the asset (uasset shrank 168489→168116 bytes, the `RageInMageAbilitySystemComponent` string is gone).
**⚠️ WIDER LESSON — never use `FindComponentByClass<UAbilitySystemComponent>` on a player pawn in this project.** The player's authoritative ASC lives on the PlayerState; always go through `IAbilitySystemInterface` / `UAbilitySystemBlueprintLibrary::GetAbilitySystemComponent`. Note the phantom also meant `ARageInMageCharacterBase::GetAbilitySystemComponent()` returned the DEAD ASC for the whole window between spawn and `InitPlayerAbilityActorInfo()` overwriting the member — so any future GAS work in the pawn's `BeginPlay` is a latent trap.
**⚠️ MOMENTUM WAS SILENTLY BROKEN TOO** — the diagnostic's control line proved `HasAttributeSetForAttribute(Momentum) = NO`, i.e. the Air mage's Momentum meter has **never actually driven the `Momentum` attribute** (all its `SetNumericAttributeBase` calls hit the dead ASC and no-op'd). It only never surfaced because nobody ran it under a debugger. **When flipping `CharacterClass` back to `AirMage`, re-verify/re-tune Momentum — any prior "tuning" was done against a gauge that did nothing.**
**✅ HARDENING — LANDED (verified in-source + compiled 2026-07-26 session 3):** `AbilitySystemComponent` and `AttributeSet` on `RageInMageCharacterBase.h:151/155` are now `UPROPERTY(Transient)` so the pointers can never serialize into a child BP's CDO again. Safe for both paths: the player's is assigned in `InitPlayerAbilityActorInfo()`, the enemy's by `CreateDefaultSubobject` in `ARageInMageEnemyCharacter`'s constructor (ctor runs on every instance, and DSO→OwnedComponents registration is independent of UPROPERTY flags, so the enemy ASC/AttributeSet still work). Header change + new UPROPERTY specifiers = UHT regen, so **Live Coding cannot do this — editor must be closed and Build.bat run.**
**DEBUGGING NOTES for next time:** a commented-out `[IMMASS]` diagnostic block sits in `ActivateStance()` (`ImmovableMassComponent.cpp:69-97`) — uncomment to dump the resolved ASC, its owner actor, `SpawnedAttributes` count, `HasAttributeSetForAttribute`, and every component on the pawn (flagging any ASC). Also: `read_log('output')` via the bridge returns EMPTY while the editor holds the file — read `Saved/Logs/RageInMage.log` from disk directly instead. And the bridge's `bp.components` lists only **SCS/Blueprint-added** components (it showed just MotionWarping) — it does NOT show native `CreateDefaultSubobject` components or CDO subobjects, so it cannot be used to rule out a phantom; use `bp:list_properties('self', <filter>)` to read CDO object references instead.

## Earth Mage School — Immovable Mass mechanic (DESIGN LOCKED + PHASE-1 C++ WRITTEN 2026-07-14, NOT compiled)
**The Earth mage's core is Immovable Mass — a passive, always-on JUGGERNAUT STANCE (innate to EarthMage, not a cast).** Stand still → charge; move → drain; dash → drain faster. Each stage trades MOBILITY (self-slow + shorter dash) for DEFENSE (hard-CC→slow conversion, knockback resist, damage reduction). Counterplay: enemies pile ADDITIVE slows; if separate-cast slows exceed 100% the stance SHATTERS to Stage 0. Reaper reworking the 6 Earth spells around this (spell design still pending — Phase 3).

**4 STAGES (gauge 0-120):**
- **Stage 0 Loose Gravel (0-29):** no changes.
- **Stage 1 Packed Earth (30-59):** hard CC → 60% slow instead; 30% knockback resist; 20% damage reduction; 15% self-slow; dash -15%.
- **Stage 2 Bedrock (60-89):** hard CC → 40% slow; 60% knockback resist; 40% damage reduction; cannot be made airborne; 30% self-slow; dash -30%.
- **Stage 3 Living Mountain (90-120):** first hard CC IGNORED (see charge below), further hard CC → 30% slow; knockback IMMUNE; 60% damage reduction; 50% self-slow; dash -50%.

**7 LOCKED DESIGN DECISIONS (Reaper, 2026-07-14):**
1. Self-slow & dash-reduction are ONE knob — the self-slow (−15/30/50% MovementSpeed) flows through to dash automatically (dash = DashDistance×MovementSpeed), so numbers match. Do NOT add a separate dash penalty.
2. "X% knockback" = X% knockback RESISTANCE (incoming reduced); Stage 3 = immune.
3. Hard CC = anything that removes free movement (Stun, Root, Petrify, Freeze, Paralyse, Airborne, Grapple, Constrict, Slam + HEAVY knockback). Heavy vs light knockback = impulse-magnitude threshold (`HeavyKnockbackThreshold`, default 900; airborne = Z-impulse ≥ `AirborneImpulseThreshold` default 300).
4. Converted CC-slows DO count toward the 100% overload pool (so hard CC is a deliberate way to shatter the mountain). Self-slow does NOT count. Item/negative-move-speed slows do NOT count.
5. Stage-3 "ignore first hard CC" = a charge on a **20s REAL-TIME cooldown**; only CONSUMABLE while in Stage 3; the timer runs regardless of stage and is NEVER reset by dropping out of/back into Stage 3 (no cheese). Starts available.
6. After a shatter: brief **vulnerability window** (`VulnerabilityDuration`, default 5s, easily tweakable) where the gauge CANNOT charge — "forced to move" just means charging is disabled, NOT literal forced movement.
7. Thresholds S0 0-29 / S1 30-59 / S2 60-89 / S3 90-120. Charge = per-SECOND while still (`ChargePerSecond` 15). Decay = DISTANCE-based while moving (`DecayPerUnitMoved`), ×`DashDecayMultiplier` while dashing — distance-based is self-balancing (slower mage covers less ground → bleeds slower, "fair"). Damage reduction applies to ALL damage types (via `Resistance_Damage`, not just physical).

**ARCHITECTURE (what exists / how it hooks):** `Resistance_Damage` is ALREADY wired in ExecCalc_Damage (both branches: `Damage *= 1 - Resistance/100`) — memory's old "incomplete" note was stale; stage GE just sets it 20/40/60. `MovementSpeed` is a multiplier on BaseWalkSpeed (MaxWalkSpeed=BaseWalkSpeed×MovementSpeed) — self-slow = additive negative modifier; dash rides it for free. `ApplyKnockbackImpulse` on CharacterBase is the SINGLE chokepoint for all knockback/airborne (already Poise-scaled) → hooked. `Poise` attr also scales knockback but nonlinear — NOT used for the stance (component does its own stage-based mult). The existing `HandleMechanicsThreshold` on ImmovableMass (0-120) is ONE-DIRECTIONAL (cross-up→apply GE) so it CANNOT drive a bidirectional stance — the component owns staging entirely and drives the gauge via `SetNumericAttributeBase` (which does NOT trigger PostGameplayEffectExecute, so it never fires the old threshold system — clean separation, gauge attribute is just a replicated display value). Overload pool tracked SEPARATELY from the MovementSpeed attribute (which includes self-slow) — pool = sum of enemy/converted slow magnitudes only. **CONVENTION I ESTABLISHED (Phase 2 GEs must follow):** all slows carry their magnitude as a SetByCaller keyed to `Condition.Slowed` in PERCENT (e.g. 60 = 60%), and use ADDITIVE MovementSpeed modifiers so they stack. The component reads that SetByCaller to size the overload pool; enemy slows from OTHER schools that don't follow this convention won't feed overload (fine for Earth-focused testing; revisit later).

**FILES (Phase 1):** `RageInMageGameplayTag.h/.cpp` (+State.Dashing), `AbilitySystem/Components/ImmovableMassComponent.h/.cpp` (new), `Character/RageInMageCharacterBase.cpp` (ApplyKnockbackImpulse hook + include), `Character/RageInMagePlayerCharacter.h/.cpp` (component member + construct + EarthMage-gated ActivateStance). **All tunables are EditDefaultsOnly UPROPERTYs on the component (categories ImmovableMass|Gauge/Stages/Knockback/CC/Overload/Stage3) — Reaper tunes in BP.** `HardCCTags` + the 4 stage/slow GE classes default empty/None — HardCCTags lazily fills a sensible set in ActivateStance if left empty; the GEs are Phase 2.

**PHASE 2 IN PROGRESS (2026-07-14): rebuild DONE (component class LIVE, `class_properties(ImmovableMassComponent)`→35 entries — compiled clean).** 4 GE shells created via bridge at `/Game/Blueprints/AbilitySystem/Player/Mechanics/ImmovableMass/`: GE_ImmovableMass_Stage1/2/3 (DurationPolicy=Infinite) + GE_ImmovableMass_ConvertedSlow (HasDuration). **Reaper's remaining manual steps (bridge CAN'T do these — see tooling note):** (1) add modifiers in each GE editor (Op=Add): Stage1 MovementSpeed −0.15 + Resistance_Damage +20; Stage2 −0.30/+40; Stage3 −0.50/+60; ConvertedSlow MovementSpeed −0.30 fixed + set Duration Magnitude ~2.0. Stages grant NO tags. (2) On BP_RageInMagePlayerCharacter select the **ImmovableMassComponent** (native, in Components panel) → assign Stage1/2/3Effect + ConvertedSlowEffect. (3) Class Defaults → CharacterClass = **EarthMage** (temporary until Reaper's char/class-select screen exists — that screen will set class before play). Then PIE-test the stance. NOTE: converted-slow's actual % is C++-driven for the overload pool (CurrentCCConvertSlow 60/40/30), independent of the GE's fixed modifier — so a fixed −0.30 tests the shatter correctly; make it stage-exact later via SetByCaller[Condition.Slowed] (would need HandleHardCC to pass −fraction not +percent — a function-body hot-patch).

**TOOLING (2026-07-14, NeoStack bridge limits confirmed for GAS work — IMPORTANT):**
- **Native C++ components (CreateDefaultSubobject) are NOT addressable via the bridge** — `set/get/list_properties("ComponentName")` → "target not found". Same class of gotcha as SwiftGust's inherited ProjectileMovement. So component UPROPERTY defaults (GE class refs, tunables) must be assigned by Reaper in the BP's Details panel (they DO save as per-BP overrides and apply at runtime). The bridge can only reach the BP CDO via `set("self", Prop, Val)`.
- **`set("self", Prop, Val)` on a GameplayEffect Blueprint WORKS** (writes CDO, e.g. DurationPolicy="Infinite"/"HasDuration" → `[OK] ... (CDO)`), but **`get("self", Prop)` reads back BLIND/empty** for enums/arrays (esp. `Modifiers`) — can't template an existing GE's modifier export string. There is NO dedicated GE-modifier bridge helper (only Reflection set/get + SetNodeProperty). ⇒ Author GE MODIFIERS in-editor by hand; the bridge is only good for create_asset + DurationPolicy + tag/name scaffolding. (create_asset for a BP GE = `create_asset(path,"Blueprint",{ParentClass="/Script/GameplayAbilities.GameplayEffect"})`.)

## GEM JAIL — FULLY BUILT & CASTABLE 2026-07-23 (2nd Earth spell done). READY TO PLAYTEST.
Assets at `/Game/Blueprints/AbilitySystem/Player/Abilities/Earth/GemJail/`:
- **BP_GemJailZone** (child of `RageInMageGemJailZone`): PetrifyEffect=GE_Petrified, PetrifyDelay=2, PetrifyDuration=4, ZoneRadius=350, **ZoneDuration=2.5** (MUST stay > PetrifyDelay or the zone is destroyed before it jails — Destroyed() clears the petrify timer). No gem VFX yet (ZoneEffect empty — no Gem/Crystal/Rock NiagaraSystem exists; add later, petrify works invisible).
- **GA_GemJail** (parent RageInMageDamageGameplayAbility, no damage used): InputTag.3/CrowdControl, Ability.Earth.GemJail, bUseHoldToAim=True, AimIndicatorShape=Circle, AimIndicatorLength/Width=800/700, MaxSpellRange=800. Cost/Cooldown=None (spammable for testing). Graph (14 nodes, compiles clean, events auto-enabled): ActivateAbility→AimMode(MaxRange=GetAimIndicatorLength)→OnInputReleased→CommitAbilityCost→Branch(RV); True→CastToPawn(avatar)→SpawnActorFromClass(BP_GemJailZone, Spawn Transform from AimMode.AimPosition via MakeTransform, **Owner=avatar, Instigator=avatar-as-Pawn** [zone's JailEnemiesInside/IsFriendly need GetInstigator]); spawn→End; Branch False→End; AimMode.OnCancelled→End; OnEndAbility→End. Petrify Delay/Duration set on the spawn node's ExposeOnSpawn pins (2.0/4.0) — belt-and-braces over the BP CDO defaults.
- **Registered as EarthMage's 2nd StartupAbility** (ChargingBull + GemJail). **BP_RageInMagePlayerCharacter.CharacterClass flipped WaterMage→EarthMage** for testing (⚠️ Air/Water spells not granted now; flip back to test those).
**⚠️ STANCE AREA SCALING (+10/20/30%) NOT wired yet** — needs C++ (ZoneRadius scaled by GetImmovableMassStageScalar). Deferred: has a networking wrinkle (ZoneRadius not replicated; would scale on authority in GemJailZone::BeginPlay before Super, cosmetic-only on true clients). Do in the next rebuild batch. Base radius works fine for the test.
**PLAYTEST (InputTag.3): aim a circle → release → gems form → after 2s enemies still inside are Petrified 4s.** Watch for: (1) petrify locks movement AND actions (CrowdControlTagChanged + ActivationBlockedTags), (2) **struggle** — needs IA_Struggle bound to call AddStruggleProgress() [NOT wired — enemies never mash so they serve full 4s], (3) **CC chaining** — petrify an enemy, then a different CC lands but re-petrify is blocked by Condition.PetrifiedImmune during grace. ⚠️ GemJail applies GE_Petrified DIRECTLY (bypasses ApplyConditionToTarget) so it does NOT apply the immunity/grace or check BlockedByConditions — only the struggle + the locks fire. To get grace/blocking on petrify, route GemJail through ApplyConditionToTarget later (C++).

## EARTH 6/6 — FINAL THREE BUILT 2026-07-26 (session 3). ALL CASTABLE, NONE PLAYTESTED.
Rebuild confirmed landed (DLL 19:50:31); every class below resolves via `class_properties`. All 6 assets compile clean and are saved.
**ROCK SOLID** `/Game/Blueprints/AbilitySystem/Player/Abilities/Earth/RockSolid/` — `GA_RockSolid` (parent `RageInMageRockSolidAbility`): InputTag.**2**/Ability.Type.Defensive, `Ability.Earth.RockSolid`, **no damage tags (deals none)**, RockWallClass=BP_RockWall_C, WallDuration=(Value=5), bUseHoldToAim=True, MaxSpellRange=800, StanceBonus 10/20/30 (C++ defaults, untouched). Cost/Cooldown **None** (spammable for testing). `BP_RockWall` = bare child of `RageInMageRockWall`, all C++ defaults (HalfWidth 250/HalfThickness 40/HalfHeight 150).
**JAGGED TERRA FORM** `.../JaggedTerraForm/` — `GA_JaggedTerraForm`: InputTag.**RMB**/Secondary, `Ability.Earth.JaggedTerraForm`, **`DamageTypeTags={DamageType.PhysicalDamage.Piercing=25}` + `ActivationOwnedTags=Piercing`** (load-bearing; the ONE Earth spell that isn't Bludgeoning), GE_Damage, KnockbackStrength=(Value=900), SpikeFieldClass=BP_SpikeField_C, FieldDuration=(Value=4), bUseHoldToAim=True, MaxSpellRange=900, FieldRadius 350 + StanceBonus 15/30/50 (C++ defaults). `BP_SpikeField` = bare child of `RageInMageSpikeField`.
**THE BOULDER** `.../TheBoulder/` — `GA_TheBoulder`: InputTag.**4**/Ultimate, `Ability.Earth.TheBoulder`, `DamageTypeTags={Bludgeoning=40}` + `ActivationOwnedTags=Bludgeoning`, GE_Damage, KnockbackStrength=(Value=1200), BoulderClass=BP_Boulder_C; GrowthDuration 6 / MaxLifetime 12 / StanceBonus **-15/-30/-50** all C++ defaults. `BP_Boulder` = bare child of `RageInMageBoulder`.
**GRAPH SHAPE — RockSolid + JaggedTerraForm (first consumers of DualAimMode, 7 nodes each):** ActivateAbility→CommitAbilityCost→**DualAimMode**(Max Range pin 800/900); `On Aim Update`→DrawDebug*Preview; `On Input Released`→SpawnRockWall / EruptSpikes; that→End Ability; `On Cancelled`→End Ability. Data: `Aim Position`→Location, `Aim Direction`→Direction (RockSolid) / `Push Direction` (JaggedTerraForm). OnEndAbility left as the unconnected disabled stub (Slingrock's shape — the ability ends itself).
**⚠️ CRITICAL DUAL-AIM PIN FACT (verified against the working `GA_GemJail` AimMode node):** the async-task node does **NOT** expose separate `Final Aim Position`/`Final Aim Direction` pins even though the release delegate names its params that way — UE merges the second delegate's params ONTO the first's pins **by index**. So `Aim Position` / `Aim Direction` carry the FINAL values when `On Input Released` fires. Wire the release path to those same two pins; don't hunt for "Final*" pins, they don't exist.
**GRAPH SHAPE — TheBoulder (ChargingBull's self-ending shape, 7 nodes):** ActivateAbility→CommitAbility→Branch(Return Value); True→**SummonBoulder** (then left UNCONNECTED — the ability ends itself on the boulder's OnDestroyed); False→End Ability; OnEndAbility→End Ability.
**REGISTERED:** all 6 in `DA_CharacterClassInfo` → `CharacterClassInformation{EarthMage}.StartupAbilities` (ChargingBull, GemJail, Slingrock, RockSolid, JaggedTerraForm, TheBoulder — round-trip verified). `CharacterClass` on `BP_RageInMagePlayerCharacter` = EarthMage. ⚠️ Air/Water spells are NOT granted while it's Earth.
**⏳ REMAINING (4 items, none blocking a playtest):**
1. **MESHES — Reaper's Details-panel step (bridge CANNOT do it: native `CreateDefaultSubobject` components are unreachable, confirmed again this session with `get('BoulderMesh',...)` → "target not found").** `BP_Boulder`→BoulderMesh, `BP_RockWall`→WallMesh, `BP_SpikeField`→its mesh are all empty, so all three spawn **invisible but fully solid**. Boulder matters most (you RIDE it). `MeshBaseRadius` is auto-derived from the mesh's own bounds so ANY mesh fits the sphere; with no mesh it falls back to 50. Only rock in the project is `SM_SlingshotRock`. **Deliberately did NOT bake a SetStaticMesh into a Construction Script** — it would silently override whatever Reaper later picks in the panel.
2. **`DismountBoulder` HAS NO INPUT BOUND** — needs an IA + IMC mapping + a PlayerController call, same open item as `IA_Struggle`. Until then you ride until impact/MaxLifetime(12s).
3. **No Cost/Cooldown GEs on any of the three** (deliberate — spammable for testing, same as the other 3 Earth spells).
4. **Navmesh is Static**, so AI won't re-path around RockWall/SpikeField (they'll collide and slide). Flip to Dynamic in Project Settings — Reaper's call, not a code bug.

## ROCK SOLID + JAGGED TERRA FORM — C++ REFERENCE (written session 2, COMPILED & LIVE; BPs built session 3)
**8 NEW FILES for these two (4 brand-new UCLASSes, ZERO edits to existing classes — the Boulder's 4 files + the PlayerController edit are on top):**
- `Public/Actor/RageInMageRockWall.h` + `Private/Actor/RageInMageRockWall.cpp`
- `Public/AbilitySystem/Abilities/RageInMageRockSolidAbility.h` + `Private/.../RageInMageRockSolidAbility.cpp`
- `Public/Actor/RageInMageSpikeField.h` + `Private/Actor/RageInMageSpikeField.cpp`
- `Public/AbilitySystem/Abilities/RageInMageJaggedTerraFormAbility.h` + `Private/.../RageInMageJaggedTerraFormAbility.cpp`

**ROCK SOLID (Defensive, 4th Earth spell).** `URageInMageRockSolidAbility : URageInMageGameplayAbility` (NOT the damage base — deals no damage). API: `SpawnRockWall(Location, Direction)` (authority-gated, returns the wall), `DrawDebugRockWallPreview(Location, Direction)`, BlueprintPure `GetWallSizeScalar()`. Props: `RockWallClass`, `WallDuration` (FScalableFloat), `StanceBonusStage1/2/3` = **10/20/30**. Direction = the wall's **FACING**; it spans perpendicular (`Yaw += 90`, the same convention as `URageInMageWallAbility` so both walls aim identically). Stance scales size through the spawn transform's **SCALE** (collision + mesh together — the proven Slingrock trick); spawn Z is offset by `HalfHeight * Scalar` so the base sits on the ground.
`ARageInMageRockWall : AActor` — the deliberate OPPOSITE of `ARageInMageFireWall`: **BLOCKS** rather than overlaps. Box is `ECR_Block` on all channels **EXCEPT `ECC_Camera` (Ignore)** — the top-down spring arm probes for geometry, so a blocking wall would yank the camera in whenever the player stood behind their own spell. **Reuse that camera exclusion on every future blocking actor.** No damage, no health, indestructible, solid from both sides INCLUDING to the caster (per Reaper's design). Mesh forced to `NoCollision` (Slingrock gotcha). Defaults: HalfWidth 250 / HalfThickness 40 / HalfHeight 150 / WallDuration 5.

**JAGGED TERRA FORM (Secondary, 5th Earth spell).** `URageInMageJaggedTerraFormAbility : URageInMageDamageGameplayAbility`. API: `EruptSpikes(Location, PushDirection)`, `ApplyDirectionalKnockback(Target, Direction)`, `DrawDebugSpikeFieldPreview(Location, Direction)`, BlueprintPure `GetFieldAreaScalar()` / `GetScaledFieldRadius()`. Props: `SpikeFieldClass`, `FieldRadius` 350, `FieldDuration` (FScalableFloat), `StanceBonusStage1/2/3` = **15/30/50** (AREA). All damage + push happen in the ABILITY in ONE burst at cast time (query `GetLivePlayersWithinRadius` at the stance-scaled radius → filter `IsFriendly` → `CauseDamage` + `ApplyDirectionalKnockback`); the spawned actor is ONLY the barrier left behind.
**`ApplyDirectionalKnockback` is the spell's whole trick** — the parent's `ApplyKnockback(Target, Origin)` always pushes AWAY FROM A POINT, so a pack scatters. This shoves everyone the SAME way, which is what "push enemies in a CHOSEN direction" means. Same impulse shape otherwise (KnockbackStrength + KnockbackUpwardForce clamped into Z).
`ARageInMageSpikeField : AActor` — blocking barrier + lifetime, no damage logic. **Blocking collision switches on after `BlockingActivationDelay` (0.15s), NOT immediately** — the caught enemies are being shoved out at that exact instant, and a solid volume spawned on top of them traps them inside their own knockback. Same `ECC_Camera` Ignore + mesh-NoCollision + `bDynamicObstacle` treatment as RockWall. Defaults: HalfExtent 350 / HalfHeight 100 / FieldDuration 4.

**GOTCHA FOUND AND GUARDED (applies to any future timed actor): `SetLifeSpan(0)` means "LIVES FOREVER", not "expires instantly".** An unset `FScalableFloat` on the ability evaluates to 0, so both abilities now only override the actor's own duration when `Duration > 0.f`. Without that, forgetting to fill the BP field leaves a permanent wall standing in the arena.
**NAV NOTE:** both actors set `bDynamicObstacle=true` + `SetCanEverAffectNavigation(true)` so AI paths AROUND them. `Config/DefaultEngine.ini` has **no `RuntimeGeneration` setting → navmesh defaults to Static**, so today the AI won't actually re-path; it will physically collide and slide along. Flip the navmesh to **Dynamic** (or Dynamic Modifiers Only) in Project Settings to get real pathing. Not a code bug — a project setting, Reaper's call.

**✅ THE BLUEPRINT AUTHORING PLAN THAT LIVED HERE WAS EXECUTED IN FULL 2026-07-26 session 3** — see the "EARTH 6/6 — FINAL THREE BUILT" section above for the as-built values. No meshes/VFX exist for either; they spawn invisible-but-solid and the debug preview is what you aim with. Fine for playtesting.

## THE BOULDER (Ultimate, 6th Earth spell) — C++ REFERENCE (written session 2, COMPILED & LIVE; `GA_TheBoulder`+`BP_Boulder` built session 3)
Tags already existed and needed no change (`Ability.Earth.TheBoulder`, `Cooldown.Earth.TheBoulder`, the `AbilityTypeToEarthAbilityTag{Ultimate}` entry; `Ability.Earth.LandSlide` also exists and is still UNUSED — a spare).
**4 more new files + an edit to the PlayerController:**
- `Public/Actor/RageInMageBoulder.h` + `Private/Actor/RageInMageBoulder.cpp`
- `Public/AbilitySystem/Abilities/RageInMageBoulderAbility.h` + `Private/.../RageInMageBoulderAbility.cpp`
- `RageInMagePlayerController.h/.cpp` — **NEW MEMBERS on an existing, subclassed class** (see the rebuild warning below).

**SHAPE — Reaper's spec, 2026-07-26: a SPHERE COLLISION *is* the boulder, and the rider attaches to the TOP of that sphere.** Everything derives from ONE number, `CurrentRadius`: the sphere's radius, the mesh's scale, the rider's attach height, AND the actor's height off the ground. **Reaper's second requirement — growth must not sink the boulder through the floor — is satisfied by `centre Z = groundZ + CurrentRadius`, retraced every tick.** As the radius grows the centre RISES by exactly as much, so the sphere always rests tangent to the ground, and it follows slopes for free.
**⚠️ WHY GROWTH IS *NOT* DONE WITH ACTOR SCALE (unlike RockWall/SpikeField/Slingrock):** attached actors inherit their parent's scale, so `SetActorScale3D` would have grown the RIDER along with the boulder. Growth is therefore applied as `SetSphereRadius` + the MESH's own relative scale + a manual `RiderAttachPoint` Z. **Never switch this to actor scale.**
`RiderAttachPoint` is a child of the SPHERE, not the mesh — the mesh spins to fake rolling (pitch, at `v = wr` so a bigger boulder visibly rolls slower) and the rider must not spin with it.
**BEHAVIOUR:** kinematic (no physics sim), so it can't be shoved off course by what it runs over. Rolls forward at `RollSpeed` (450, "slow" by design), never stops. Growth lerps radius 90→220 and a damage multiplier 1→3 over `GrowthDuration`. Runs over non-friendlies (radius overlap, `AlreadyHit` so nobody is hit twice) for damage + forward knockback, and probes ahead with a sphere sweep for world geometry. **Ends on a character hit (`bEndOnCharacterHit`, default TRUE per Reaper's "ends on hitting something (actor/wall)" — expose-flipped to false if that turns out to feel weak for an ultimate)**, on a wall, or at `MaxLifetime` (12s).
**GROWING DAMAGE:** the spec is built once at spawn, but the boulder keeps `BaseDamageByType` and **re-stamps the SetByCaller magnitudes × the current growth multiplier at impact time** — that's how damage can grow off a single spec.
**RIDING:** `Mount(Character)` attaches to `RiderAttachPoint`, `StopMovementImmediately` + `MOVE_None`, and mutual `MoveIgnoreActor`. `Dismount()` detaches, restores `MOVE_Falling`, and gives a small upward hop (`DismountHopStrength` 400) so the rider clears the boulder instead of landing back on it. **The boulder keeps rolling after a dismount** — so the ABILITY ends on the boulder's `OnDestroyed`, NOT on the dismount.
**STEERING — mirrors the DualAim precedent exactly:** `ARageInMagePlayerController::BeginBoulderRide/EndBoulderRide` + a `RiddenBoulder` UPROPERTY, and `Move()` swallows movement and forwards **X only** to `Boulder->AddSteerInput` (there's no forward axis to honour — it never stops). That check sits FIRST in `Move()`, ahead of the dual-aim one. Steer is consumed and zeroed every tick, because `Move()` only fires while the stick is deflected and a leftover value would keep it turning after release.
**STANCE:** bonuses are **NEGATIVE** here (-15/-30/-50) and multiply `GrowthDuration`, so a higher stance reaches full size SOONER. This is the negative-percent case `GetImmovableMassStageScalar` was built for.
**✅ BUILT session 3** (`BP_Boulder` + `GA_TheBoulder`, registered 6th → **Earth 6/6, the 5th completed school**) — as-built values in the "EARTH 6/6" section above. Still open: **`BP_Boulder` has NO mesh assigned** (Reaper's Details-panel step — bridge can't reach native components; `MeshBaseRadius` auto-derives from whatever mesh is picked, falls back to 50 when empty) and **`DismountBoulder` HAS NO INPUT BOUND** (needs an IA + binding to bail early, same open item as `IA_Struggle`).

## DUAL-STICK AIM — BUILT & COMPILED 2026-07-26 (session 2). NO CONSUMER YET. + LIVE CODING CRASH POST-MORTEM.
**This whole section was reconstructed from logs on 2026-07-26 — the session that wrote the code crashed before it could update memory.**

**THE CRASH (what actually happened, so it isn't repeated):** `Saved/Logs/RageInMage-backup-2026.07.26-04.37.52.log` ends abruptly at `04:19:50` UTC, last line `LogClass: UPackage /Script/RageInMage Reload.` — i.e. the editor died **mid Live Coding package reload**, right after `patch_2` linked. NO crash dump was written (`Saved/Crashes` newest is 20 June), so it was a hard kill, not a caught assert. **Cause:** Live Coding was asked to hot-patch a **brand-new UCLASS** (`URageInMageAbilityTask_DualAimMode`) **AND ~10 new members on the already-loaded, heavily-subclassed `ARageInMagePlayerController`** (2 new UPROPERTYs, 3 new UFUNCTIONs) in the same patch — and it was the **THIRD layout-changing patch back to back** (patch_0 04:50, patch_1 05:06, patch_2 05:19). Same failure mode as the 4 July "Live Coding class-layout corruption" incident.
**⚠️ CORRECTION TO THE 2026-07-26 SESSION-1 HEADER NOTE:** "Live Coding now handles new UPROPERTYs/struct members fine" is **TOO BROAD**. It held for adding 3 plain floats to `URageInMageProjectileSpell`. It did NOT hold for new UFUNCTION/UPROPERTY members on `PlayerController` + a new UCLASS in one patch. **RULE: new members on an existing, subclassed class → close the editor and run Build.bat. Reserve Live Coding for function bodies and isolated new plain-data members, and NEVER stack 3 layout-changing patches in one editor run.**
**RECOVERY (already done, verified 2026-07-26):** `Binaries/Win64/UnrealEditor-RageInMage.dll` was fully rebuilt at **05:38:49** (after the crash), and a byte-scan of the DLL confirms `DualAimMode`, `BeginDualAim` and `StanceBonusStage1` are all present. **The code is compiled and live — no work was lost, no rebuild is pending for it.**

**WHAT EXISTS (both halves complete):**
- **`URageInMageAbilityTask_DualAimMode`** (`Public/AbilitySystem/AbilityTasks/` + `Private/...`) — sibling of `RageInMageAbilityTask_AimMode` but every delegate carries a **direction as well as a position**. BP node **`DualAimMode`** (`CreateDualAimModeTask(OwningAbility, MaxRange)`). Delegates: `OnAimUpdate(AimPosition, AimDirection, DeltaTime)` / `OnInputReleased(FinalAimPosition, FinalAimDirection)` / `OnCancelled()`. Lifecycle mirrors AimMode exactly (Activate → bTickingTask, polls `Spec->InputPressed` each tick, EndTask on release, broadcasts Cancel from OnDestroy if never released). Calls `MagePC->BeginDualAim(MaxRange)` on Activate and `EndDualAim()` on OnDestroy.
- **`ARageInMagePlayerController`** — `BeginDualAim/EndDualAim/UpdateDualAim` (`.cpp:382-505`) + BlueprintPure `GetDualAimPoint()` / `GetDualAimDirection()` / `IsDualAiming()`. **Gamepad:** LEFT stick velocity-integrates the point (no snapback, starts ON the caster, clamped to MaxRange, column-traced onto the ground), RIGHT stick sets direction **absolutely** (points where the stick points, no drift). `Move()` **swallows left-stick input entirely** while dual-aiming on gamepad (`.cpp:226`) so the pawn stays put. **M&K:** the point is LOCKED at the cursor on entry, the mouse then only swings the direction around it (with `DualAimMinDirectionDistance`=50 dead zone so it can't flip wildly), release confirms. `PlayerTick` hands over completely to `UpdateDualAim` while active (`.cpp:277`) rather than layering on the single-stick paths. Tunables on the PC: `DualAimPointSpeed`=1200, `DualAimMinDirectionDistance`=50.
- Keeps `CurrentAimWorldPosition` in sync so existing pawn-facing/indicator logic follows the placed point.

**NOT DONE:** **nothing consumes the task** — grep for `DualAimMode` hits only its own 2 files + the PC header. No `RockSolid`/`JaggedTerraForm` folders exist under `/Game/Blueprints/AbilitySystem/Player/Abilities/Earth/` (only ChargingBull, GemJail, Slingrock). No aim-preview indicator is driven off `OnAimUpdate` yet.

## SLINGROCK — FULLY BUILT & CASTABLE 2026-07-26 (3rd Earth spell, Primary). READY TO PLAYTEST.
Assets at `/Game/Blueprints/AbilitySystem/Player/Abilities/Earth/Slingrock/`. **Almost no new code was needed** — the base classes already did the work; only stance scaling was missing.
- **KEY CORRECTION to the old Earth design note:** it claimed Slingrock "needs on-hit knockback added to the rock projectile". **STALE — `ARageInMageProjectile` ALREADY has `KnockbackStrength`/`KnockbackUpwardForce`**, applied via the file-local `ApplyKnockbackToActor()` in BOTH `OnProjectileOverlap` and `ApplyAoEDamage`, and `URageInMageProjectileSpell::SpawnProjectile` already pipes them ability→projectile. Set knockback on the ABILITY, not the projectile BP (the BP value is overwritten at spawn).
- **C++ (hot-patched, live): 3 new `StanceBonusStage1/2/3` floats on `URageInMageProjectileSpell`** (EditDefaultsOnly, `Projectile|ImmovableMass`, **default 0**). In `SpawnProjectile` they resolve one `StanceScalar` via `GetImmovableMassStageScalar(avatar,...)` which is applied to (a) `SpawnTransform.SetScale3D` — scales mesh AND collision, set before both SpawnActorDeferred and FinishSpawning — and (b) `Projectile->KnockbackStrength *= StanceScalar`. **Damage is deliberately NOT scaled** (per locked design). Defaulting to 0 means scalar 1.0, so **every pre-existing projectile spell is provably unaffected** — and any future Earth projectile gets stance scaling for free by just filling in the 3 numbers. Added `#include "AbilitySystem/RageInMageAbilitySystemLibrary.h"` to RageInMageProjectileSpell.cpp.
- **GA_Slingrock** (parent `RageInMageProjectileSpell`): InputTag.**LMB**/Ability.Type.Primary, `Ability.Earth.SlingRock` (⚠️ **capital R in "SlingRock"** — the tag is NOT "Slingrock"), GE_Damage, DamageTypeTags={Bludgeoning=20}, **ActivationOwnedTags=Bludgeoning** (load-bearing), bUseHoldToAim=True, KnockbackStrength=(Value=800), StanceBonusStage1/2/3=**10/20/30**. Cost/Cooldown deliberately **None** (spammable for testing, same as ChargingBull/GemJail).
- **Graph = SwiftGust's proven projectile shape, copied exactly** (7 nodes, compiles clean, 0 warnings): ActivateAbility→CommitAbilityCost→AimMode(Max Range=1500); AimMode.OnInputReleased→SpawnProjectile→EndAbility; AimMode.OnCancelled→EndAbility; AimMode.AimPosition→SpawnProjectile.ProjectileTargetLocation; MakeLiteralGameplayTag(**CombatSocket.Weapon**)→SocketTag; ProjectileClass pin default=BP_Slingrock_C. **NOTE: there is NO Branch on CommitAbilityCost's Return Value** — SwiftGust doesn't branch either, and OnEndAbility stays a disabled/unconnected stub. Don't "fix" this to match the ChargingBull/GemJail shape; it matches its sibling Primary.
- **BP_Slingrock** (child of `RageInMageSphereProjectile`): inherits `InitialSpeed=550`, `ProjectileGravityScale=0` from the C++ ctor, so it flies FLAT and works with no tuning. Added a `RockMesh` StaticMeshComponent using **`/Game/Assets/Enemies/Goblin/Slingshot/SM_SlingshotRock`** (the goblin slingshot rock — no dedicated Earth mesh exists). **⚠️ Its `BodyInstance` had to be forced to `CollisionEnabled=NoCollision` / profile `NoCollision`** — a StaticMeshComponent defaults to `BlockAllDynamic`+`QueryAndPhysics` and would otherwise fight the projectile's own sphere overlap. **Do this on every future projectile mesh.**
- **Registered as EarthMage's 3rd StartupAbility** (ChargingBull, GemJail, Slingrock — round-trip verified).
**PLAYTEST (InputTag.LMB): hold to aim → release → rock flies flat, damages + knocks back on hit. Then stand still to reach Stage 2-3 and re-cast — the rock should be visibly BIGGER and knock enemies FURTHER, but hit for the same damage.**
**NOT DONE:** no impact VFX/SFX (ImpactEffect=None), no cost/cooldown GE, mesh scale untuned (SM_SlingshotRock is goblin-sized — may need a scale bump for a "medium rock"), `bCalculatePitch` left False (set True + a non-zero gravity scale if Reaper wants an arcing throw).

**BRIDGE LEARNINGS (2026-07-26, correcting/extending prior notes):** `read_graph` is a **GLOBAL** `read_graph(path, graph)`, NOT a method on the asset (`a:read_graph` is nil). Each node it returns carries full `connections` / `connections_in` / `connections_out` tables plus per-pin `default`/`connected`/`linked_to` — so you CAN fully reverse-engineer an existing graph's wiring before copying it (this is how SwiftGust's shape was lifted exactly). Pin defaults use **`set_pin(handle, pin_name, value)` / `get_pin` / `reset_pin`** (globals; domain `help('SetPin')`). **`execute_python` is NOT a Lua global here** — save via the asset method **`a:save()`** (and `a:compile()`); both exist on the Blueprint enrichment and on DataAssets. `find_nodes` results print `owning_class`/`_spawner_id` in the tool's OK line but those fields read as **nil from Lua** — don't try to inspect them, just pass the result table straight to `add_node`. Struct **sub-paths do NOT work** in set/get (`a:set(comp,'BodyInstance.CollisionEnabled',...)` → "property not found"); read the whole struct export string, string-replace the field, write it back. `bp.components` is keyed oddly (iterating `pairs` yields both an index and the name).

## CONDITION GEs + ConditionInfo ROWS — BUILT VIA BRIDGE 2026-07-23 (rebuild already landed; only granted-tags remain manual)
**Rebuild CONFIRMED live:** GemJailZone (65 props), ChargingBull (45), 4 Condition*Bonus attrs, all new tags (JaggedTerraForm, 6 Condition.*Immune, 4 Attributes.Conditions.*) verified registered.
**BRIDGE WIN — GE authoring is more scriptable than the old notes said:** `a:get/set('self','DurationMagnitude', ...)` round-trips the FULL export string — you CAN script SetByCaller duration: `(MagnitudeCalculationType=SetByCaller,ScalableFloatMagnitude=(Curve=(),RegistryType="None"),SetByCallerMagnitude=(DataTag=(TagName="X")))`. DurationPolicy scriptable too. **STILL NOT scriptable: granted tags** — `InheritableOwnedTagsContainer` set is WIPED on compile (deprecated field; real storage is the TargetTagsGameplayEffectComponent in `GEComponents`, which reads back as bare object-pointers and can't be written). So GE authoring split = bridge does create+DurationPolicy+DurationMagnitude, Reaper adds the "Grant Tags to Target Actor" component in-editor. Also NEW: on a DATAASSET, `FGameplayAttribute` fields ARE settable via `(Attribute="/Script/RageInMage.RageInMageAttributeSet:PropName")` and `FGameplayTag` empties need `(TagName="None")` (plain `()` gets reverted).
**11 GEs CREATED** at `/Game/Blueprints/AbilitySystem/Shared/Effects/Conditions/` (DurationPolicy=HasDuration + DurationMagnitude=SetByCaller done): condition GEs `GE_Petrified/GE_Grappled/GE_Constricted/GE_Paralysed/GE_Shocked` (SetByCaller = their own Condition.* tag); immunity GEs `GE_FrozenImmunity/GE_PetrifiedImmunity/GE_GrappledImmunity/GE_ConstrictedImmunity/GE_ParalysedImmunity/GE_ShockedImmunity` (SetByCaller = their Condition.*Immune tag). (GE_Stunned/GE_Frozen/GE_StunImmunity already existed.)
**7 ConditionInfo ROWS filled** (DA_ConditionInfo, indices): [1] Frozen (dur3/grace3, heat-reset), [4] Stunned (2/3), [5] Petrified (4/3, **struggle**), [6] Grappled (3/2, **struggle**), [7] Constricted (3/2), [8] Paralysed (2.5/3), [9] Shocked (1.5/2). Each: ConditionTag, ConditionEffect, Category=CrowdControl, BaseIntensity=duration, StunImmunityGraceSeconds, ImmunityEffect, ImmunityTag, **BlockedByConditions=[own immune tag ONLY]** (this is what makes DIFFERENT-CC chaining legal + same-CC spam illegal), bResetMechanicsOnTrigger, bCanStruggleFree, DurationBonusAttribute=ConditionDurationBonus, ImmunityBonusAttribute=ConditionImmunityBonus. **FROZEN FIX DONE:** its BlockedByConditions is now `[Condition.FrozenImmune]` (was `[Condition.Immune, Condition.FrozenImmune]` — I REMOVED `Condition.Immune`; that's damage-type immunity, wrong to gate freezing on it, but ⚠️ FLAG to Reaper in case it was intentional).
**⚠️ REAPER'S REMAINING IN-EDITOR STEP (bridge can't script GEComponents) — add "Grant Tags to Target Actor" to each:** condition GEs grant their own `Condition.X` (Petrified/Grappled/Constricted/Paralysed/Shocked — Frozen/Stunned already have theirs); immunity GEs grant their own `Condition.XImmune` (all 6). Until then the GEs apply but grant no tag → no lock, no immunity. Also verify GE_Frozen still grants ONLY Condition.Frozen (NOT Condition.Stunned — post-CC-refactor, sharing Stunned would break chaining).
**NOT yet done:** Rooted/Silenced rows (movement-only / action-only tiers — lower priority, no spell uses them yet). ConditionName/Description FText left blank. Enemy AI never mashes (serves full struggle duration). GemJailZone applies GE_Petrified DIRECTLY (bypasses ApplyConditionToTarget → no immunity/blocking/scaling, but DOES trigger struggle via CrowdControlTagChanged) — consider routing GemJail through ApplyConditionToTarget later for consistency (C++ change).

## STRUGGLE / MASH-TO-BREAK-FREE (C++ ✅ COMPILED & LIVE — verified 2026-07-26 session 3; still needs its INPUT BINDING)
Reaper confirmed he wants a real input-mash escape (not just duration) for the "but can Struggle free" conditions (Petrified, Grappled per their tag descriptions). **Data-driven off the ConditionInfo row**, implemented on `ARageInMageCharacterBase` next to the CC lock (not a new component — it hooks `CrowdControlTagChanged` directly).
**ConditionInfo row fields (Category="Struggle"):** `bCanStruggleFree` (off by default), `StruggleRequiredProgress` (100), `StruggleProgressPerMash` (10), `StruggleProgressDecayPerSecond` (5). Required/PerMash = presses needed; decay forces FAST mashing rather than patient mashing (keep decay < PerMash × realistic mash rate or escape is impossible).
**CharacterBase API:** `AddStruggleProgress()` (BlueprintCallable — call from input; safe on client, routes via `ServerAddStruggleProgress` RPC), `IsStruggling()`, `GetStruggleProgressPercent()` (0-1), `OnStruggleProgressChanged` (BlueprintAssignable, for a UI bar). Private: `BeginStruggle/EndStruggle/TickStruggleDecay/BreakFreeFromStruggle` + cached row values + `StruggleDecayTimerHandle` (0.1s ticker).
**Flow:** `CrowdControlTagChanged` (authority only) → tag added → `BeginStruggle(tag)` looks up the row, bails unless `bCanStruggleFree`. Mash → progress += PerMash → at Required → `BreakFreeFromStruggle()` → `RemoveActiveEffectsWithGrantedTags(thatTag)` → tag removal re-fires the handler → movement lock released (if no other CC holds it) + `EndStruggle`. A SECOND escapable CC applied mid-struggle takes over and RESETS progress (deliberate — chaining Grapple onto Petrify is a real reset).
**⚠️ REAPER MANUAL STEP:** needs an input binding — create `IA_Struggle` + map it in the IMCs, and call `AddStruggleProgress()` from the PlayerController's handler. No key is wired yet. Also: enemies never mash, so AI simply serves the full duration (add an auto-struggle rate later if wanted). Added `#include "AbilitySystem/Data/ConditionInfo.h"` to CharacterBase.cpp.

## CONDITION SCALING — per-character bonuses via Attributes (C++ ✅ COMPILED & LIVE — all 4 `Condition*Bonus` attrs verified on the AttributeSet 2026-07-26 session 3)
**Problem Reaper posed:** a FireMage passive should raise burning stacks; an item should extend stun-immunity grace; freezes should last longer — WITHOUT rewriting the shared ConditionInfo asset. **Reaper worked out the right answer himself: don't mutate ConditionInfo, modify the values where they're READ.** (Key correction made during the discussion: Reaper initially thought each bonus step would need its own *Condition* — it does not. Bonuses are ATTRIBUTES; ONE attribute holds any value, so N items = N item GEs, not N conditions. Total new game objects = 4 attributes, 0 new conditions.)
**ARCHITECTURE (3 layers):** ConditionInfo DataAsset = shared immutable BASE → Attributes = per-character DELTA (driven by passive/item GEs, so they aggregate + revert + replicate for free) → composed at APPLY TIME. Only 2 read points exist and both now share one resolver so they can't drift: `URageInMageAbilitySystemLibrary::ApplyConditionToTarget` and `URageInMageAttributeSet::ApplyConditionFromData`.
**4 NEW ATTRIBUTES** (Reaper chose the `Attributes.Conditions.*` namespace): `ConditionDurationBonus`, `ConditionImmunityBonus`, `ConditionStackBonus`, `ConditionDamageThresholdBonus` — all replicated + OnRep + DOREPLIFETIME + registered in `TagsToAttributes` (so items/passives can target them by tag like any other attribute). Tags: `Attributes.Conditions.DurationBonus/ImmunityBonus/StackBonus/DamageThresholdBonus`. All default 0 = no change.
**FLEXIBLE (picker) VERSION — Reaper explicitly chose this over the hardcoded one.** `FRageInMageConditionInfo` gains 4 pairs of `FGameplayAttribute BonusAttribute` + `bool bFromTarget` (Duration/Immunity/Stack/DamageThreshold), all under Category="Scaling". `FGameplayAttribute` is editor-exposable → attribute dropdown per row, so re-pointing a condition at a different attribute is an EDITOR change, not a code change. Required `#include "AttributeSet.h"` in ConditionInfo.h.
**`bFromTarget` IS THE LOAD-BEARING DECISION:** false = SOURCE ("my freezes last longer" — offensive investment), true = TARGET ("I stay frozen longer / I resist" — defensive investment). Defaults chosen: Duration=source, Immunity=target, Stack=source, DamageThreshold=target.
**SHARED RESOLVER:** `static float URageInMageAbilitySystemLibrary::ResolveConditionValue(Base, BonusAttribute, bFromTarget, SourceASC, TargetASC, MinClamp=0, MaxClamp=0)` — returns Base untouched when the attribute is unset, guards with `HasAttributeSetForAttribute`, clamps (MaxClamp<=0 = no ceiling). **Clamping is not optional**: enough stacked bonus could otherwise produce a 0-length condition or an effectively permanent immunity window.
**IGNITE STACK CAP FIX (separate plumbing — do NOT confuse with ConditionInfo.MaxStacks, which has NO consumer and is dead):** burning stacks are capped by `URageInMageAbilitySystemComponent::MaxIgniteStacks` (EditDefaultsOnly, default 10) enforced in `AddIgniteStack`. That lives on the **victim**, so reading a bonus off the victim would raise the cap for EVERY attacker at once. Fixed by reading `ConditionStackBonus` off the **INSTIGATOR** (already a param) → `EffectiveMaxStacks = MaxIgniteStacks + instigator's bonus`, min 1.
**HOW A PASSIVE/ITEM ACTUALLY WORKS END-TO-END:** GA_FireMage_Passive → Infinite GE on self → `+3 ConditionStackBonus` → FireMage's ignites cap at 13 while other casters on the same victim still cap at 10. Item: equip → Infinite GE → `+2 ConditionImmunityBonus` on wearer → their immunity window = Duration + Grace + 2; unequip → GE removed → bonus gone, zero bookkeeping.
**STILL OPEN:** no percent/multiplier slot yet (additive only — deliberate, add a second slot if % CC-resistance is ever wanted). `ExpirationDamageThreshold` + `MaxStacks` on the row still have no consumers in C++, so DamageThresholdBonus/StackBonus attributes exist but only StackBonus is actually wired (via the ignite path).

## CROWD-CONTROL REFACTOR — all CCs now self-consuming + per-condition immunity (C++ ✅ COMPILED & LIVE — verified 2026-07-26 session 3)
**Reaper's design goal: stun-locking should be POSSIBLE but require cleverness — you chain DIFFERENT CCs (stun → petrify → freeze) and cannot spam the SAME one.** Previously ALL incapacitation piggybacked on `Condition.Stunned` (so Frozen/Petrified were inert), and one shared `Condition.StunImmune` blocked everything (which would have blocked chaining outright).
**TIER MAP (taken verbatim from the Condition.* tag descriptions):**
- **Full incapacitate** (movement + actions, "unable to Move or make any Action"): Stunned, Frozen, Petrified, Grappled, Constricted, Paralysed, Shocked.
- **Movement only** ("unable to Move", can still act): Rooted.
- **Actions only** ("can not use Abilities", can still move): Silenced.
**IMPLEMENTED:** (1) `ARageInMageCharacterBase` — `BindStunDelegate`/`StunTagChanged` REPLACED by `BindCrowdControlDelegates()`/`CrowdControlTagChanged()` + new `IncapacitationTags` & `MovementOnlyBlockTags` FGameplayTagContainers (EditDefaultsOnly, lazily defaulted in Bind… since native tags aren't guaranteed registered at CDO construction — same pattern as ImmovableMassComponent::HardCCTags) + `GetMovementBlockingTags()`. Binds EVERY movement-blocking tag; the handler **re-evaluates `HasAnyMatchingGameplayTags(all blockers)`** instead of trusting one tag's count. Call sites updated in RageInMagePlayerCharacter.cpp + RageInMageEnemyCharacter.cpp. **🐛 LATENT BUG FIXED IN PASSING:** old code saved `MovementModeBeforeStun` on EVERY lock, so two overlapping CCs saved the already-disabled `MOVE_None` and the character could NEVER move again once both expired. New `bMovementBlockedByCC` latch captures the mode on the FIRST lock only. (2) `URageInMageGameplayAbility` ctor — `ActivationBlockedTags` now includes all 7 incapacitation tags + Silenced (was Stunned only). (3) **PER-CONDITION IMMUNITY:** `FRageInMageConditionInfo` gains `ImmunityEffect` (TSubclassOf<UGameplayEffect>) + `ImmunityTag` (FGameplayTag); new `URageInMageAbilitySystemLibrary::ApplyConditionImmunity(...)` applies them via SetByCaller(ImmunityTag) and **falls back to the shared StunImmunityEffect/Condition.StunImmune when either is unset** (so unmigrated rows behave exactly as before). `ApplyStunImmunity` is now a thin wrapper over it; both `ApplyConditionToTarget` and `ApplyConditionFromData` route through it. (4) 6 new tags: `Condition.FrozenImmune/PetrifiedImmune/GrappledImmune/ConstrictedImmune/ParalysedImmune/ShockedImmune`.
**RULE for authoring a CC condition row:** set `ImmunityTag` = its own immune tag, `ImmunityEffect` = a GE granting that tag (duration = SetByCaller of that same tag), and put **that same tag (and ONLY it)** in `BlockedByConditions`. That is precisely what makes different-CC chaining legal and same-CC spam illegal.
**⚠️ MUST FIX AFTER REBUILD:** I earlier set Frozen's `BlockedByConditions = [Condition.StunImmune]` — that now BREAKS chaining (a stun would block a follow-up freeze). Change it to **`Condition.FrozenImmune`** once the tag is registered (an unregistered tag silently writes an empty container, so it can only be done post-rebuild).
**STILL TO DO (Phase 3, post-rebuild, assets):** create the per-condition immunity GEs + the missing condition GEs, then FILL OUT the 20 empty `ConditionInfos` rows with all the remaining conditions (Reaper's request). Each row needs a ConditionEffect GE; granted tags on GEs remain a manual in-editor step (bridge can't script GEComponents). Also STILL OPEN: the "can Struggle free" mechanic on Petrified/Grappled (input-mash escape) — not designed yet.

## Condition / Heat-stage system — KEY FACTS + Frozen fix (2026-07-22)
- **`FRageInMageConditionInfo::BaseIntensity` IS THE CONDITION'S DURATION.** `ApplyConditionFromData` (AttributeSet.cpp:535) does `AssignTagSetByCallerMagnitude(Spec, CondInfo->ConditionTag, CondInfo->BaseIntensity)` — the condition GE's DurationMagnitude reads SetByCaller keyed to its OWN ConditionTag. BaseIntensity=0 ⇒ the GE applies and expires the same instant (looks like "it never triggers" AND "no duration").
- **FIXED 2026-07-22 (via bridge, DA_ConditionInfo `ConditionInfos[1]` = Condition.Frozen):** BaseIntensity ""→**3.0**, StunImmunityGraceSeconds ""→**3.0**, BlockedByConditions ()→**[Condition.StunImmune]**, bResetMechanicsOnTrigger False→**True**. Mirrors the working OverCharged row [2] (Intensity 2 / Grace 4 / BlockedBy StunImmune / Reset True). All tunable in DA_ConditionInfo.
- **The anti-CC-lock grace needs NO C++** — `ApplyConditionFromData` already checks `BlockedByConditions` (line 516) and already calls `ApplyStunImmunity(BaseIntensity + StunImmunityGraceSeconds)` when `StunImmunityGraceSeconds > 0` (lines 541-546). Set those two fields on ANY condition row to make it non-chainable.
- **⚠️ `Condition.Frozen` AND `Condition.Petrified` ARE BOTH INERT IN C++** — nothing consumes either. ALL incapacitation runs off **`Condition.Stunned`**: movement lock = `BindStunDelegate`→`StunTagChanged` (player+enemy), action lock = `Condition.Stunned` in `URageInMageGameplayAbility::ActivationBlockedTags`. **So GE_Frozen / GE_Petrified MUST ALSO GRANT `Condition.Stunned`** (exactly what GE_OverCharged does) or the victim keeps moving/casting.
- **Frozen (heat stage -4) is gated on ICE damage:** `HandleHeatChange` downgrades -4→-3 unless `IsIceDamage(context)`. That flag is only set in ExecCalc_Damage.cpp:355-357 when a DamageTypeTags key `MatchesTagExact(DamageType.MagicalDamage.Ice)`. So an ice spell must carry that EXACT tag (in DamageTypeTags **and** ActivationOwnedTags) and drive Heat to ≤ -100.
- `FindConditionForMechanicsThreshold` (ConditionInfo.cpp:38) handles negatives correctly (`Thresh<0 && Value<=Thresh`) but is still **first-match-wins** — insert new rows for the same MechanicsTag in descending-magnitude order. Heat currently has only Burning(+100) and Frozen(-100), which don't overlap, so no reorder needed.
- **HOUSEKEEPING:** `ConditionInfos` has 25 entries but **[5]-[24] are 20 empty junk rows** (no ConditionTag/MechanicsTag, default Thresh=100/Intensity=10). Harmless — the MechanicsAttributeTag filter skips them — but worth deleting. Row [4] Condition.Stunned has no MechanicsTag/GE (placeholder; real stuns go via ApplyGuaranteedStun).
- **Debug readout RE-ENABLED 2026-07-22** in `ARageInMageCharacterBase` (BeginPlay timer + `LogMovementSpeedDebug` body uncommented, now prints `[Speed] <Name> | MaxWalkSpeed | Vel | MoveSpeed | Slow%`, amber when Slow>0, red when speed is zeroed). Comment both out again to silence. Follows [[feedback_debug_instrumentation]].

## Earth Mage School — the 6 SPELLS (design LOCKED by Reaper 2026-07-21; build IN PROGRESS, all C++ accumulating into ONE rebuild alongside the Slow attribute)
Phase 3 of Earth. Every spell scales a property off the caster's CURRENT Immovable Mass STAGE (1/2/3). Shared primitive built: `URageInMageAbilitySystemLibrary::GetImmovableMassStage(Actor)` + `GetImmovableMassStageScalar(Actor, s1%, s2%, s3%)` (→ 1 + pct/100; negative pcts = reductions) — universal so projectile/zone/wall/boulder ACTORS can scale too, not just abilities. `UImmovableMassComponent::GetCurrentStage()` now public (mirrors MomentumComponent::GetStage). **DAMAGE TYPE: all Bludgeoning EXCEPT JaggedTerraForm = Piercing.** Load-bearing as always: each GA needs `ActivationOwnedTags` = its DamageType tag + `DamageTypeTags={DamageType.PhysicalDamage.<Bludgeoning|Piercing>=X}`.
- **Primary → Slingrock** — fire a medium rock projectile; damage + knockback on hit. Stance scales projectile SIZE + KNOCKBACK +10/20/30% (NOT damage). Reuses ProjectileSpell + SphereProjectile (like SwiftGust); needs on-hit knockback added to the rock projectile + size/knockback scaled at spawn via the library scalar.
- **Secondary → JaggedTerraForm** (RENAMED from "Jewel Fist Shatter") — punch ground, spikes erupt at a designated area, damage + push enemies in a CHOSEN direction; spikes are an impassable barrier. **Piercing.** Stance scales AREA +15/30/50%. Barrier lifetime = a variable on the GA, default **4s**. DUAL-STICK aim (see below). New spike-field actor (blocking collision) + directional knockback via ApplyKnockbackImpulse(chosen dir).
- **Movement → Charging Bull** — ✅ **C++ WRITTEN THIS SESSION** (`URageInMageChargingBullAbility`, parent damage base). Rock-encased forward charge; plows through enemies (ECC_Pawn pass-through, continuous per-tick sweep, hits each once) for damage + FORWARD knockback. Stance scales BOTH damage + knockback +10/20/30% (via library scalar, captured at charge start). Cooldown-gated (no charges). Modeled on CleanWindDash minus charges. Tunables: CharacterSpeed, DashDistance, DashWidth=150, MaxDashSafetyDuration=2, StanceBonusStage1/2/3=10/20/30. **TODO after rebuild: build GA_ChargingBull (Commit→Branch→BeginChargingBull[unconnected]/EndAbility, OnEndAbility→EndAbility), defaults incl. DamageTypeTags={Bludgeoning=X}+ActivationOwnedTags=Bludgeoning, cost/cooldown GEs, register on EarthMage.**
- **Defensive → Rock Solid** — summon an indestructible wall for 5s. **Blocks ALL damage/spells/movement, omnidirectional (solid both sides), a literal wall to path around.** Stance scales wall SIZE +10/20/30%. DUAL-STICK aim. New `RockWall` actor: model on FireWall/RageInMageWallAbility BUT collision = BLOCKING (FireWall uses overlap), indestructible, blocks all channels. WallAbility already has DrawDebugWallPreview(Location, Rotation).
- **CrowdControl → Gem Jail** — ✅ **ZONE C++ WRITTEN 2026-07-22 (needs rebuild — new class).** `ARageInMageGemJailZone : ARageInMageZone` (`Public/Actor/` + `Private/Actor/`). Deals NO damage (`UsesBuiltinTickDamage()`→false opts out of BOTH the on-entry hit and the tick timer). BeginPlay (authority) starts a one-shot `PetrifyDelay`(2s) timer → `JailEnemiesInside()` petrifies every non-friendly still in `ActorsInside`. Tunables (ExposeOnSpawn): PetrifyDelay=2, PetrifyDuration=4, + `PetrifyEffect` TSubclassOf<UGameplayEffect>. Applies like IceZone does (build spec on TargetASC w/ AddInstigator, SetByCaller(`Condition.Petrified`)=PetrifyDuration, ApplyGameplayEffectSpecToSelf). **KEY BASE-CLASS FACTS (verified):** `ARageInMageZone::BeginPlay` RE-APPLIES `SetSphereRadius(ZoneRadius)` (line 35) so a spawn-time radius really does resize collision (= stance area scaling works), AND it seeds `ActorsInside` with a snapshot of pawns already overlapping at spawn (lines 52-59) — essential since enemies are already inside when the gems form. **⚠️ `Condition.Petrified` IS INERT IN C++** — nothing consumes it. Incapacitation is driven ENTIRELY by `Condition.Stunned`: movement lock = `BindStunDelegate`→`StunTagChanged` (player+enemy), action lock = `Condition.Stunned` in `URageInMageGameplayAbility`'s `ActivationBlockedTags`. **So `GE_Petrified` MUST grant `Condition.Petrified` + `Condition.Stunned`** (exactly what GE_OverCharged does) — that's the project convention and needs no C++. Nuance: both Petrified AND Stunned are in ImmovableMass `HardCCTags`, so an Earth mage hit by petrify fires `HandleHardCC` twice → two converted slows (minor, only matters if enemies petrify the Earth mage). **OPEN DESIGN Q for Reaper: "must break free" = just PetrifyDuration expiring (current impl), or a real struggle/mash-to-escape mechanic (separate feature)?** TODO after rebuild: BP_GemJailZone (+gem VFX), GE_Petrified, GA_GemJail graph (AimMode→Commit→SpawnActor with ZoneRadius = base × `GetImmovableMassStageScalar(avatar,10,20,30)` — that helper is BlueprintPure so NO ability C++ is needed), register on EarthMage.
- **TAG RENAME DONE 2026-07-22 (needs rebuild):** `Ability.Earth.JewelFistShatter`→**`Ability.Earth.JaggedTerraForm`** and `Cooldown.Earth.JewelFistShatter`→**`Cooldown.Earth.JaggedTerraForm`** (members `Ability_Earth_JaggedTerraForm`/`Cooldown_Earth_JaggedTerraForm`, descriptions + the `AbilityTypeToEarthAbilityTag{Secondary}` map entry all updated; grep confirms zero remaining "JewelFistShatter" refs in Source).
- **Ultimate → The Boulder (Landslide)** — summon + RIDE (on top) a slow boulder that auto-rolls forward, STEERABLE but never stops. Grows size + damage the longer it moves. Ends on hitting something (actor/wall) OR a duration (variable). Dismountable — keeps rolling after dismount until hit/duration. Stance REDUCES grow-time -15/30/50% (library scalar with negative pcts). Most novel — new rideable growing actor; design/build LAST.
**DUAL-STICK AIM (JaggedTerraForm + Rock Solid share it) — Reaper's scheme:** controller = left stick positions, right stick sets direction/rotation. **M&K = PRESS to place the point, then mouse sets direction, RELEASE confirms** (press-place / release-confirm gesture, mirrors hold-to-aim). New shared aim task/PC input feature — NOT built yet (the existing RageInMageAbilityTask_AimMode is single-axis position only).
**BUILD ORDER — ✅ COMPLETE 2026-07-26 (session 3):** foundation ✅ → Charging Bull ✅ → Gem Jail ✅ → Slingrock ✅ → dual-stick aim task ✅ → **Rock Solid ✅ + JaggedTerraForm ✅ + The Boulder ✅. EARTH IS 6/6 — the 5th completed school.** Nothing left but meshes/VFX, the DismountBoulder input binding, cost/cooldown GEs, and a PIE playtest of all six.

## Air Mage School — STARTED 2026-07-06 (Reaper directing; design + first spell C++ landed this session)
Tag scaffolding for all 6 Air abilities already existed pre-session in `RageInMageGameplayTag.cpp` (Ability.Air.*, Cooldown.Air.*, `ECharacterClass::AirMage`, `AbilityTypeToAirAbilityTag` map, `Condition.Airborne`). The 6 (slot → name → tag):
- Primary → **Swift Gust** (`Ability.Air.SwiftGust`) — fire a quick small tornado projectile forward, fast travel, damages first enemy hit.
- Secondary → **Catapult Launch** (`Ability.Air.CatapultLaunch`) — short dash; if you hit an enemy you grab their arms, vault over, throw them: damage on impact + damage to enemies they collide with mid-air.
- Movement → **Clean Wind Dash** (`Ability.Air.CleanWindDash`) — medium dash; enemies dashed through are struck + stunned 1s. Max 2 charges.
- Defensive → **Mountain Vortex** (`Ability.Air.MountainVortex`) — wind vortex around you; reflects incoming projectiles back at their caster.
- CrowdControl → **Blue Sky Whirlwind** (`Ability.Air.BlueSkyWhirlwind`) — spin upside-down AOE kick around you, then dash a medium distance and do a second AOE kick. Each kick = damage + knockback to all hit.
- Ultimate → **Dance of the Wind Dragon** (`Ability.Air.DanceOfTheWindDragon`) — become untargetable 3s making 7 attacks; while active, afterimages of you attack any enemies every 1s within a zone created around you on cast.

**DAMAGE TYPE (Reaper's call 2026-07-06): Air spells deal `DamageType.PhysicalDamage.Bludgeoning` "for now"** (NOT Force/magical). Verified Bludgeoning flows fully through `ExecCalc_Damage` physical branch (lines 211-323): resistance-aware (Resistance.PhysicalDamage.Bludgeoning captured+mapped), crit, PhysicalDefence mitigation. CAVEAT: physical branch scales off caster **PhysicalAttack**, not MagicalAttack — INT Air mage gets less scaling there; revisit at tuning. Physical branch uses EGameplayModOp::Override for IncomingDamage (fine, single-type spells). Physical branch adds NO mechanic meter (Momentum is built by movement, not by damage — see below). So each Air GA: `DamageTypeTags = {DamageType.PhysicalDamage.Bludgeoning = <val>}` (no mechanic key), and `ActivationOwnedTags` MUST include `DamageType.PhysicalDamage.Bludgeoning` (the load-bearing ExecCalc convention — same trap that silently zeroed LightningFlash damage).

**MOMENTUM — PHASE 1 C++ BUILT 2026-07-14 (NOT yet compiled; needs Reaper close-editor + Build.bat — new component class + new member).** Reaper asked to build Momentum before finishing ImmovableMass wiring (easier to playtest — pure self-buff, no enemies needed). Modeled on the just-built ImmovableMassComponent (simpler: no CC/knockback/overload). New **`UMomentumComponent`** (`Public/AbilitySystem/Components/MomentumComponent.h` + `Private/.../MomentumComponent.cpp`): gauge 0-120 mirrored to the `Momentum` attribute; **+MomentumPerSecondMoving(10)/s while moving** (location-delta speed > MovingSpeedThreshold 20), **+MomentumPerMovementCast(10) per movement-spell cast** (bound to `ASC->AbilityActivatedCallbacks`, checks the activated ability's `StartupAbilityTypeTag == Ability.Type.Movement` — NO change to the abilities themselves), **decay DecayPerSecond(20)/s after StillnessDecayDelay(1.5s) standing still**; stages 0-3 at 35/70/105 (Still Air/Rising Wind/Gale Force/Tempest); bidirectional stage-GE swap (same machine as ImmovableMass). Public `AddMomentum(float)` for other hooks. Server-authoritative; created on all players, `ActivateMomentum()` AirMage-gated in `InitPlayerAbilityActorInfo`. **CDR TIER DEFERRED** (Stage2 10% / Stage3 20% cooldown reduction): needs a NEW `CooldownReduction` attribute (confirmed absent) + an MMC on every ability's cooldown GE (big cross-cutting change) — build later; v1 = move-speed + attack-speed only (immediately feelable). **3 GE shells created via bridge at `/Game/Blueprints/AbilitySystem/Player/Mechanics/Momentum/` (GE_Momentum_Stage1/2/3, DurationPolicy=Infinite).** REAPER MANUAL STEPS (after rebuild): add modifiers Stage1 MovementSpeed +0.10 / AttackSpeed +0.05; Stage2 +0.20/+0.15; Stage3 +0.30/+0.25 (Op=Add); assign the 3 on the native **MomentumComponent** in BP_RageInMagePlayerCharacter; CharacterClass=AirMage; PIE. (Dash distance rides MovementSpeed for free. ARCHITECTURE NOTE: ImmovableMass + Momentum now share ~40 lines of gauge/stage/GE-swap logic — if a 3rd stance meter appears, extract a `UStanceMeterComponent` base; not worth refactoring the two just-built ones yet.)

**MOMENTUM MECHANIC (Reaper's full design 2026-07-06) — a SELF-BUFF STANCE METER, distinct from Lightning's enemy-debuff Charge.** Built by the Air mage MOVING and casting movement spells. Attribute `Momentum` already exists (0-120, replicated, `HandleMechanicsThreshold(Momentum,0,120,...)` already runs on change in AttributeSet.cpp:261 — but NO ConditionInfo rows consume it yet, so today it just accumulates). 4 stages, each an escalating self-buff:
- Stage 0 **Still Air** — no changes.
- Stage 1 **Rising Wind** — +10% movement speed & dash distance, +5% attack speed.
- Stage 2 **Gale Force** — +20% move speed & dash distance, +15% attack speed, 10% spell cooldown reduction.
- Stage 3 **Tempest** — +30% move speed & dash distance, +25% attack speed, 20% spell cooldown reduction.
FINALIZED NUMBERS (Reaper approved 2026-07-06): (a) **Stage thresholds on 0-120 gauge: Stage0 Still Air = 0-34, Stage1 Rising Wind = 35-69, Stage2 Gale Force = 70-104, Stage3 Tempest = 105-120.** (b) **Gain: +10/sec while moving, +10 per movement-spell cast.** (c) **Decay: after ~1.5s standing still, drain ~20/sec.** (d) **Cooldown reduction = NEW `CooldownReduction` attribute + tiny MMC on each cooldown GE reading it, applied at CAST time (does NOT retroactively shrink an already-ticking cooldown — only spells cast while the stage is active get the shorter cooldown). Reaper explicitly approved this "cast-time" model over live-shrinking.** Move-speed% → additive MovementSpeed modifier (dash distance rides along FREE: dash abilities already compute `DashDistance * MovementSpeed coefficient`, so buffing MovementSpeed also lengthens dashes — matches Reaper listing them at equal %). Attack-speed% → additive AttackSpeed modifier. LIKELY ARCHITECTURE: bespoke system (component or player-char logic) that adds/decays Momentum, computes stage 0-3, and swaps the active stage buff GE on stage change (bidirectional up/down staging — the Charge threshold-cross system does NOT downgrade, so don't reuse it here). Momentum-gain hooks get added to movement + each movement-spell once the subsystem exists.

**SWIFT GUST (Air Primary) — FULLY BUILT 2026-07-09, compiles clean, NOT yet PIE-tested.** 2nd Air spell done (Air school now 2/6). NO new C++ needed — reused `URageInMageProjectileSpell` (graph-driven `SpawnProjectile` BlueprintCallable). Assets built via `create_asset` + graph authoring (NOT duplicate_asset — that's a hard trap, see tooling warning):
  - **`BP_SwiftGust`** (`/Game/Blueprints/AbilitySystem/Player/Abilities/Air/SwiftGust/`) — child of `RageInMageSphereProjectile`; added `TornadoVFX` NiagaraComponent = `NS_Tornado` (from Content/Assets/Effects/Firenado — placeholder, fire-tinted; Reaper may retint/swap), RelativeScale3D 0.35 (small tornado). Base does "damage first enemy hit + 15s lifespan" for free. **SPEED FIX (2026-07-09): the inherited C++ `ProjectileMovement` component's InitialSpeed CANNOT be set via the bridge (`set("ProjectileMovement",...)` → "not found"; native inherited components aren't exposed as BP variables/components to the bridge — `find_nodes("Get Projectile Movement")` = 0 results). WORKAROUND (reusable for any inherited-component tweak): added an EventGraph BeginPlay chain → `Get Component by Class`(ProjectileMovementComponent) → `Set Velocity in Local Space`(New Velocity=(2500,0,0)) so the tornado launches forward fast at spawn. Tunable = the X on that SetVelocityInLocalSpace node. Was base 550 (too slow — Reaper said it looked like "dust drifting"); now 2500.**
  - **`GA_SwiftGust`** — parent `URageInMageProjectileSpell`. MINIMAL functional graph (7 nodes, skipped SearingFlame's ~58-node aim-indicator VFX subgraph): ActivateAbility→CommitAbilityCost→AimMode(MaxRange=1500); AimMode.OnInputReleased→SpawnProjectile→End Ability; AimMode.OnCancelled→End Ability; AimMode.AimPosition→SpawnProjectile.ProjectileTargetLocation; MakeLiteralGameplayTag(`CombatSocket.Weapon`)→SpawnProjectile.SocketTag; SpawnProjectile.ProjectileClass=BP_SwiftGust. Used `CommitAbilityCost` (mana-only, spammable primary, NO cooldown) not full CommitAbility. Defaults: StartupInputTag=InputTag.LMB, StartupAbilityTag=Ability.Air.SwiftGust, Type=Primary, DamageEffectClass=GE_Damage, **DamageTypeTags={DamageType.PhysicalDamage.Bludgeoning=15}** (placeholder), **ActivationOwnedTags=Bludgeoning** (load-bearing), bUseHoldToAim=True, CostGameplayEffectClass=GE_Cost_SwiftGust.
  - **`GE_Cost_SwiftGust`** — Blueprint GE (Instant), Mana modifier −1×CT_ManaCost row "SwiftGust". CT_ManaCost row "SwiftGust" added = 10→14 over lvl 1-5 (cheaper than dash's 20→28; spammable primary). **GOTCHA: `create_asset(path,"GameplayEffect")` makes a NON-Blueprint GE asset (can't be a TSubclassOf) — must use `create_asset(path,"Blueprint",{ParentClass="/Script/GameplayAbilities.GameplayEffect"})` instead.** GE compile ERRORED until the curve row existed — create the CT row BEFORE compiling the cost GE.
  - **Registered on AirMage** in DA_CharacterClassInfo (now 2 StartupAbilities: CleanWindDash + SwiftGust). **TOOLING WIN — supersedes the old map_set-whole-struct workaround:** `a:set("CharacterClassInformation{AirMage}.StartupAbilities", "(...array export...)")` writes the sub-path DIRECTLY (map `{key}.field` path works for get AND set — only `array_add`/`array_count` can't traverse it). No need to rewrite the whole struct.
  - NOT DONE (optional polish, non-blocking): no UAbilityInfo entry (spell-menu UI metadata); no cast montage; projectile speed manual tweak above.
- **PIER-QUEUE:** PIE-test Swift Gust (aim with LMB hold, release → tornado flies forward, hits first enemy for Bludgeoning, costs ~10 mana) alongside CleanWindDash.

**BLUE SKY WHIRLWIND (Air CrowdControl) — FULLY BUILT 2026-07-09 (C++ compiled via Reaper's Ctrl+Alt+F11 Live Coding, class live = 42 entries; GA built+registered), compiles clean, NOT yet PIE-tested.** 3rd Air spell done; Air school now 4/6 (CleanWindDash, SwiftGust, BlueSkyWhirlwind + old CleanWindDash... = CleanWindDash/SwiftGust/BlueSkyWhirlwind built, 2 remain: Mountain Vortex, Catapult Launch, Dance of Wind Dragon = 3 remain actually). GA_BlueSkyWhirlwind: parent=new class; graph = ActivateAbility→CommitAbility→Branch(RV); True→BeginBlueSkyWhirlwind (then UNCONNECTED, C++ self-ends); False→EndAbility; OnEndAbility→EndAbility. Defaults: InputTag.3, Ability.Air.BlueSkyWhirlwind, Type.CrowdControl, DamageEffectClass=GE_Damage, DamageTypeTags={Bludgeoning=20}, ActivationOwnedTags=Bludgeoning (load-bearing), KnockbackStrength=1000, KickRadius=350, CharacterSpeed=2500, DashDistance=600, bUseHoldToAim=False, Cost=GE_Cost_BlueSkyWhirlwind. Registered on AirMage (now 3 StartupAbilities).
  - **TWO MANUAL FOLLOW-UPS for Reaper (cooldown):** (1) add the granted `Cooldown.Air.BlueSkyWhirlwind` tag component to GE_Cooldown_BlueSkyWhirlwind (Component: "Grant Tags to Target Actor" — the bridge can't script this; same as StaticMegaSurge). (2) THEN set GA_BlueSkyWhirlwind's CooldownGameplayEffectClass = GE_Cooldown_BlueSkyWhirlwind. **Currently CooldownGameplayEffectClass=None** — I had to clear it because GAS HARD-ERRORS at GA compile if the assigned cooldown GE grants no tags ("must grant tags (Component: Grant Tags to Target Actor) to be used as cooldown"). So the ability works & is spammable (mana-gated only) until Reaper does both steps.
  - **NOT DONE (polish):** no "spin upside-down kick" montage/VFX; kicks are instant radial bursts (no anim timing between kick1→dash→kick2).
  - **COMPILE-LOG TOOLING WIN:** `read_log('compile', {asset='/Game/.../GA_X'})` DOES surface real BP compile errors (severity+text) — NOT a useless stub for Blueprints (only the livecoding/generic variants are stubs). Use it to diagnose `a:compile()==false`.

(superseded — see FULLY BUILT line above) Original in-progress note: **C++ WRITTEN 2026-07-09, NEEDS COMPILE (blocked), then GA build.** 3rd Air spell (in progress). Design: radial AOE "kick" (damage+knockback) around caster → medium forward dash → 2nd radial kick at landing. New class **`URageInMageBlueSkyWhirlwindAbility`** (`.h`/`.cpp`), parent `URageInMageDamageGameplayAbility`. Reuses CleanWindDash's dash mechanic (BaseWalkSpeed boost, MovementSpeed-scaled distance, ECC_Pawn pass-through) but NO charge system — cooldown-gated. Public `BeginBlueSkyWhirlwind()` for the GA graph. `DoKick(Origin)` = GetLivePlayersWithinRadius(KickRadius) → per non-friendly: CauseDamage + ApplyKnockback(target, Origin) (both from the damage base; knockback tuned on shared KnockbackStrength/KnockbackUpwardForce), authority-gated. Kick1 in BeginBlueSkyWhirlwind, dash via 60fps DashTick, kick2+K2_EndAbility when distance reached. Tunables: KickRadius, CharacterSpeed, DashDistance, MaxDashSafetyDuration=2, bShowDebug. InstancedPerActor.
  - **BLOCKER (2026-07-09): the new class did NOT auto-compile.** Writing .h/.cpp via the Write tool did NOT trigger the editor's on-save Live Coding this time (`class_properties('RageInMageBlueSkyWhirlwindAbility')` → "could not resolve class"; CleanWindDash still resolves fine at 46 entries, so class_properties works — it's specifically the brand-new files needing a compile). External filesystem writes apparently don't fire the on-save watcher. **Reaper must trigger a Live Coding compile (Ctrl+Alt+F11 in-editor) — or a full Build.bat rebuild — to bring the class live. THEN I finish: create GA_BlueSkyWhirlwind (parent the new class), wire graph (ActivateAbility→CommitAbility→BeginBlueSkyWhirlwind, leave unconnected — C++ self-ends; OnEndAbility→EndAbility), set defaults, assign cost+cooldown GEs, register on AirMage.**
  - **DONE (class-independent):** `GE_Cost_BlueSkyWhirlwind` (Instant, Mana −1×CT row "BlueSkyWhirlwind"; CT_ManaCost row added = 35→45 over lvl1-5, compiles clean). `GE_Cooldown_BlueSkyWhirlwind` created (HasDuration, 12s) — **BUT its granted `Cooldown.Air.BlueSkyWhirlwind` tag is NOT set: setting `InheritableOwnedTagsContainer` via bridge gets CLEARED on compile (deprecated field), and the GE blueprint handle does NOT expose add_object/set_object (those are DataAsset-only) so the `TargetTagsGameplayEffectComponent` can't be scripted. MANUAL STEP (same as StaticMegaSurge): Reaper must add the granted cooldown tag component in-editor, else the 12s cooldown grants no tag and won't block reactivation.**

**MOUNTAIN VORTEX (Air Defensive) — FULLY BUILT 2026-07-09 (C++ compiled fine — Reaper confirmed; ability class live=37 entries, ReflectFrom method live; GA built+registered), compiles clean, NOT yet PIE-tested.** 4th Air spell done; Air school now 4/6 (remaining: Catapult Launch/Secondary, Dance of the Wind Dragon/Ultimate). GA_MountainVortex: parent=new class; graph ActivateAbility→CommitAbility→Branch(RV) True→BeginMountainVortex[unconnected] False→EndAbility; OnEndAbility→EndAbility. Defaults: InputTag.2, Ability.Air.MountainVortex, Type.Defensive, VortexDuration=3, VortexRadius=450, bUseHoldToAim=False, ActivationOwnedTags=Condition.Reflecting (state/VFX only, not load-bearing), Cost=GE_Cost_MountainVortex, Cooldown=None. Registered on AirMage (now 4 StartupAbilities). **SAME cooldown manual step as BlueSky: add granted Cooldown.Air.MountainVortex tag component to GE_Cooldown_MountainVortex, then assign it as CooldownGameplayEffectClass. No VFX yet (the vortex is invisible — needs a wind Niagara).** PIE-test idea: stand in front of a Goblin_Slingshot/ranged enemy, cast, watch its shots reverse and damage it.
Original in-progress note (superseded): **C++ WRITTEN 2026-07-09, NEEDS COMPILE.** 4th Air spell (in progress). Design: for VortexDuration a wind vortex around caster reflects incoming ENEMY projectiles back at their shooter. Deals no damage itself — reflected projectiles carry their own damage back.
  - **NEW projectile-reflect infra (`Condition.Reflecting` had NO consumer before this):** added `void ARageInMageProjectile::ReflectFrom(AActor* NewInstigator)` (public UFUNCTION) to the SHARED base `RageInMageProjectile.h/.cpp`. It: aims velocity back toward the old shooter (fallback = reverse heading), sets actor rotation, FIXES the collision ignore-list (`GetCollisionComponent()->IgnoreActorWhenMoving(oldShooter,false)` + ignore the reflector — else the reflected shot phases through the very shooter it should hit), and `SetInstigator(NewInstigator)`/`SetOwner` so OnProjectileOverlap's `OtherActor==GetInstigator()` + `IsBothEnemy(GetInstigator(),Other)` checks flip (now damages the shooter's team, spares the reflector). Damage spec source stays the enemy (fine — deals the enemy's own damage back). **Because this adds a UFUNCTION to the already-loaded ARageInMageProjectile UCLASS, Live Coding may reject → full Build.bat rebuild (Reaper: try Ctrl+Alt+F11 first, fall back to Build.bat).**
  - **New ability `URageInMageMountainVortexAbility`** (`.h`/`.cpp`), parent `URageInMageGameplayAbility` (NOT the damage base — deals no damage). InstancedPerActor. `BeginMountainVortex()` (BlueprintCallable) starts a ScanInterval(0.05s) repeating timer + a VortexDuration one-shot that K2_EndAbilitys. `ScanAndReflect()` (authority-only): `GetAllActorsOfClass(ARageInMageProjectile)` (bulletproof vs varied collision profiles) within VortexRadius, for each with a hostile instigator (`!IsFriendly(caster, shooter)`) → `ReflectFrom(caster)`. Reflected shots self-exclude next scan (instigator becomes caster → IsFriendly true). Tunables: VortexDuration, VortexRadius, ScanInterval=0.05, bShowDebug.
  - **DONE (class-independent):** GE_Cost_MountainVortex (Instant, Mana −1×CT row "MountainVortex" = 25→35 lvl1-5, compiles). GE_Cooldown_MountainVortex (HasDuration 14s) — **granted `Cooldown.Air.MountainVortex` tag is the SAME manual step as BlueSky (add "Grant Tags to Target Actor" component in-editor), then assign as GA CooldownGameplayEffectClass; leave None until then or the GA won't compile.**
  - **AFTER COMPILE:** create GA_MountainVortex (parent new class; graph ActivateAbility→CommitAbility→Branch(RV) True→BeginMountainVortex[then unconnected, C++ self-ends] False→EndAbility; OnEndAbility→EndAbility), defaults: InputTag=? (Defensive slot — check convention, Fire Defensive RisingFlameWave uses InputTag.?), Ability.Air.MountainVortex, Type.Defensive, VortexDuration~3, VortexRadius~450, bUseHoldToAim=False, no DamageTypeTags/ActivationOwnedTags needed (deals no damage; optionally ActivationOwnedTags=Condition.Reflecting for VFX/state). Register on AirMage (4th). No cost-tag load-bearing issue since no damage.

**✅ "suddenly NO damage anywhere" bug — RESOLVED 2026-07-11, Reaper confirmed.** Turned out to be a stale in-editor/Live-Coding state, not a real code/data bug — closing the editor and doing a full rebuild made it go away on its own. No root cause needed further chasing (never actually traced to team/tag logic, ExecCalc, or any of the ruled-out suspects below — just a stuck editor session). Kept the original write-up below for history in case something *similar* resurfaces (same symptom next time should NOT be assumed to be a stale-session issue by default — verify first).
Original parked note (2026-07-09): "suddenly NO damage anywhere" — no player spell damages enemies AND no enemy damages the player. Reaper hit this mid-session while playtesting/working on Swift Gust VFX. RULED OUT at the time: my code changes are projectile-only (`ReflectFrom`) which can't affect enemy MELEE damage; DA_CharacterClassInfo shared attribute GEs (Secondary/Vital/Resistance/ItemSpecific/ConditionInfo) all INTACT; GE_Damage still wired to ExecCalc_Damage (Executions=ExecCalc_Damage, DurationPolicy=Instant). NOTE: Dance of the Wind Dragon REMOVES the caster's "Player" tag during its 3s (restored on end) — unrelated to this bug (predates it) but keep in mind while debugging tag issues in general.

**CATAPULT LAUNCH (Air Secondary) + DANCE OF THE WIND DRAGON (Air Ultimate) — FULLY BUILT 2026-07-11 (C++ compiled — Reaper's earlier Ctrl+Alt+F11 landed both classes live, 46/43 entries; both GA Blueprints built+wired+registered this session), compiles clean, NOT yet PIE-tested. AIR SCHOOL NOW 6/6.** See the top-of-file NEWEST note for full details (values chosen, graph pattern used, pending cooldown-tag manual steps). Final 2 Air spells; Air = 6/6 (CleanWindDash, SwiftGust, BlueSkyWhirlwind, MountainVortex, CatapultLaunch, DanceOfTheWindDragon).
  - **Catapult Launch** (`URageInMageCatapultLaunchAbility`, parent damage base): short dash (reuses CleanWindDash dash) pre-sweeps for the NEAREST enemy, dashes to it, then `DoThrow()`: CauseDamage + `ApplyKnockbackImpulse` launching the victim **backward relative to ITS OWN facing** (`-Enemy->GetActorForwardVector()`, +ThrowUpwardForce arc) — Reaper's spec "thrown backward to its current facing, mage vaults over & throws over his shoulders". Then tracks the flying victim for ThrowTrackDuration(1s) via 60fps timer, damaging+knocking fresh enemies within ThrowCollisionRadius(150) (the "mid-air collision" hits). Authority-gated. Tunables: CharacterSpeed, DashDistance, DashWidth=150, ThrowStrength, ThrowUpwardForce=0.5, ThrowTrackDuration=1, ThrowCollisionRadius=150. InstancedPerActor.
  - **Dance of the Wind Dragon** (`URageInMageDanceOfTheWindDragonAbility`, parent damage base): on cast captures ZoneCenter=cast loc, becomes UNTARGETABLE by removing the caster's "Player" ActorTag (restored in EndAbility), fires NumAttacks(7) evenly-spaced zone pulses over Duration (each: CauseDamage+ApplyKnockback to non-friendlies within ZoneRadius of ZoneCenter), and spawns cosmetic `AfterimageActorClass` every AfterimageInterval(0.5s) at the caster (Reaper: afterimages cosmetic, C++ does damage). Authority-gated damage. Tunables: Duration, NumAttacks=7, ZoneRadius, AfterimageInterval=0.5, AfterimageActorClass(TSubclassOf, null=none). InstancedPerActor.
  - **DONE (class-independent):** GE_Cost + GE_Cooldown for both + CT rows: CatapultLaunch cost 30→40 / cooldown 8s; DanceOfTheWindDragon cost 60→80 / cooldown 45s. All compile. **Cooldown GEs need the SAME manual granted-tag step (Cooldown.Air.CatapultLaunch / Cooldown.Air.DanceOfTheWindDragon) before assigning as CooldownGameplayEffectClass.**
  - **AFTER COMPILE:** build GA_CatapultLaunch (InputTag.RMB? check Secondary convention — Fire Secondary ChanneledFire uses InputTag.?; SearingFlame Primary=LMB so Secondary likely RMB) + GA_DanceOfTheWindDragon (Ultimate input — Fire Ultimate FallOfTheScorchingSun uses InputTag.?). Graph = Commit→Branch→Begin[unconnected]/EndAbility, OnEndAbility→EndAbility. Both need DamageTypeTags={Bludgeoning=X} + ActivationOwnedTags=Bludgeoning (load-bearing!). Catapult bUseHoldToAim=False (dashes in facing). Dance bUseHoldToAim=False. Register both on AirMage (→6 total).

**PROGRESS THIS SESSION (Air spell 1, 2026-07-06):**
- **Clean Wind Dash — C++ DONE, compiled via Live Coding, class live (`class_properties` → 46 entries).** New class `URageInMageCleanWindDashAbility` (`.h`/`.cpp`), parent `URageInMageDamageGameplayAbility`. Near-clone of the proven `URageInMageZipNZapAbility` dash+2-charge template (InstancedPerActor, CurrentCharges, self-re-arming recharge timer, velocity-driven DashTick raising BaseWalkSpeed to CharacterSpeed, path SweepMultiByChannel on ECC_Pawn). On-hit payload differs: `CauseDamage` + `URageInMageAbilitySystemLibrary::ApplyGuaranteedStun(SourceASC, TargetASC, Avatar, StunDuration, StunGraceSeconds)` per enemy crossed (defaults StunDuration=1, grace=0.5), instead of ZipNZap's CauseChainDamage. Tunables: MaxCharges=2, RechargeTime=5, DashDistance (set ~750 on BP), CharacterSpeed, DashWidth=150. Public `BeginCleanWindDash()` for the GA graph to call. Tunables: MaxCharges=2, RechargeTime=5, DashDistance (set ~750 on BP), CharacterSpeed, DashWidth=150.
  - **BLUEPRINT PROGRESS (partial — interrupted by the duplicate_asset bridge hang):** `GA_CleanWindDash` **created** at `/Game/Blueprints/AbilitySystem/Player/Abilities/Air/CleanWindDash/GA_CleanWindDash` (parent=`/Script/RageInMage.RageInMageCleanWindDashAbility`, currently just the 2 default event nodes). `GE_Cost_CleanWindDash` **NOT yet created** — the `duplicate_asset` from GE_Cost_ZipNZap hung the bridge (see tooling warning); recreate it with `create_asset(... 'GameplayEffect' ...)` or copy structure by hand instead.
  - **CLEAN WIND DASH NOW FULLY BUILT (2026-07-06, post-rebuild session) — compiles clean, saved, NOT yet PIE-tested.** All 5 remaining steps done:
    (1) GE_Cost_CleanWindDash created by Reaper; cooldown GE left UNASSIGNED (charges, not cooldown — confirmed None). (2) GA class defaults set via a:set export-strings: StartupInputTag=InputTag.1, StartupAbilityTag=Ability.Air.CleanWindDash, StartupAbilityTypeTag=Ability.Type.Movement, bUseHoldToAim=False, DamageEffectClass=GE_Damage, **DamageTypeTags={DamageType.PhysicalDamage.Bludgeoning=15}** (placeholder val — tune later), **ActivationOwnedTags=Bludgeoning** (load-bearing), CharacterSpeed=3000, DashDistance=750, CostGameplayEffectClass=GE_Cost_CleanWindDash. (3) Graph wired = ActivateAbility→CommitAbilityCost→Branch(RV); True→BeginCleanWindDash (**then left UNCONNECTED — C++ K2_EndAbility ends it when dash finishes, per the dash-fix**); False→EndAbility; OnEndAbility→EndAbility. (4) Registered on AirMage in DA_CharacterClassInfo via `map_set` (had to rewrite whole struct preserving ProgressBarColor/BG material/XPReward — `array_add` won't traverse a `Map{key}.field` path; AirMage now has 1 StartupAbility). (5) Added CT_ManaCost row "CleanWindDash" (20→28, mirrors ZipNZap) — **GE_Cost_CleanWindDash's Mana modifier was pointing at row "IgnitionLeap" (Fire leftover from Reaper's hand-duplication) and CT's CurveTable IS wired so it really charged that; repointed RowName→"CleanWindDash"** (had to rewrite the whole `Modifiers` export string — indexed dotted path `Modifiers[0]...` is not settable).
  - **CT_ManaCost tooling note:** it's a CurveTable — use `a:list("rows")`, `a:list("keys",{row=..})`, `a:add("row",{name=,mode="simple",keys={{time=,value=}}})`. All 14 existing rows are 5-key Linear/simple. (Contrast the old SearingFlame caveat where the Curve.CurveTable ref was null → flat -1 fallback; the Lightning/Air cost GEs DO have the CurveTable ref set, so their row values are live.)

**TOOLING learned this session (IMPORTANT — corrections to earlier assumptions):**
- NeoStack `execute_script` (Lua bridge) only surfaces its own `[OK]/[ERROR]/[FAIL]` log lines — a bare `return <string/table>` is NOT shown. To dump arbitrary data for inspection, `error("\n"..text)` (surfaces as `[ERROR]`). `read_graph(path, graph)` / `class_properties(name)` are the workhorses; `read_log('livecoding')`/`read_log('compile')` are near-useless stubs.
- **`execute_python` is NOT available as a Lua global in this bridge** (tested: `type(execute_python)==nil`). The system-prompt's "execute_python is available" does NOT hold here — do not rely on it. Corollary: my earlier belief that I triggered the Clean Wind Dash compile via `execute_python("LiveCoding.Compile")` was WRONG (that call errored silently inside a pcall). **The class actually compiled because this editor AUTO-COMPILES C++ on source-file save (Live Coding on-save is on).** So the real C++ workflow here = write `.h`/`.cpp` with the Write tool, then just wait/verify with `class_properties('ClassName')` (`-> N entries` = loaded; `[FAIL] could not resolve class` = not yet). No manual compile trigger found/needed.
- **Asset-op globals that exist: `duplicate_asset(src,dst)`, `delete_asset`, `rename_asset`, `create_asset`, `open_asset`, `find_assets`.** Saving/compiling a Blueprint is via the opened-asset object methods (`a:compile()`, `a:save()`), not a global `save_asset` (that's nil).
- **WARNING: `duplicate_asset` HANGS THE BRIDGE — CONFIRMED ON BLUEPRINTS TOO (2026-07-09), not just GameplayEffects.** Tried `duplicate_asset(GA_Player_SearingFlame → GA_SwiftGust)` to clone the projectile aim-indicator graph; the call timed out AND froze the bridge (subsequent `error("ping")` also timed out), and NO asset landed on disk. `create_asset` works fine. **DO NOT USE `duplicate_asset` AT ALL — treat it as a hard trap.** It pops an in-editor modal that Reaper must dismiss to free the bridge. To clone a complex GA instead: `create_asset(path,'Blueprint',{ParentClass=...})` then rebuild the graph via add_node/connect. There is NO cross-blueprint node copy (`duplicate_nodes` is same-graph only; no `copy_asset`/`save_asset` globals — save via `a:save()`). (Betide Discord report warranted — recurring.)

## Dash abilities (ZipNZap + CleanWindDash) — "doesn't dash" bug + pass-through collision (CODE WRITTEN 2026-07-06, NEEDS FULL REBUILD — new member added, then PIE-test)
Reaper reported ZipNZap (Lightning Movement) "doesn't make the character dash" and was confused about a cooldown he'd just made. Two findings + one new feature, applied IDENTICALLY to both dash classes (`URageInMageZipNZapAbility` and its near-clone `URageInMageCleanWindDashAbility`):
1. **Cooldown vs charges (explanation, no code):** these abilities intentionally use a C++ **charge system** (MaxCharges=2, self-re-arming RechargeTimer, `CanActivateAbility` gates on `CurrentCharges<=0`, InstancedPerActor), NOT a GAS cooldown GE. A cooldown GE fights it (blocks ALL reactivation regardless of charges). GA_ZipNZap's `CooldownGameplayEffectClass` is (correctly) **None** — confirmed via get. Reaper made a `GE_Cooldown_ZipNZap` asset but it is NOT assigned to the GA; leave it unassigned. Design decision: KEEP charges, DROP cooldown.
2. **ROOT CAUSE of "doesn't dash":** the graph flow was `...Branch(True) → BeginZipNZap → End Ability`. `BeginZipNZap()` starts the dash on a 60fps **timer** (async) and returns immediately; the very next node **End Ability** fired → `EndAbility` override calls `EndZip()` which clears the dash timer + reverts BaseWalkSpeed on the SAME FRAME the dash started → dash killed instantly. (Never PIE-tested before, so latent.) **Fix:** (a) removed the `BeginZipNZap.then → End Ability.execute` graph connection (via `disconnect_from`, compiled+saved) so the ability stays active during the dash; (b) `ZipTick`/`DashTick` now call `K2_EndAbility()` when the dash completes (distance reached or safety timeout) or when MoveComp is null — the ability ends ITSELF when the dash finishes, EndAbility→EndZip still does cleanup and still handles external cancel (death/stun mid-dash); (c) BeginZipNZap early-out (no character) also calls `K2_EndAbility()` so it can't hang active.
3. **NEW: pass-through collision during dash (Reaper request — anime-style phase through actors, re-block on end).** On dash start, capsule's response to `ECC_Pawn` is saved then set to `ECR_Ignore` (character phases through other pawns; walls are WorldStatic so still block, safety timer still bails on a wall); restored to the saved response in `EndZip`/`EndDash`. New member `TEnumAsByte<ECollisionResponse> SavedPawnCollisionResponse` on both classes + `#include "Components/CapsuleComponent.h"` in both .cpp and `#include "Engine/EngineTypes.h"` in both .h.
**Files:** `RageInMageZipNZapAbility.h/.cpp`, `RageInMageCleanWindDashAbility.h/.cpp`, `GA_ZipNZap` graph (connection removed, saved). **REBUILD LANDED 2026-07-06 — Reaper ran the full `Build.bat`, both classes live (`class_properties` → 46 entries each).** Dash-fix code is now compiled but STILL NOT PIE-TESTED — top open item: PIE-test ZipNZap (zip forward ~600u, phase through pawns, chain-zap once, 2 charges) AND CleanWindDash (now fully wired, see Air section). CleanWindDash's graph was wired the CORRECT way (BeginCleanWindDash.then left unconnected; C++ ends the ability).

## Controller support regressions — bootstrap deadlock (FIXED & PIE-confirmed 2026-07-04) + chord-spell no-release (CODE WRITTEN 2026-07-04, pending Live Coding compile + PIE test)
Two separate controller bugs found/fixed this session. Both edits are function-body/template-body only (no member/signature/layout changes) → Live Coding hot-patchable, no full rebuild needed.
1. **Bootstrap deadlock (FIXED, Reaper confirmed "the controller was working" + spells firing).** Controller couldn't move the character or fire at all. Root cause in `RageInMagePlayerController.cpp`: `BeginPlay` only added `IMC_MAKContext` (priority 0), which has ZERO gamepad mappings. `IMC_ControllerContext` (left-stick→IA_Move, right-stick→IA_Look, triggers/buttons→abilities) was added only inside `UpdateControllerIMC()` and only once `bUsingGamepad` was already true — but `bUsingGamepad` flips true ONLY in `Look()`, which fires only from `IA_Look` = `Gamepad_Right2D`, a mapping that lives ONLY in the never-added controller context. Total chicken-and-egg: the context that detects the gamepad can't activate until the gamepad is detected. (Even the saved-keybinding path `ApplyControllerCustomKeybindings` was gated behind the same `bControllerIMCActive` flag, so it couldn't bootstrap it either.) Regressed from the documented original design (Architecture Notes ~line 543: "Both IMCs active simultaneously"). **Fix:** `BeginPlay` now adds `ControllerMageContext` at priority 1 and sets `bControllerIMCActive = true`, BEFORE `LoadAndApplyKeybindings()` (so the runtime-rebind swap still works — sees the flag true and swaps in the runtime copy). The two contexts map disjoint physical keys (keyboard/mouse vs `Gamepad_*`) so both-active is conflict-free. `UpdateControllerIMC()` gutted to a no-op (comment retained) — it used to tear the controller context down on M&K switch, which would re-create the deadlock in reverse (couldn't switch back to gamepad because right-stick Look lives in the torn-down context). Device detection (`bUsingGamepad`) still drives cursor visibility / aim mode / which InputConfig+IMC the UI reads for key icons — just no longer touches mapping contexts. Assets were all fine (both IMCs exist; all `BP_RageInMagePlayerController` defaults assigned). NOTE UE5.8: IMC key mappings now live under `DefaultKeyMappings.Mappings` (legacy top-level `Mappings` array reads empty via NeoStack `array_count` — not a bug, runtime reads DefaultKeyMappings fine).
2. **Chord spells aim but never release (CODE WRITTEN, pending compile+test).** Controller chord abilities (modifier LB + button, e.g. LB+RT = Ultimate; Defensive/CrowdControl are the other `InputTriggerChordAction` entries in `IMC_ControllerContext`) activate and show their hold-to-aim indicator but never fire. Activation flows through `Triggered`/Held (`AbilityInputTagHeld`→TryActivate + sets `Spec.InputPressed=true`); release flows ONLY through `Completed` (`AbilityInputTagReleased`→clears `Spec.InputPressed`). The `AimMode` task (`RageInMageAbilityTask_AimMode.cpp`) polls `Spec->InputPressed` each tick and fires `OnInputReleased` when it goes false. But a chord-triggered action fires **`ETriggerEvent::Canceled`, NOT `Completed`**, when the chord breaks on release — and `BindAbilityActions` (`RageInMageEInputComponent.h` template) only bound `Completed→ReleasedFunc`. So chord releases were dropped, `InputPressed` stayed true forever. Plain Pressed triggers (M&K + non-chord controller) emit `Completed` normally → they worked. **Fix:** added a second `BindAction(..., ETriggerEvent::Canceled, ..., ReleasedFunc, Action.InputTag)` alongside the Completed binding. Harmless for non-chord bindings (Pressed triggers don't emit Canceled). **NOT yet PIE-tested.** If still broken, WATCH: possible separate issue — RT is shared by IA_Ability_Secondary (no chord) AND IA_Ability_Ultimate (chord), so LB+RT may double-fire Secondary unless a `UInputTriggerChordBlocker` is on the non-chord action (Reaper didn't report double-fire, so blockers are probably configured — but verify Secondary doesn't also trigger). **Chord fix PIE-CONFIRMED working by Reaper 2026-07-04.**
3. **Gamepad aim reworked to a velocity-driven virtual cursor (CODE WRITTEN 2026-07-04, pending compile+test; MAY NEED FULL REBUILD — new members added).** Evolved over two passes this session:
   - Pass A (superseded): removed the `* StickMagnitude` scaling so aim was pure DIRECTION at fixed `AimProjectionDistance`. Killed the original snapback (magnitude scaling had dragged the point toward the caster as the stick eased off) but Reaper found "point a direction → instantly max range" too twitchy/imprecise.
   - Pass B (current): right stick now drives a **persistent virtual cursor** — stick deflection is a VELOCITY that moves the cursor, not an absolute direction. Stored as `FVector2D AimCursorOffset` (a LOCAL XY offset from the caster), integrated each Tick: `AimCursorOffset += camRelative(GamepadAimInput) * AimCursorSpeed * DeltaTime`, clamped to `AimProjectionDistance`. World aim = `PawnLocation + offset`; because it's caster-relative it keeps its screen position as the character moves (mouse-cursor-pinned-to-caster feel). Releasing the stick stops integrating → cursor holds (no snapback). Z resolved by tracing straight DOWN the cursor column (ECC_Visibility, ignores the pawn) → snaps aim onto ground/objects and highlights an enemy under the cursor. `CursorTrace()` now early-returns after `GetHitResultUnderCursor` when `bUsingGamepad` (keeps CursorTraceHit fresh for the mouse-move → M&K switch-back detection, but skips the M&K highlight so the gamepad virtual-cursor highlight owns it). NEW members in `RageInMagePlayerController.h`: `FVector2D AimCursorOffset` (plain) + `UPROPERTY(EditDefaultsOnly,"Aim") float AimCursorSpeed = 1500.f` (units/sec at full stick — tune to taste). **Because new members change class layout, Live Coding may reject with C2065 (the documented inconsistent gotcha) — if it does, close editor + full `Build.bat` rebuild.**
   - Pass C (current): the virtual cursor is now MODAL — only active while aiming a spell. Reaper wanted normal movement to keep the simple direction+snapback so turning/dodging stay tight. Added `bool bAimingSpell` (private) + public `BeginGamepadAim(float InitialReach)` / `EndGamepadAim()`. The `AimMode` ability task (`RageInMageAbilityTask_AimMode.cpp`) calls `BeginGamepadAim(MaxRange)` in `Activate()` and `EndGamepadAim()` in `OnDestroy()` (task already includes the PC header for GetClampedAimPosition). `BeginGamepadAim` seeds `AimCursorOffset` = **ZeroVector (changed 2026-07-06 per Reaper)** so the cursor starts ON the caster and the stick pushes it outward from there. (Was caster forward × min(MaxRange, AimProjectionDistance) → started at max range; Reaper wanted start-at-character.) InitialReach param retained only as the clamp bound. In `UpdateAimDirection`'s `bUsingGamepad` branch: `if (bAimingSpell)` → virtual cursor (velocity, no snapback, Z-trace, highlight); `else if (past deadzone)` → ORIGINAL magnitude-scaled direction `Origin + AimDir * AimProjectionDistance * StickMagnitude` (snapback restored for facing/dodge). Files: `RageInMagePlayerController.cpp` (UpdateAimDirection split + BeginGamepadAim/EndGamepadAim + CursorTrace guard), `RageInMagePlayerController.h` (`bAimingSpell` + 2 methods + AimCursorOffset/AimCursorSpeed), `RageInMageAbilityTask_AimMode.cpp` (Activate/OnDestroy hooks).

## SparkingSphere over-damage fix — per-target cooldown model (CODE WRITTEN 2026-07-02, NEEDS FULL REBUILD, not yet compiled/tested)
**Symptom (Reaper):** SparkingSphere (Lightning Secondary) "kills WAAAAY too quickly" — appeared to damage all actors in range every frame. **Root cause:** SparkingSphere's damage is 100% inherited from base `ARageInMageZone`, which has TWO damage sources that double-dip on a *moving* zone: (1) `OnZoneOverlap` hits each actor immediately on entry, AND (2) a global timer (`DamageTickEnemiesInside` every `DamageTickInterval`) hits everyone currently inside on one synchronized schedule. Moving sphere gliding through a crowd → entry hit + near-immediate synchronized timer hit + repeated. **Reaper's desired model:** check every frame who's in range, damage an actor only if it hasn't been damaged within `DamageTickInterval` (per-target independent cooldown), NOT a global every-0.5s nuke.
**Fix (scoped so the 3 other zone subclasses + Meteor/Surf/ChargedSpell/Yoyo that share the base are UNAFFECTED):** Added `virtual bool ARageInMageZone::UsesBuiltinTickDamage() const { return true; }` to the base — when true (every existing zone), behaviour is unchanged; when false, base BeginPlay skips the timer AND OnZoneOverlap skips the immediate on-entry damage (both gated on it). Also added base helper `void ApplyDamageSpecToActor(AActor*) const` (DRY'd the two existing apply sites). `ARageInMageSparkingSphereZone` overrides `UsesBuiltinTickDamage()`→false and drives damage in its `Tick()`: each frame `ZoneCollision->GetOverlappingActors(_, APawn::StaticClass())`, and for each valid non-instigator non-friendly actor, if `!LastDamageTimeByActor.Contains(Actor) || Now - last >= DamageTickInterval` → `ApplyDamageSpecToActor` + record `Now`. Cooldown map is `TMap<TWeakObjectPtr<AActor>, float>` (weak so dead actors fall out safely; pruned each tick). New-in-range actor has no timestamp → immediate first hit, then silent for the interval. Ability's `DamageTickInterval` default = 0.5f (confirmed in header).
**Files:** `RageInMageZone.h/.cpp` (new virtual + helper + gated timer/overlap damage), `RageInMageSparkingSphereZone.h/.cpp` (override + TMap + Tick damage). **NEEDS FULL `Build.bat` REBUILD** — new virtual = vtable change on an already-loaded base + new member on subclass; Live Coding cannot hot-patch (per the established gotcha). Not yet compiled or PIE-tested. IceZone's `DamageTickEnemiesInside` slow/freeze override + Fire/Water base-damage zones are untouched (they keep the builtin timer path).

## Player attribute-init HasAuthority guard (DONE 2026-07-02, hot-patched via Live Coding)
`ARageInMagePlayerCharacter::InitPlayerAbilityActorInfo()` called `InitializeDefaultAttributes()` **unconditionally**, but that function runs from BOTH `PossessedBy` (server) AND `OnRep_PlayerState` (clients / any PlayerState re-replication). The enemy version already gates it with `HasAuthority()`; the player did not. Effect: on clients it re-applied authority-only attribute GEs (Instant Primary permanently re-bakes base stats; Infinite non-stacking Secondary adds duplicate permanent modifiers), causing client-side stat inflation/desync — latent (net-mode-dependent), not the cause of the movement bug but a real multiplayer bug. **Fix:** wrapped the `InitializeDefaultAttributes()` call at `RageInMagePlayerCharacter.cpp:167` in `if (HasAuthority())` (function-body-only → Live Coding hot-patched clean, "Live coding succeeded"). Server initializes once and replicates down; clients just receive values.

## Horde-mode enemy spawner `BP_EnemySpawner` (BUILT 2026-07-02, compiles clean, NOT yet PIE-tested)
**Why:** Reaper wants horde mode AND wants to stop hitting the stale-placed-instance bug — runtime-`SpawnActor`ed enemies always build from the current CDO, so future BP edits always propagate (unlike hand-placed level actors). Note this only avoids the *stale-instance* class of bug, not wrong-CDO bugs (a spawner faithfully copies the CDO, mistakes included).
**Asset:** `/Game/Blueprints/AI/Spawner/BP_EnemySpawner` (Actor parent, authored entirely via NeoStack Lua bridge). Compiles 0 errors/0 warnings, saved.
**Variables (all with defaults on CDO):** `EnemyClasses` = `TArray<TSubclassOf<ARageInMageEnemyCharacter>>` (seeded with BP_Ghoul_C + BP_Goblin_Spear_C; add ranged/casters as desired) — NOTE: declared via add_variable type string `class:ARageInMageEnemyCharacter[]` which yields TSubclassOf-array (element_type Object(Class), meta_class the enemy); `SpawnRadius`=1000, `EnemiesPerWave`=3, `WaveSizeIncrement`=2, `TimeBetweenWaves`=8, `MaxAliveEnemies`=20, `bAutoStart`=true, `CurrentWave`=0, `AliveCount`=0.
**Graph logic:** `EventGraph` BeginPlay → Branch(HasAuthority) → Branch(bAutoStart) → StartNextWave. `StartNextWave` (custom event): CurrentWave+=1; SpawnCount = EnemiesPerWave + (CurrentWave-1)*WaveSizeIncrement; ForLoop(1..SpawnCount) → call `SpawnOneEnemy`; on Completed → SetTimerByFunctionName("StartNextWave", TimeBetweenWaves) — self-perpetuating waves. `SpawnOneEnemy` (function): Random Integer(0..Len) index into EnemyClasses via "Get (a copy)" array node → class; GetActorLocation → GetRandomReachablePointInRadius(SpawnRadius) → Branch(success) → SpawnActorFromClass(collision=AdjustIfPossibleButAlwaysSpawn).
**MaxAlive cap + wave-on-clear ADDED 2026-07-02 (compiles clean, saved, still NOT PIE-tested):** Reaper asked for both. Implemented via a **poll**, not delegate binding, because the `Bind Event to OnDeathDelegate` node (K2Node_AddDelegate) is NOT findable via `find_nodes` (context-sensitive to dragging off the delegate pin — the bridge can't surface it; confirmed 0 results for "Bind Event to On Death Delegate"/"On Death Delegate"). Design: new `bWaveActive` bool + `CheckWaveCleared` custom event on a 1s **looping** timer started from BeginPlay (`Set Timer by Function Name "CheckWaveCleared", Looping=true`, inserted between the autostart Branch.True and the first StartNextWave call). `CheckWaveCleared`: `if bWaveActive AND Length(GetAllActorsOfClass(ARageInMageEnemyCharacter)) <= 0` → set bWaveActive=false → `Set Timer by Function Name "StartNextWave", Time=TimeBetweenWaves` (breather then next wave). `StartNextWave`: the old fixed-cadence self-re-arm timer was REWIRED — `ForLoop.Completed` now sets bWaveActive=true (that same `Set Timer "StartNextWave"` node is now driven by CheckWaveCleared's cleared-branch instead). `SpawnOneEnemy`: cap guard prepended — `GetAllActorsOfClass(Enemy) → Length < MaxAliveEnemies` Branch gates the existing nav+spawn; `AliveCount` var is now unused/vestigial (poll uses live GetAllActorsOfClass count, no manual counter). **Tradeoff to note:** the poll counts ALL `ARageInMageEnemyCharacter` in the level (spawner-authored AND hand-placed), so with mixed placement the next wave waits until every enemy is dead — desired for a pure horde arena. Dead enemies clear from the count only once destroyed (`Die()` → `SetLifeSpan(DeathSpan)`), so the effective breather includes DeathSpan. **STILL TODO:** (1) Not PIE-tested; **requires a NavMesh in the level** or GetRandomReachablePointInRadius returns false and nothing spawns. (2) Not yet placed in any level. (3) `Set Timer by Function Name` "Actor Class" class-pin was set via `set_pin(h,"Actor Class","/Script/RageInMage.RageInMageEnemyCharacter")` — that string format works for class pins.
**More bridge gotchas learned this pass:** `find_nodes` result ORDER IS NOT STABLE between calls (the `Length` Set/Array/Map variants reshuffled between identical queries) — NEVER pick math/container nodes by index; filter by `category` substring (e.g. `h.category:find("Array")`). The wildcard comparison operators `Less ( < )` / `Less Equal ( <= )` spawn as `Timespan < Timespan` but promote to int once an int wire hits pin A (compiles clean). Custom events expose an `Output Delegate` out-pin usable directly as a bind source WITHIN the same graph (see BP_Summon), but cross-graph you'd need a `Create Event` node (findable) — moot here since we polled instead.
**Bridge authoring gotchas learned:** array element accessor node = search `"Get (a copy)"` (its index pin is labelled "Dimension 1", not "Index" — still works for 1D TArray, wildcard resolves on connect); integer math = generic `"Add"/"Subtract"/"Multiply"` under category `Utilities|Operators` (result #2 for Add — result #1 is the ARRAY Add), they spawn as wildcard `"FrameNumber + Int"` etc. but resolve to int once an int wire hits pin A; `"Random Integer"` = 0..ExclusiveMax-1 (no subtract needed for indexing); timer self-re-arm via `"Set Timer by Function Name"` with Function Name="StartNextWave" (custom events are callable by name, avoids Create Event/delegate-bind nodes); `GetRandomReachablePointInRadius` spawns as a PURE node (no exec pins) so route exec through the following Branch on its Return Value.

## Enemy stale-CDO / stale-placed-instance bug cluster (DONE 2026-07-02, all PIE-confirmed by Reaper)
Three chained bugs, all the same root theme Reaper flagged upfront ("it's internal, we changed so much it just bugged and isn't listening to new info"). Order matters — each fix exposed the next symptom:
1. **Enemy MaxWalkSpeed wildly wrong** (Ghoul MaxWalkSpeed=425 w/ MovementSpeed .7, player 18750, etc). Root cause Reaper found: `BP_EnemyBase`'s own `BaseWalkSpeed` was **600** (should be 100-ish), and the leaf enemy BPs were "ignoring their own BaseWalkSpeed and using the parent's." Their serialized values read correct via reflection (Ghoul=100, Goblin_Spear=100, Shaman=35, Demon=120) but the running CDO used the stale parent value. **Fix (cheap, no reparent/recreate needed):** for each leaf BP, `a:set('self','BaseWalkSpeed', <sameValue>)` then `compile()` + `save()` — force-rewriting the property recomputes the "differs from parent" delta and unsticks it. Applied to BP_Ghoul, BP_Goblin_Spear, BP_Goblin_Slingshot, BP_Goblin_Shaman, BP_Demon. This propagated to runtime fine (movement confirmed fixed). NOTE: also separately found `ARageInMagePlayerCharacter::InitPlayerAbilityActorInfo()` calls `InitializeDefaultAttributes()` UNCONDITIONALLY from both PossessedBy and OnRep_PlayerState (enemy version correctly gates on `HasAuthority()`), so a listen-server host applies default-attribute GEs TWICE (Instant Primary bakes in twice; Infinite non-stacking Secondary adds two permanent MovementSpeed modifiers) — a real latent double-apply bug, NOT yet fixed, offered but superseded by the actual cause above. Worth fixing later: add `if (HasAuthority())` guard around that call (function-body-only, hot-patchable).
2. **Ghoul "doesn't recognise the player" / wanders.** Actually it WAS finding the player (detection is shared `BTS_FindNearestPlayer` → `GetAllActorsWithTag("Player")`, and `BP_Ghoul` vs working `BP_Goblin_Spear` are byte-identical in BT/AIController/AutoPossess/tags/blackboard — verified). Real cause: `BP_Ghoul.AttackMontages` array was **empty** (log spam `GA_MeleeAttack_MultiCheck failed to play montage None`), so every reached-the-player attack aborted instantly on a null montage and the BT fell through to go-around/wander. ALSO `LeftHandSocketName`/`RightHandSocketName` were `None` (Ghoul attacks with claws, not a weapon). **Fix on BP_Ghoul CDO:** populated `AttackMontages` with 2 entries mirroring the working Goblin pattern — `AM_Ghoul_Attack_LeftHand`→`MontageEventTag=Montage.Attack.1`,`SocketTag=CombatSocket.LeftHand`; `AM_Ghoul_Attack_RightHand`→`Montage.Attack.2`,`CombatSocket.RightHand` (event tags MUST match the `AN_MontageEvent` `Event Tag` baked into each montage's notifies — read those via `open_asset("<montage>.<montage>:AN_MontageEvent_C_<n>"):get('Event Tag')`; LeftHand notifies send Attack.1, RightHand send Attack.2, HitSound reuses `sfx_HitSwipe_multi_Mid`). Set `LeftHandSocketName=LeftHandAttackSocket`, `RightHandSocketName=RightHandAttackSocket` (the actual sockets on `SKM_Ghoul`, bones Wrist-L/Wrist-R). The melee ability flow that consumes this: `GA_MeleeAttack_MultiCheck` → `GetRandomAttackMontage` (ICombatInterface, reads `ARageInMageCharacterBase::AttackMontages` directly, melee = entries with no ProjectileClass/SummonClass) → break → PlayMontageAndWait + WaitGameplayEvent(MontageEventTag) → GetCombatSocketLocation(SocketTag) → CauseDamage. BTT_Attack_Melee just does `TryActivateAbilitiesByTag(AttackTag)`.
3. **After the CDO fix, montages STILL didn't play in PIE** — because the single **placed level instance** `BP_Ghoul_C_1` had the OLD empty `AttackMontages` + `None` socket names baked in as per-instance overrides shadowing the corrected CDO. Confirmed via `get_actor_properties('BP_Ghoul_C_1', {changed_only=true})` newly listing AttackMontages/LeftHandSocketName/RightHandSocketName as changed-from-CDO (they weren't listed before the CDO edit, because instance-empty == CDO-empty back then). PIE spawns from the level actor, so it got the stale empty array regardless of the fixed CDO. **Fix:** Reaper deleted the stale placed Ghoul and dropped a fresh one, which inherits the corrected CDO — now attacking properly. **GENERAL LESSON for next time:** when a Blueprint CDO fix reads back correct but runtime still misbehaves, suspect a stale PLACED INSTANCE shadowing the CDO — `get_actor_properties(changed_only=true)` reveals it; re-place the actor (or reset those props on the instance) to cure. This is the level-actor cousin of the stale-CDO staleness. (Tool gap: no `set_actor_property`/reset-override verb was found in the Lua bridge to clear an instance override programmatically — had to re-place by hand. Worth reporting to Betide Studio Discord.)
> Memory split: this file holds all RageInMage technical/project history (dev log, pending tasks, gotchas).
> The home Claude memory (~/.claude/projects/.../memory/) only holds durable user/feedback facts — no duplication.

## Lightning Mage School — Chain Lightning + Stun Infrastructure + Ultimate (Lightning Flash DONE & re-confirmed working in PIE post-rebuild incl. chain-lightning; Flash And Awe + Ceaseless Wrath fully built as Blueprint assets this session — C++ compiled via the full rebuild, GA_FlashAndAwe/GA_CeaselessWrath created+wired+registered as LightningMage startup abilities, NEITHER YET PIE-TESTED; 3 abilities remain entirely unbuilt: SparkingSphere/Secondary, ZipNZap/Movement, StaticMegaSurge/Defensive)

### Chain-lightning debug visualization + jump VFX (DONE this session, hot-patched via Live Coding)
**User-requested follow-up** after asking how to inspect `ChainJumpRadius`/`ChainDamageFalloffPerJump`/`ChainMaxJumps` (confirmed via `class_properties`: 350 / 0.1 / 10 — unchanged defaults, on `URageInMageLightningDamageAbility`). Two additions to `ApplyChainLightningDamage` (`RageInMageAbilitySystemLibrary.cpp`):
1. **`DrawDebugSphere`** (gated `#if ENABLE_DRAW_DEBUG`) at `CurrentTarget`'s location with radius `JumpRadius`, drawn right before each jump's `GetLivePlayersWithinRadius` search — makes the actual per-jump search area visible in PIE. Cyan, 3s duration, matches `RageInMageConeBlastAbility`'s existing `DrawDebugCone` convention.
2. **New `UNiagaraSystem* ImpactEffect` param** on `ApplyChainLightningDamage` (default `nullptr`) + new `ChainImpactEffect` `EditDefaultsOnly` property on `URageInMageLightningDamageAbility`, passed through from `CauseChainDamage`. On each successful jump, spawns `ImpactEffect` at the just-left target's location and sets its `BeamEnd` user parameter (Position type) to the next target's **absolute world location** — same convention `RageInMageLightningFlashAbility`'s `RodImpactEffect` already uses for the weapon-tip-to-target rod beam (confirmed via reading that code: `BeamEnd` is absolute, not relative/distance-based, so no separate distance scalar needs to be sent to the Niagara system — just the target's location).
3. Set `GA_LightningFlash`'s new `ChainImpactEffect` = `NS_LightningShock` (same system already used for rods) — compiled clean, saved.
**Confirmed this DID hot-patch via Live Coding** despite adding new UPROPERTY members (`ChainImpactEffect`) to an already-loaded UCLASS — `class_properties('RageInMageLightningDamageAbility')` showed it live immediately after compile, no restart needed. Consistent with the Knockback-session precedent (sometimes new members hot-patch fine, sometimes hit a hard C2065 — inconsistent, don't assume either way without checking).
**Files:** `RageInMageAbilitySystemLibrary.h/.cpp`, `RageInMageLightningDamageAbility.h/.cpp` (this session, on top of the still-pending reparent rebuild below), `GA_LightningFlash.uasset` (ChainImpactEffect set).
**Re-confirmed in PIE (2026-06-27) after the full rebuild landed:** user confirmed chain-lightning is "still working perfectly" post-reparent (`URageInMageLightningFlashAbility` → `URageInMageLightningDamageAbility`, `CauseChainDamage` in place of `CauseDamage`). No regressions from the rebuild.

### Lightning Flash — chain-lightning wired in via new shared base class (CODE WRITTEN, PENDING FULL REBUILD — not yet compiled/tested)
**User request:** wanted the already-built `ApplyChainLightningDamage` chain-jump mechanic actually triggered by Lightning Flash (previously it was fully implemented in C++ but never called by any ability — see the damage-bug entry below), specifically so the chain mechanic itself could be validated in PIE before more Lightning spells get built, and so future spells have a proven, reusable way to plug into it.
**New shared class `URageInMageLightningDamageAbility`** (`Source/RageInMage/{Public,Private}/AbilitySystem/Abilities/RageInMageLightningDamageAbility.h/.cpp`), subclass of `URageInMageDamageGameplayAbility`. Exposes three new `EditDefaultsOnly` tunables — `ChainJumpRadius` (350), `ChainDamageFalloffPerJump` (0.1), `ChainMaxJumps` (10), mirroring `ApplyChainLightningDamage`'s own default params — and one new method, `CauseChainDamage(AActor* InitialTarget)`: builds the spec from `DamageEffectClass`/`DamageTypeTags` exactly like the inherited `CauseDamage()` does, then routes it through `URageInMageAbilitySystemLibrary::ApplyChainLightningDamage` instead of a single `ApplyGameplayEffectSpecToTarget`. **Any future Lightning spell that wants chaining should inherit from this class instead of `URageInMageDamageGameplayAbility` directly, and call `CauseChainDamage()` instead of `CauseDamage()`** — that's the whole integration surface.
**`URageInMageLightningFlashAbility` reparented** from `URageInMageDamageGameplayAbility` to `URageInMageLightningDamageAbility`. In `ApplyLightningFlash`'s per-rod loop, the `CauseDamage(Target)` call was replaced with `CauseChainDamage(Target)` — each rod still picks its own random cone target (no replacement, refills when the pool runs dry, per the existing design), but now that hit can also jump onward through nearby Charged enemies, independently per rod.
**Why this needs a full rebuild, not Live Coding:** reparenting an already-loaded UCLASS (changing its base class) changes layout/vtable, which Live Coding cannot hot-patch — same category as the documented "new class members" gotcha below, just a more severe version of it. User explicitly opted for the full rebuild instead of a same-session hack (inlining the spec-build duplicate directly in `ApplyLightningFlash` would have avoided the rebuild but duplicated logic) — "I don't mind if we need to rebuild to do this properly. Time is on our side."
**On-screen debug instrumentation added this session** (both still live, both function-body-only edits, already confirmed hot-patched via Live Coding *before* the reparent — will still be present after the rebuild):
- `RageInMageLightningFlashAbility.cpp`, per rod: `[LightningFlash] Rod {N} -> {Target} | Charge={X} | Charged={Yes/No} OverCharged={Yes/No}` (white/cyan/red by state).
- `RageInMageAbilitySystemLibrary.cpp`, inside `ApplyChainLightningDamage`, per jump: `[ChainLightning] Jump {N} -> {Target} | Charge={X} | Charged=... OverCharged=...`, plus a reason message when a chain stops (`not Charged` vs `Charged but no eligible target in range`).
**Also this session:** the pre-existing `// DEBUG: show Heat on screen` block in `RageInMageAttributeSet.cpp` (`PostGameplayEffectExecute`, Heat-attribute branch) was **commented out, not deleted or converted to UE_LOG** — user wants it silenced while testing Lightning/Charge but trivially re-enabled later when revisiting Heat/Frozen work. See `[[feedback_debug_instrumentation]]` in home Claude memory for the general rule this follows.
**Status: NOT yet compiled or tested.** User is closing the editor now so a full `Build.bat` rebuild can run (Live Coding is blocking it while active). Once the editor reopens: confirm `RageInMageEditor` builds clean, then PIE-test that (a) rods still hit independently as before, (b) a rod landing on a target that ends up Charged actually jumps to a nearby enemy (watch for the `[ChainLightning] Jump 1 ->` message), (c) falloff/OverCharged-bypass behaves per the original chain design.
**Files this session:** `RageInMageLightningDamageAbility.h/.cpp` (NEW), `RageInMageLightningFlashAbility.h/.cpp` (reparented + simplified), `RageInMageAbilitySystemLibrary.cpp` (chain debug messages), `RageInMageAttributeSet.cpp` (Heat debug message commented out).

### Lightning Flash — "visual works, deals no damage" bug (DONE this session)
**Symptom:** User confirmed the rod VFX/cone targeting all worked, but Lightning Flash dealt zero damage and presumably never built Charge either.
**Root cause:** `ExecCalc_Damage::Execute_Implementation` only evaluates a magical-damage branch for a given `DamageType.*` tag if that tag shows up in `Spec.CapturedSourceTags.GetAggregatedTags()` — which is the **source ASC's currently-owned gameplay tags** (`GetActorTags()`) unioned with the GE's own asset tags (`Def->GetAssetTags()`, confirmed empty for the shared `GE_Damage` — verified by grepping the raw `.uasset` for "DamageType", zero hits). The owned-tags half is populated by each ability's **`ActivationOwnedTags`** (the standard GAS mechanism that grants tags to the avatar only while the ability is active). Every other damage ability in the project (`GA_Player_SearingFlame`, `GA_SimpleIce`, `GA_TearsDoFall`, etc.) sets `ActivationOwnedTags` to exactly mirror its `DamageTypeTags` map keys (e.g. SearingFlame: `[DamageType.MagicalDamage.Fire, Attributes.Mechanics.Heat]`) — this is the load-bearing convention that makes the generic tag-matching loop in `ExecCalc_Damage.cpp` ever fire for that element. `GA_LightningFlash`'s `ActivationOwnedTags` was left empty when the ability was built, so `ExecCalc_Damage` never found `DamageType.MagicalDamage.Electric` in the source tags and the entire magical-damage branch (damage AND Charge gain) silently no-opped every cast.
**Fix:** Set `GA_LightningFlash`'s `ActivationOwnedTags` to `[DamageType.MagicalDamage.Electric, Attributes.Mechanics.Charge]` (mirrors its existing `DamageTypeTags` keys exactly, same pattern as every other element). Compiled clean (0 errors/warnings), saved.
**Tooling note:** `a:set('self', 'ActivationOwnedTags', {...})` with a Lua table failed ("table value not recognized... use {x=,y=,z=} for Vector" etc — the generic `set()` doesn't have a table-literal path for `FGameplayTagContainer`). Workaround: pass the engine's own export-text string format directly, e.g. `a:set('self', 'ActivationOwnedTags', '(GameplayTags=((TagName="DamageType.MagicalDamage.Electric"),(TagName="Attributes.Mechanics.Charge")))')` — this round-tripped correctly and survived compile+save. Useful for any future `FGameplayTagContainer`-typed property that `get()` itself shows in this same parenthesized format.
**Separate pre-existing issue, now resolved:** the `GE_Cooldown_LightningFlash` GEComponent tag (previously granting the duplicated-from-source `Cooldown.Fire.SearingFlame` instead of `Cooldown.Lightning.LightningFlash`) was fixed by the user manually in-editor at the end of the prior session. Confirmed correct as of this session — no longer an open item.
**Not yet playtested in PIE** — fix is based on confirmed root-cause analysis (matching the exact convention every other working ability uses) and a clean compile, but the user should confirm damage now lands and Charge accumulates before considering this fully closed.

**Rebuild completed this session:** the editor restart + full rebuild (blocked at end of prior session) succeeded. Root cause of the failure was a missing `RageInMageGameplayTag` header include in `RageInMageAbilitySystemLibrary.cpp` — user fixed it, rebuild went clean. Confirmed via `class_methods('RageInMageAbilitySystemLibrary', {filter='ApplyChainLightningDamage'})`: signature is now the final 6-param form `(InstigatorActor, InitialTargetActor, DamageEffectSpecHandle, JumpRadius=350, DamageFalloffPerJump=0.1, MaxJumps=10)` — no `ChargeJumpThreshold`.

### Lightning Flash — Primary ability (DONE, first Lightning ability built)
**Design (user-directed):** cone burst that fires `RodCount` "rods" — round-robin distributed across every living enemy found in a cone in front of the caster (`RodIndex % Targets.Num()`), not a flat AoE that hits everyone once. Each rod is its own full damage hit (own crit roll, own Charge gain). User explicitly wants the `NS_LightningShock` Niagara spawn count to equal the attack count — one `SpawnSystemAtLocation` call per rod, in the same loop iteration as the damage call, so they can never drift apart. Default `RodCount=5` at level 1 (flat `FScalableFloat`, no curve yet — user said "at Lvl1 I want there to be a default of 5").
**New C++ class `URageInMageLightningFlashAbility`** (`Source/RageInMage/{Public,Private}/AbilitySystem/Abilities/RageInMageLightningFlashAbility.h/.cpp`), subclass of `URageInMageDamageGameplayAbility`, sibling to `RageInMageConeBlastAbility` (copied its cone-gather math: flatten-to-XY direction, dot-product half-angle check, optional height clamp) but with different target-resolution: builds a `TargetsInCone` array first, then loops `RodCount` times calling the **inherited** `CauseDamage(Target)` (already builds a fresh spec from `DamageEffectClass`/`DamageTypeTags` and applies it — did NOT duplicate that logic) + spawns `RodImpactEffect` per rod. New properties: `ConeHalfAngle` (20°), `ConeRange`, `ConeHeightLimit`, `RodCount`, `RodImpactEffect` (Niagara), `LightningFlashSound`. Confirmed via this session's experience that **brand-new UCLASSes hot-patch fine via `LiveCoding.Compile`** — it's specifically *new members on an already-loaded class* that need a full rebuild, not new classes themselves.
**Assets created:** `/Game/Blueprints/AbilitySystem/Player/Abilities/Lightning/LightningFlash/` folder with `GA_LightningFlash` (ParentClass=`RageInMageLightningFlashAbility`), `GE_Cost_LightningFlash` and `GE_Cooldown_LightningFlash` (both duplicated from `GE_Cost_SearingFlame`/`GE_Cooldown_SearingFlame` as templates — Fire's Primary, the closest tier match). Used the pre-existing `/Game/RageInMageSpells/VFX/Lightning/NS_LightningShock` (artist had already staged a Lightning VFX folder with this + `NS_Lightning_Strike_Blue` + the aim-line `NS_Aim_Line_Lightning`, unused until now).
**Class defaults set:** `StartupInputTag=InputTag.LMB`, `StartupAbilityTag=Ability.Lightning.LightningFlash`, `StartupAbilityTypeTag=Ability.Type.Primary`, `DamageEffectClass=GE_Damage`, `DamageTypeTags={DamageType.MagicalDamage.Electric=8, Attributes.Mechanics.Charge=15}` (placeholder tuning — no prior Lightning ability existed to calibrate against; flagged as likely needing a playtest pass), `ConeRange=600`, `ConeHeightLimit=100`, `RodCount=5` flat, `RodImpactEffect=NS_LightningShock`, `bUseHoldToAim=true`, `AimIndicatorShape=Box`, `AimIndicatorLength=600`, `AimIndicatorWidth=420`, `MaxSpellRange=600`.
**EventGraph wired** mirroring `GA_DropRippleBlast`'s aim-and-release pattern exactly (skipped its cosmetic reroute nodes, connected `AimMode`'s `Aim Position` output directly to both consumers instead): `ActivateAbility → AimMode(Max Range=GetAimIndicatorLength) → [On Input Released] → CommitAbilityCost → Branch(Return Value) → [True] UpdateFacingTarget(Target=GetAvatarActorFromActorInfo, Target Location=AimMode.Aim Position) → ApplyLightningFlash(Target Location=AimMode.Aim Position) → CommitAbilityCooldown → EndAbility`; `[False]` and `AimMode.On Cancelled`/`OnEndAbility` all route to their own `EndAbility` nodes (3 total, matching the template). Compiled clean (0 errors/0 warnings via `read_log('compile')`), saved.
**Registered as LightningMage startup ability** in `DA_CharacterClassInfo`'s `CharacterClassInformation` map (was previously empty for `LightningMage` — only had `ProgressBarColor`/`BackGroundMaterialInstance`/`XPReward` set, no `StartupAbilities` at all). Added `GA_LightningFlash` as its first entry.
**Added `CT_ManaCost` row `"LightningFlash"`** (20→30 across levels 1-5, copied from `SearingFlame`'s curve) and pointed `GE_Cost_LightningFlash`'s Mana modifier `Curve.RowName` at it. **Caveat discovered while doing this:** `GE_Cost_SearingFlame`'s own Mana modifier curve only has `RowName` set, no `CurveTable` reference (`Curve=(RowName="SearingFlame")` with no `CurveTable=` field) — meaning `FScalableFloat::GetValueAtLevel` likely treats `Curve.CurveTable` as null and falls back to the flat `Value=-1`, so the `CT_ManaCost` row data may be entirely decorative/unwired for every Primary ability already in the game, not just this new one. Did not investigate or fix this — out of scope for what was asked, but worth checking if mana costs ever seem to not scale with level. Lightning Flash now matches whatever SearingFlame already does, so it's at least *consistent*.
**MANUAL STEP RESOLVED (was a tool limitation, fixed by user in-editor at the end of the prior session):** `GE_Cooldown_LightningFlash`'s `TargetTagsGameplayEffectComponent` previously still granted the duplicated-from-source tag `Cooldown.Fire.SearingFlame` instead of `Cooldown.Lightning.LightningFlash` (NeoStack's `get`/`set` can't reach into GEComponent sub-object properties — see the documented GEComponent gotcha below — so this had to be done by hand). User confirmed it now correctly grants `Cooldown.Lightning.LightningFlash`.

**What's NOT done yet:** the other 5 Lightning abilities (SparkingSphere/Secondary, ZipNZap/Movement, StaticMegaSurge/Defensive, FlashAndAwe/CrowdControl, CeaselessWrath/Ultimate) — see per-spell design notes below, still valid. Lightning Flash itself hasn't been playtested in PIE yet (no enemies-in-cone real-world check, no confirmation the Charge gain actually trends toward Charged/OverCharged as expected over repeated casts).

### Original chain-lightning design notes (superseded items already marked inline)

**User request:** Start building the Lightning Mage school (6 abilities: LightningFlash/Primary, SparkingSphere/Secondary, ZipNZap/Movement, StaticMegaSurge/Defensive, FlashAndAwe/CrowdControl, CeaselessWrath/Ultimate). Tags for all 6 already existed pre-session (`Ability.Lightning.*`, `Cooldown.Lightning.*`, `ECharacterClass::LightningMage`), as did `DamageType.MagicalDamage.Electric`, `Resistance.MagicalDamage.Electric`, and a `Charge` mechanics attribute (0-120, generic threshold system shared with Heat/Momentum/etc.) — but nothing used them yet. No Lightning ability assets/folder exist yet (only `Content/Blueprints/AbilitySystem/Player/Abilities/Fire` and `/Water` exist as siblings).

**CURRENT final mechanic design** (the Charged/OverCharged tag system below *superseded* an earlier plan that used a standalone `GE_Stunned` ConditionInfo row keyed directly off Charge>=100 — keep reading, that earlier plan is obsolete):
- Every lightning spell hit deals damage AND increases the target's `Charge` attribute via the same SetByCaller pattern Fire uses for `Heat` (set `DamageType.MagicalDamage.Electric` + `Attributes.Mechanics.Charge` SetByCaller tags on the spec — `ExecCalc_Damage.cpp` already reads/scales/applies both automatically, no changes needed there).
- `Charge` decays on its own over time (every 1s, flat tunable amount via `ChargeDecayRate`) when not being topped up — mirrors Heat-decay, simpler (no negative/cold side).
- **`Condition.Charged` (Charge >= 50, native tag, pre-existing):** purely a *visual* tell (`GE_Charged`, Infinite duration, `bResetMechanicsOnTrigger=False` — persists once triggered, nothing currently removes it as Charge decays back down, that's a known gap, not yet asked to be fixed). Functionally, it's also the **chain-eligibility gate**: a lightning hit only arcs onward from a target if that target owns `Condition.Charged` *after* the hit lands (whether they already had it, or this very hit's Charge gain just pushed them over 50).
- **`Condition.OverCharged` (Charge >= 100, native tag, pre-existing):** "its only purpose is to stun" (user's words) — layers on top of Charged as an extra tag, doesn't change the visual. `GE_OverCharged` now: `HasDuration`, `DurationMagnitude=SetByCaller(Condition.OverCharged)` (fed by `DA_ConditionInfo`'s `BaseIntensity=2.0`), grants Target Tags `Condition.OverCharged`+`Condition.Stunned` (Stunned drives the pre-existing `StunTagChanged` movement-lock), Asset Tags `Condition.OverCharged`, and `RoutineExpirationEffectClasses=[GE_LightningStunImmunity]` (auto-grants the immunity grace window the instant the stun naturally expires). `DA_ConditionInfo`'s OverCharged row: `BlockedByConditions=[Condition.StunImmune]`, `bResetMechanicsOnTrigger=true` (Charge resets to 0 once it stuns).
- **Chain/jump damage falloff:** each successive jump is multiplied by `(1 - DamageFalloffPerJump)` (default 0.1) **cumulatively** — EXCEPT a jump leaving a target that owns `Condition.OverCharged` always chains at 100% (no falloff for that hop, full reset to 1.0×, not just skipping one decay step).
- **Stun-lock prevention:** `Condition.StunImmune` grace-period tag (already in `BlockedByConditions` above) — a tag-block, not a timer, so it only affects the Charge→OverCharged→Stun path.
- **Flash And Awe is the only spell with a *guaranteed*, unconditional stun** (dash-grab) — ignores the Charge system entirely. Applies `GE_Stunned` directly (MakeOutgoingSpec + AssignTagSetByCallerMagnitude + ApplyGameplayEffectSpecToTarget), bypassing `ApplyConditionToTarget`/ConditionInfo — `GE_Stunned`/`GE_LightningStunImmunity` (built in an earlier session) are reserved for exactly this bypass path now, NOT used by the generic Charge threshold system anymore (that's `GE_OverCharged`'s job, see above).
- Per-spell specifics (still valid): **Lightning Flash** — no flat cone AoE; checks all living actors in a short cone, distributes "rod" hits round-robin/evenly across them (rod count scales with level, pool refills once everyone's been hit once — needs its own new targeting helper, no precedent in codebase). **Sparking Sphere** — slow zone, travels toward cast-aim direction at fixed speed, despawns after lifetime. **Zip 'N' Zap** — 2 independently-recharging dash charges (first use of ability charges in this project). **Static Megasurge** — halves incoming damage while active, zaps attacker back for a flat non-scaling value (no lifesteal, explicitly rejected as unfair). **Ceaseless Wrath** — `ARageInMageZone` reuse, radius 400, tick damage every 0.5s.
- **Naming nit (not fixed, flag if confusing later):** the native tag tooltips in `RageInMageGameplayTag.cpp` are backwards vs. the live mechanic — `Condition.Charged`'s comment says "chain **to** you" and `Condition.OverCharged`'s says "chain **from** you", but the actual implemented logic is the opposite (Charged gates chaining FROM a target; OverCharged only affects that target's own outgoing falloff). Cosmetic only, doesn't affect behavior.

**Pre-existing trap found + fixed this session:** `UConditionInfo::FindConditionForMechanicsThreshold` (`ConditionInfo.cpp:38`) does a **linear scan, first match wins** — it does NOT pick the highest qualifying threshold. `DA_ConditionInfo`'s `Charged`(50)/`OverCharged`(100) rows both key off `Attributes.Mechanics.Charge`, so whichever row comes first in the array wins for ANY Charge value above its threshold. They were originally ordered Charged-then-OverCharged, which meant `OverCharged` could **never** fire (Charged always matched first). User reordered the array so `OverCharged` is checked before `Charged`. **If anyone ever adds a third Charge-tag row, it must be inserted in threshold-descending order, or it'll have this same bug.**

**What's built/configured this session:**
- `RageInMageAbilitySystemLibrary.h/.cpp` `ApplyChainLightningDamage`: rewrote the chain-eligibility check from a raw `Charge >= ChargeJumpThreshold` float compare (param now **removed** from the signature) to `TargetASC->GetOwnedGameplayTags().HasTag(Condition_Charged)` checked on the just-hit target after its damage/Charge-gain lands; added the `Condition.OverCharged` 100%-damage falloff bypass described above. Signature is now `(InstigatorActor, InitialTargetActor, DamageEffectSpecHandle, JumpRadius=350, DamageFalloffPerJump=0.1, MaxJumps=10)`.
- `GE_OverCharged`: configured `DurationPolicy=HasDuration`, `DurationMagnitude=SetByCaller(Condition.OverCharged)`, `RoutineExpirationEffectClasses=[GE_LightningStunImmunity]` via the scripting tool's generic `get`/`set` (these are plain `UGameplayEffect` UPROPERTYs, NOT GEComponents, so the tool could write them directly — unlike tag-granting, which still needs the manual GEComponent step below). User manually added Target Tags (`Condition.OverCharged`+`Condition.Stunned`) and Asset Tags (`Condition.OverCharged`) GEComponents in-editor — confirmed correct.
- `DA_ConditionInfo`: OverCharged row now has `BaseIntensity=2.0`, `BlockedByConditions=[Condition.StunImmune]`, `bResetMechanicsOnTrigger=true`. Charged row's `MechanicsThreshold` corrected from 30 → **50** (user's final call) to match the chain-eligibility gate value referenced everywhere else.
- Confirmed `Condition.StunImmune` (added to C++ in an earlier session) is now registered post-restart — verified by round-tripping it through a `GameplayTagContainer` set/get on `DA_ConditionInfo` and getting a non-empty result back (an unregistered tag silently produces an empty container — see Tooling Gotchas).
- Confirmed RidingTide's WaveBox rebuild (from an earlier session, see Completed Work below) is fully live: `class_properties('RageInMageWaveBox')` shows all the new members.

**Status as of 2026-06-26 (both items below are DONE, kept for history — see the Flash And Awe / Ceaseless Wrath sections further down for current state):**
1. ~~Fix the `GE_Cooldown_LightningFlash` GEComponent tag~~ — done, user fixed in-editor in an earlier session.
2. ~~PIE sanity check of Lightning Flash~~ — done. User confirmed chain-lightning actually working in PIE this session (after the `IsBothEnemy` inverted-check bug fix — see chain-lightning section above); rods/Charge/OverCharged/stun all behaving as designed.
3. Remaining abilities to build: **SparkingSphere** (Secondary), **ZipNZap** (Movement, 2 dash charges), **StaticMegaSurge** (Defensive, halves incoming damage + flat reflect) — not started. **FlashAndAwe** and **CeaselessWrath** designs have both changed significantly from what's described here (FlashAndAwe is now teleport+AoE-stun via `ApplyGuaranteedStun`, not a direct `GE_Stunned` dash-grab; CeaselessWrath is now repeated simultaneous lightning-strike waves, not a `ARageInMageZone` DoT) — see their own dedicated sections further down for the current, accurate design and implementation status.

**Files touched:** `RageInMageAbilitySystemLibrary.h/.cpp` (this session's chain-lightning rewrite). Earlier-session files (`RageInMageGameplayTag.h/.cpp`, `RageInMageGameplayAbility.cpp`, `RageInMageAbilitySystemComponent.h/.cpp`, `RageInMageAttributeSet.cpp`, `RageInMageCharacterBase.h/.cpp`, `RageInMagePlayerCharacter.cpp`, `RageInMageEnemyCharacter.cpp`) remain as previously documented and are confirmed live. Assets: `GE_Stunned.uasset`/`GE_LightningStunImmunity.uasset` (earlier session, reserved for Flash And Awe bypass), `GE_OverCharged.uasset`/`GE_Charged.uasset` (pre-existing, reconfigured this session), `DA_ConditionInfo.uasset` (reordered + edited this session).

## Flash And Awe redesign + shared Stun/StunImmunity system (IN PROGRESS — core system wired, ability itself not started)
**User redesign of Flash And Awe** (Lightning CrowdControl, not yet built as an ability): instead of a short dash, the caster **teleports** to a target point ("FLASH!"), then on landing **stuns ALL nearby enemies** in a radius for X seconds ("...and awe"), no damage. Teleport has no precedent in the codebase yet (`RageInMageLeapAbility` is the closest existing pattern — leap-then-AoE-on-landing — but uses a real parabolic launch, not an instant teleport; will need a new approach, not yet designed).
**Design discussion before any Flash And Awe code was written** settled on a generic, reusable Stun/StunImmunity architecture (since user confirmed other elemental schools will get their own stun sources later — this must not be Lightning-specific):
- Charge/OverCharged remains the *only* current stun source in the game. **Decided against pausing Charge decay during Stunned/OverCharged** (an earlier idea floated mid-discussion, reversed before any code was written) — Charge decays normally at all times, stunned or not. No code change needed since this was never implemented; just don't act on the earlier "stop decay" note if it resurfaces from older context.
- **User manually renamed** `GE_LightningStunImmunity` → `GE_StunImmunity` (generic, reusable name) and converted its `DurationMagnitude` to `SetByCaller(Condition.StunImmune)` (was a flat 3.0). Also manually added the missing `TargetTagsGameplayEffectComponent` to `GE_Stunned` (it previously granted no tags at all — a separate gap found while investigating, now fixed) — both confirmed correct via reflection this session.
- **Key architectural decision:** StunImmunity is now applied **immediately alongside the stun itself** (duration = stun's own duration + a grace period), NOT only when the stun naturally expires (the old `RoutineExpirationEffectClasses` approach). This closes a real permalock path: if a stun can be refreshed/extended before it naturally ends (very possible once Charge stops decaying mid-stun), the old expiry-triggered immunity would never fire. Applying immunity up-front means it always covers the full stun+grace window regardless of refreshes.
- Confirmed by reading the assets directly: `GE_Stunned`'s `OngoingTagRequirements`/`RemovalTagRequirements` are both empty, so granting `Condition.StunImmune` alongside an active `Condition.Stunned` does NOT cancel the ongoing stun — immunity only blocks *future* stun applications that check for it (via `BlockedByConditions` or the new `ApplyGuaranteedStun` explicit-strip path below). This satisfies the user's requirement that StunImmunity shouldn't disable a currently-active stun, only block new ones.
**Code written this session (NOT yet compiled/tested — same struct-wipe risk as below, blocked on full rebuild):**
- `UConditionInfo` (`ConditionInfo.h`): two new shared GE class refs, `StunnedEffect` and `StunImmunityEffect` — single source of truth for any future stun source to reference, rather than each ability/condition carrying its own pointer.
- `FRageInMageConditionInfo` struct: new `StunImmunityGraceSeconds` field (default 0 = opt-out). Any condition row with this > 0 is treated as a stun.
- `URageInMageAbilitySystemLibrary::ApplyConditionToTarget` now calls the new `ApplyStunImmunity` helper right after applying a condition whose `StunImmunityGraceSeconds > 0`, with `Info.BaseIntensity + StunImmunityGraceSeconds` as the total duration (`BaseIntensity` is this codebase's existing convention for "the value fed into the condition GE's SetByCaller duration" — e.g. OverCharged's `BaseIntensity=2.0` is its 2s stun length).
- New `URageInMageAbilitySystemLibrary::ApplyStunImmunity(SourceASC, TargetASC, WorldContextObject, TotalDuration)` — applies `UConditionInfo::StunImmunityEffect` via `SetByCaller(Condition.StunImmune)`.
- New `URageInMageAbilitySystemLibrary::ApplyGuaranteedStun(SourceASC, TargetASC, WorldContextObject, StunDuration, GraceSeconds)` — the building block Flash And Awe will call: strips any active `Condition.StunImmune` from the target first (so the guaranteed stun always lands through existing immunity, per user's explicit request), applies `UConditionInfo::StunnedEffect` for `StunDuration`, then calls `ApplyStunImmunity` for `StunDuration + GraceSeconds`.
**Still open / not yet answered:** user asked "can someone be stunned again during a stun purely from Charge, or is there a built-in defense?" — answer is now yes, defended, via the immediate-apply-immunity approach above (no longer needs the previously-floated `Condition.Stunned`-in-`BlockedByConditions` patch, which would have been redundant/inferior to this).

### Stun-lock bug found via PIE retest (DONE, hot-patched)
**Symptom (user, 2026-06-27):** after the full rebuild, user PIE-tested Lightning and found the enemy still got hit-stunned then stun-locked indefinitely, despite `GE_StunImmunity`/`StunImmunityGraceSeconds` being correctly configured on `DA_ConditionInfo`'s OverCharged row.
**Root cause:** the StunImmunity-granting logic only ever existed in `URageInMageAbilitySystemLibrary::ApplyConditionToTarget` (`RageInMageAbilitySystemLibrary.cpp:184`) — but that function is **never called anywhere in the runtime**. The actual live Charge-threshold pipeline is `URageInMageAttributeSet::HandleMechanicsThreshold` → `ApplyConditionFromData` (`RageInMageAttributeSet.cpp:746`), a separate, older, duplicate implementation that checks `BlockedByConditions` and applies the condition GE but never followed up with `ApplyStunImmunity`. So every OverCharged trigger correctly stunned (and correctly reset Charge to 0 via `bResetMechanicsOnTrigger`), but left zero immunity behind it — the very next hit that pushed Charge back over 100 (near-instant with a 1s ability cooldown + rod/chain hits) re-stunned with nothing blocking it. `GE_StunImmunity` itself was never the problem.
**Fix:** added the same `ApplyStunImmunity(Instigator, Properties.TargetASC, Properties.TargetAvatarActor, CondInfo->BaseIntensity + CondInfo->StunImmunityGraceSeconds)` call to `ApplyConditionFromData`, gated on `StunImmunityGraceSeconds > 0.f`, mirroring `ApplyConditionToTarget` exactly. Pure function-body change — hot-patched via Live Coding, confirmed via raw log grep (`"Live coding succeeded"`, no C2065). **User confirmed fixed in PIE.**
**Note:** `ApplyConditionFromData` has a second call site (`RageInMageAttributeSet.cpp:853`, Heat ±4 Ignited/Frozen stages) that now also benefits from this fix automatically — relevant if Heat-driven Ignited/Frozen ever gets a `StunImmunityGraceSeconds > 0` row in `DA_ConditionInfo`. `ApplyConditionToTarget` in the library remains unused dead code for now (originally written for Flash And Awe's planned threshold-driven path, but Flash And Awe uses `ApplyGuaranteedStun` instead) — not removed since it's a plausible future entry point, but worth remembering it's NOT what's live today.
**Files:** `RageInMageAttributeSet.cpp`.

### Hit Reaction Grace Period — confirmed working in PIE (DONE)
After the full rebuild, user confirmed in PIE that rapid back-to-back rod/chain hits on the same enemy no longer read as a stun-lock — `HitReactionGraceSeconds` (0.3s) is correctly preventing hit-reaction retriggering. No further action needed.
**Full rebuild completed successfully — confirmed the DA wipe was in-memory only, not on disk.** Closed the editor without saving `DA_ConditionInfo` (per the wiped-state warning below), ran a full rebuild, reopened: `ConditionInfos` came back with all original rows (Burning/Frozen/OverCharged/Charged) fully intact, AND the new `StunnedEffect`/`StunImmunityEffect`/`StunImmunityGraceSeconds` fields were live (`class_properties('ConditionInfo')` confirmed all 4 fields present). Validates the recovery approach documented in Tooling Gotchas below — full restart+rebuild (not Live Coding) is the safe way to land a USTRUCT-in-TArray field addition without losing the data asset's existing rows.
**Wiring completed this session:**
- `DA_ConditionInfo.StunnedEffect = GE_Stunned`, `StunImmunityEffect = GE_StunImmunity` (set via `a:set('StunnedEffect', ...)` — direct top-level properties, not GEComponents, so the generic tool can write them fine).
- `ConditionInfos[2]` (= `Condition.OverCharged`, confirmed by index) `.StunImmunityGraceSeconds = 4.0`, set via dot-path `a:set('ConditionInfos[2].StunImmunityGraceSeconds', 4.0)` — `configure_at` doesn't work here since `FRageInMageConditionInfo` has no `Name`/`ParameterName`-style identifying field for the matcher; direct index dot-notation is the way for this struct.
- `GE_OverCharged.RoutineExpirationEffectClasses` cleared to `()` (was `[GE_StunImmunity]`) — superseded by the immediate-apply-on-trigger approach, would otherwise double-apply immunity on natural expiry. Compiled clean, saved.
**GA_FlashAndAwe Blueprint + assets built this session (2026-06-27):**
- `GE_Cost_FlashAndAwe`/`GE_Cooldown_FlashAndAwe` duplicated from `GE_Cost_BlazingFlame`/`GE_Cooldown_BlazingFlame` (Fire's CrowdControl tier — chosen over `DropRippleBlast` since DropRippleBlast's own Mana modifier curve reference is one of the vestigial RowName-only/no-CurveTable cases flagged in the SearingFlame caveat above, whereas BlazingFlame's is properly wired). Cost curve repointed to a new `CT_ManaCost` row `"FlashAndAwe"` (30/35/40/45/50 across levels 1-5 — BlazingFlame's own row was a broken placeholder with 0s at levels 4-5, not used as a copy source). Cooldown left at BlazingFlame's flat 2.0s default, untuned.
- `GA_FlashAndAwe` (ParentClass=`RageInMageFlashAndAweAbility`) created under `/Game/Blueprints/AbilitySystem/Player/Abilities/Lightning/FlashAndAwe/`. Class defaults: `StartupInputTag=InputTag.3` (matches BlazingFlame's CrowdControl-slot convention), `StartupAbilityTag=Ability.Lightning.FlashAndAwe`, `StartupAbilityTypeTag=Ability.Type.CrowdControl`, `bUseHoldToAim=true`, `AimIndicatorShape=Circle`, `AimIndicatorRadius=400`, `MaxSpellRange=1200`, `StunRadius=400` (flat), `StunDuration=2.0` (flat), `StunImmunityGraceSeconds=4.0`, `FlashDepartEffect=NS_LightningShock`, `FlashLandEffect=NS_Lightning_Strike_Blue`, `FlashSound=SFX_ThunderStrike_01` (all untuned placeholder picks — first audio asset ever set on a Lightning ability, LightningFlash's own `LightningFlashSound` is still `None`).
- EventGraph mirrors `GA_LightningFlash`'s skeleton exactly (skipping reroute nodes and the `UpdateFacingTarget` call, since Flash And Awe teleports rather than aims a directional attack): `ActivateAbility → AimMode(Max Range=GetAimIndicatorRadius) → [On Input Released] → CommitAbilityCost → Branch(Return Value) → [True] ApplyFlashAndAwe(Target Location=AimMode.Aim Position) → CommitAbilityCooldown → EndAbility`; `[False]`, `AimMode.On Cancelled`, and `OnEndAbility` all route to their own `EndAbility` nodes. Compiled clean (0 errors/0 warnings), saved.
- Registered as LightningMage's 2nd startup ability in `DA_CharacterClassInfo` (`CharacterClassInformation{LightningMage}.StartupAbilities`, appended after `GA_LightningFlash`).
- **Tooling note confirmed again this session:** dot-path indexing into `TArray<UObject*>`-typed properties (`GEComponents[0].SubField`) fails even via the Blueprint-asset two-arg `get/set("self", path)` form — same root limitation as the documented GEComponent gotcha below, just confirmed it's not specific to the DataAsset-style generic methods. The workaround that *does* work for plain-struct array elements (not object-pointer arrays) like `UGameplayEffect.Modifiers` (`TArray<FGameplayModifierInfo>`): read the whole array as one export-text string via `a:get('self','Modifiers')`, do a Lua string substitution on it, then `a:set('self','Modifiers', newString)` — round-trips fine since it's never indexing into the array, just replacing the whole blob.
- **Manual step (same GEComponent limitation as `GE_Cooldown_LightningFlash` originally) — RESOLVED:** `GE_Cooldown_FlashAndAwe`'s `TargetTagsGameplayEffectComponent` granted the duplicated-from-source tag `Cooldown.Fire.BlazingFlame` instead of `Cooldown.Lightning.FlashAndAwe`; user fixed it by hand in-editor same session. Confirmed correct.
- **Not yet PIE-tested.** `DA_AbilityInfo` already has a pre-seeded placeholder row for `Ability.Lightning.FlashAndAwe` (row 30 of 62 — all 72 ability tags were pre-seeded project-wide before any abilities existed) but, like `LightningFlash`'s own row, its `InputTag`/`AbilityTypeTag`/description/icon fields are all still empty — that UI-metadata pass is a separate not-yet-started task across the whole Lightning school, not something blocking gameplay.

### Lightning Flash — cone retuned + debug visualization added (DONE this session, hot-patched)
User wanted to confirm the cone hit-area visually before designing a Niagara prediction effect for it. Added a cyan `DrawDebugCone` in `URageInMageLightningFlashAbility::ApplyLightningFlash` (`RageInMageLightningFlashAbility.cpp`), gated `#if ENABLE_DRAW_DEBUG`, using the same draw call shape as `RageInMageConeBlastAbility`'s existing debug cone (flattens to a wedge if `ConeHeightLimit > 0`). Pure function-body change, hot-patched via Live Coding successfully.
**Also retuned the cone shape** per user request ("shorter but wider"): `GA_LightningFlash.ConeRange` 600 → **400**, `ConeHalfAngle` 20° → **35°** (40° total → 70° total). Compiled and saved.
**Next step (not started):** design + build the actual Niagara "prediction" VFX that shows players the cone area live during aim mode (currently Lightning Flash's `AimIndicatorShape=Box` doesn't match the true cone shape at all — the debug cone above is dev-only, no player-facing telegraph exists yet).

### Hit Reaction Grace Period — separate from Condition.Stunned (CODE WRITTEN, BLOCKED ON FULL REBUILD)
**User observation while testing Lightning Flash:** enemies felt stun-locked even outside the Charge/OverCharged CC system. Root cause confirmed by reading `GA_HitReaction` (`InstancedPerActor`, `bRetriggerInstancedAbility=False`): there's already a guard against *retriggering while a reaction is still playing*, but **no grace period after a reaction ends** before the next one can start. Lightning Flash fires all 5 rods back-to-back in one frame, so multiple rods landing on the same enemy can chain flinches with zero recovery window — reads as a stun-lock without `Condition.Stunned` ever being involved. User explicitly wanted this kept separate from the real Stun system (confirmed this is the right call — different problem, same "feels like CC" symptom).
**Implementation (mirrors the existing `BindStunDelegate`/`StunTagChanged` pattern exactly):**
- `ARageInMageCharacterBase`: new `HitReactionGraceSeconds` (`EditDefaultsOnly`, default 0.3f), `BindHitReactionGraceDelegate()` (registers a tag-event listener on `Effects.HitReaction`, same double-bind guard as `BindStunDelegate`), `HitReactionGraceTagChanged` (private handler — records `LastHitReactionEndTime = GetWorld()->GetTimeSeconds()` when the owned-tag count drops to 0), `CanTriggerHitReaction()` (public, checks elapsed time >= `HitReactionGraceSeconds`).
- Bind call added next to the existing `BindStunDelegate()` call in both `RageInMagePlayerCharacter.cpp` (`OnRep_PlayerState`/`PossessedBy` area) and `RageInMageEnemyCharacter.cpp` (`PossessedBy`).
- `RageInMageAttributeSet.cpp`'s `PostGameplayEffectExecute` (incoming-damage branch, ~line 199) now checks `TargetCharacter->CanTriggerHitReaction()` before calling `TryActivateAbilitiesByTag(Effects.HitReaction)` — added `#include "Character/RageInMageCharacterBase.h"`.
**BLOCKED — Live Coding failure, confirmed via raw log (NOT surfaced by `read_log('livecoding')`'s status fields, which showed `is_compiling=false` with no error — had to grep `Saved/Logs/RageInMage.log` directly):** `"Reload/Re-instancing Complete: No object changes detected"` immediately followed by `"LogLiveCoding: Error: Live coding failed, please see Live console for more information"`. New members on `ARageInMageCharacterBase` (a heavily-subclassed base with many live BP instances in the editor) — same "Live Coding cannot hot-patch new class members" category as previous sessions, just a different failure message shape than the C2065 case. **Needs a full editor restart + `Build.bat` rebuild.** Pure code change (no `UDataAsset`/struct-array involved) — unlike the `DA_ConditionInfo` situation, there is no data-wipe risk here, nothing special needs to happen before closing the editor.
**Files:** `RageInMageCharacterBase.h/.cpp`, `RageInMagePlayerCharacter.cpp`, `RageInMageEnemyCharacter.cpp`, `RageInMageAttributeSet.cpp`.

### Flash And Awe — C++ ability class written (NOT yet compiled, no asset exists yet)
**New `URageInMageFlashAndAweAbility`** (`Source/RageInMage/{Public,Private}/AbilitySystem/Abilities/RageInMageFlashAndAweAbility.h/.cpp`), subclass of `URageInMageGameplayAbility` directly (not the damage subclass — this ability deals no damage). One function, `ApplyFlashAndAwe(const FVector& TargetLocation)`:
- Clamps `TargetLocation` to `MaxSpellRange` (inherited base property, 0=unlimited — same clamp convention as `RageInMageChargedSpell::GetArcForDistance`).
- Teleports via `AActor::TeleportTo` (built-in encroachment check — fails gracefully with a `UE_LOG` warning instead of dropping the caster inside geometry; on failure the AoE still fires from wherever the caster currently is, just doesn't move).
- Spawns `FlashDepartEffect` at the old location, `FlashLandEffect` (expects a `StunRadius` float user param) + `FlashSound` at the new one.
- Gathers all living enemies within `StunRadius` of the landing point (mirrors `RageInMageLeapAbility::ApplyLandingExplosion`'s gather pattern) and calls the new `ApplyGuaranteedStun` helper (see above) on each — `StunDuration` and `StunImmunityGraceSeconds` are separate `EditDefaultsOnly` tunables (the latter per the user's item-hook request, kept independent of duration).
**This is a brand-new UCLASS, not new members on an existing one** — per the established gotcha ("brand-new UCLASSes hot-patch fine via Live Coding, only new members on already-loaded classes need a full rebuild"), this could in principle compile separately via Live Coding. Deliberately NOT compiled yet — bundled into the same pending full rebuild as the Hit Reaction Grace Period work above, per user's choice to keep stacking changes rather than rebuild twice.
**Cannot create the `GA_FlashAndAwe` Blueprint asset or wire its EventGraph yet** — the editor has no knowledge of this new C++ class until it's compiled/loaded. Queued for immediately after the rebuild: create `GA_FlashAndAwe` (ParentClass=`RageInMageFlashAndAweAbility`) under a new `/Game/Blueprints/AbilitySystem/Player/Abilities/Lightning/FlashAndAwe/` folder, `GE_Cost_FlashAndAwe`/`GE_Cooldown_FlashAndAwe` (duplicate from an existing Lightning or CrowdControl-tier template), a `CT_ManaCost` row, set `StartupAbilityTag=Ability.Lightning.FlashAndAwe`/`StartupAbilityTypeTag=Ability.Type.CrowdControl` (tags already exist pre-session per the original Lightning Mage plan), and wire the EventGraph: AimMode (`AimIndicatorShape=Circle`, radius = `StunRadius`) → OnInputReleased → CommitAbilityCost → `ApplyFlashAndAwe(AimMode.Aim Position)` → CommitAbilityCooldown → EndAbility — same skeleton as `GA_LightningFlash`/`GA_DropRippleBlast`.
**Files:** `RageInMageFlashAndAweAbility.h/.cpp` (NEW).
**Compile attempted via Live Coding and FAILED** (confirmed via raw log grep — `LogLiveCoding: Error: Live coding failed` — and `class_properties` unable to resolve the class at all). This contradicts the earlier-documented "brand-new UCLASSes hot-patch fine" precedent from the Lightning Flash session — that precedent does NOT reliably hold; don't assume a new class is safe to Live-Coding-compile without checking the raw log (the `read_log('livecoding')` status API has repeatedly failed to surface real failures — always grep `Saved/Logs/RageInMage.log` for `"Starting Live Coding compile"`/`"Live coding failed"`/`"Live coding succeeded"` directly instead of trusting that API alone). Bundled into the same pending full rebuild as everything else below.

### Ceaseless Wrath (Lightning Ultimate) — C++ ability class written (NOT yet compiled, no asset exists yet)
**Design discussion (no zone reuse this time — supersedes the much older "ARageInMageZone reuse, radius 400, tick damage every 0.5s" placeholder idea from the original Lightning Mage plan):** user wanted something like `GA_FallOfTheScorchingSun`/`URageInMageMeteorAbility` (tracked-enemy impacts, no-repeat-per-batch, random fallback) but reusing only that *targeting* idea, not the meteor travel-projectile rig — lightning strikes hit straight down at the target point, they don't fly in from the caster's position, so a distance-based travel time doesn't make sense for this spell. Also explicitly **no stagger** (every strike in a wave fires simultaneously, unlike Meteor's `MeteorStaggerDelay`) and **no lingering zone/burning area** left behind (AoE damage on impact only, like a meteor explosion, but nothing persists after).
**Wave-count formula (user-derived, confirmed correct over an earlier flawed `round(Z/Y)` draft of mine):** first wave fires immediately at t=0, so total waves = `floor(WrathDuration / WaveInterval) + 1` — e.g. Y=1.5s over Z=3s gives waves at t=0, 1.5, 3 → 3 waves, not 2.
**Overlap risk discussion (resolved by design, not by tuning):** with Meteor's staggered-individual-strike approach, overlapping waves would stomp shared per-instance state (`MeteorsRemaining`/`MeteorsPendingImpact`/timer handle) since each wave's strikes resolve over an extended window. Ceaseless Wrath avoids this architecturally rather than relying on careful Y tuning: each wave's chosen impact locations are captured **by value in that wave's own timer lambda**, so there's no shared mutable per-wave state at all — concurrent/overlapping waves literally cannot corrupt each other regardless of how tight `WaveInterval` is tuned.
**New `URageInMageCeaselessWrathAbility`** (`Source/RageInMage/{Public,Private}/AbilitySystem/Abilities/RageInMageCeaselessWrathAbility.h/.cpp`), subclass of `URageInMageDamageGameplayAbility` directly (not `URageInMageMeteorAbility` — deliberate, see above). `BeginCeaselessWrath(TargetCenter)` computes `WavesRemaining` via the formula above, fires the first wave immediately, then repeats via timer every `WaveInterval`. `LaunchWave()` (private): gathers living enemies fresh in `ZoneRadius` each wave (re-tracks current positions, matching FallOfTheScorchingSun's per-wave re-gather), picks up to `StrikeCount` impact points (tracked enemies first, no repeats *within that wave*, random-point fallback once enemies run out, ground-traced same as Meteor), spawns `StrikeEffect` at all of them in the same frame (no stagger), then one shared `StrikeImpactDelay` (default 0.2f, flat VFX-sync constant — NOT distance-based) later applies AoE damage (`StrikeExplosionRadius`) via the inherited `CauseDamage`/`ApplyKnockback` to everything in range of each point, and ends the ability once the final wave's delay resolves. Other tunables: `StrikeCount`, `ZoneRadius`, `WaveInterval`, `WrathDuration` (all `FScalableFloat`, scale with level), `bShowDebug` (same blue-zone/orange-strike debug sphere convention as Meteor/ConeBlast/LightningFlash).
**Compile attempted via Live Coding and FAILED** (same as Flash And Awe above — raw log confirms `"Live coding failed"`, `class_properties` can't resolve the class). Bundled into the pending full rebuild.
**GA_CeaselessWrath Blueprint + assets built this session (2026-06-27):**
- `GE_Cost_CeaselessWrath` duplicated from `GE_Cost_BlazingFlame` (the properly-CurveTable-wired template, not `GE_Cost_FallOfTheScorchingSun`'s own cost GE which has the vestigial RowName-without-CurveTable issue) and repointed to a new `CT_ManaCost` row `"CeaselessWrath"` (50/55/60/65/70 across levels 1-5 — priced as the most expensive spell in the school, Ultimate tier). `GE_Cooldown_CeaselessWrath` duplicated from `GE_Cooldown_FallOfTheScorchingSun` (flat 5.0s, Fire's own Ultimate-tier cooldown).
- `GA_CeaselessWrath` (ParentClass=`RageInMageCeaselessWrathAbility`) created under `/Game/Blueprints/AbilitySystem/Player/Abilities/Lightning/CeaselessWrath/`. Class defaults mirror `GA_FallOfTheScorchingSun`'s Ultimate-tier aim numbers (`StartupInputTag=InputTag.4`, `MaxSpellRange=2500`, `AimIndicatorShape=Circle`, `AimIndicatorRadius=750`) plus Ceaseless-Wrath-specific tuning: `StrikeCount=5`, `ZoneRadius=750`, `StrikeExplosionRadius=200`, `WaveInterval=1.0`, `WrathDuration=4.0` (→ 5 simultaneous-strike waves at t=0,1,2,3,4 per the wave-count formula above), `StrikeEffect=NS_Lightning_Strike_Blue`, `StrikeSound=SFX_ThunderStrike_02`, `KnockbackStrength=100`/`KnockbackUpwardForce=0.3` (matching FallOfTheScorchingSun's meteor knockback). `DamageTypeTags={DamageType.MagicalDamage.Electric=15, Attributes.Mechanics.Charge=25}` per strike — **and critically, `ActivationOwnedTags` was set to mirror those same two tags exactly**, learned the hard way from the Lightning Flash "visual works, deals no damage" bug earlier this session (skipping this silently no-ops the entire magical-damage branch in `ExecCalc_Damage`).
- EventGraph: `ActivateAbility → AimMode(Max Range=GetAimIndicatorRadius) → [On Input Released] → CommitAbilityCost → Branch(Return Value) → [True] BeginCeaselessWrath(Target Center=AimMode.Aim Position) → CommitAbilityCooldown`; deliberately **no `EndAbility` node after `CommitAbilityCooldown`** — the C++ ability ends itself once the final wave resolves (per the design notes above), wiring one in here would end the ability immediately and kill the in-flight wave timer. `[False]`, `AimMode.On Cancelled`, and `OnEndAbility` all still route to their own `EndAbility` nodes as normal. Compiled clean (0 errors/0 warnings), saved.
- Registered as LightningMage's 3rd startup ability in `DA_CharacterClassInfo`.
- **Same manual step still needed:** `GE_Cooldown_CeaselessWrath`'s `TargetTagsGameplayEffectComponent` will still be granting the duplicated-from-source tag `Cooldown.Fire.FallOfTheScorchingSun` instead of `Cooldown.Lightning.CeaselessWrath` — needs the same by-hand fix in-editor as Flash And Awe's cooldown tag (already done) and Lightning Flash's cooldown tag (already done) before.
- **Not yet PIE-tested.**
- **Aim indicator VFX (ground circle matching `ZoneRadius`, class-colored, spawned/positioned/cleaned-up exactly like `GA_FallOfTheScorchingSun`'s `IndicatorVFX` system) added by user directly in-editor**, reusing the same node pattern (`SpawnSystemAtLocation` → `Set IndicatorVFX` variable → `Set Niagara Variable (Float)` for radius from `Break Scalable Float(ZoneRadius)` → `Set Niagara Variable (LinearColor)` from `GetClassProgressBarColor` → `Set World Location` on `On Aim Update` → `Deactivate`+`Destroy Component` on cancel/release). User confirmed "it works great." I had not wired this myself when first building the ability — only set the abstract `AimIndicatorShape`/`AimIndicatorRadius` properties, which drive `AimMode`'s internal clamping math but produce no visible ground telegraph on their own; the real visual circle is this separate per-ability Niagara wiring pattern, copied per-ability in the EventGraph rather than shared in C++. Worth remembering for any future ability that wants a matching ground indicator (`SparkingSphere`/`ZipNZap`/`StaticMegaSurge` will likely want the same treatment).
**Files:** `RageInMageCeaselessWrathAbility.h/.cpp` (NEW).

### MovementSpeed attribute wired to actual movement (DONE this session, hot-patched)
**Found while designing Sparking Sphere's slow effect:** the `MovementSpeed` secondary attribute existed on `URageInMageAttributeSet` (replicated, tag-mapped) but was never read anywhere except two one-shot call sites in `RageInMageEnemyCharacter.cpp` (`BeginPlay` and `HitReactionTagChanged`), both using `MaxWalkSpeed = BaseWalkSpeed + GetMovementSpeed()` (additive) — and the player character had **zero** wiring at all (relied purely on the engine's stock `UCharacterMovementComponent::MaxWalkSpeed` default of 600, confirmed via reflection). A `GE_Slow` built against this attribute would have silently done nothing, same shape of bug as the SearingFlame mana-curve gap. User confirmed they'd already converted `MovementSpeed`'s base-value GE to a multiplicative coefficient (default 1.0, "which can be altered by other means") and asked for the C++ side to be wired up properly (option 1 of two offered).
**Fix — moved `BaseWalkSpeed` up to the shared base class and made it live-reactive:**
- `BaseWalkSpeed` (was `ARageInMageEnemyCharacter`-only, default 100) moved to `ARageInMageCharacterBase` (new default 600, matching the engine's stock `MaxWalkSpeed` so player behavior is unchanged). Confirmed via reflection that `BP_Goblin_Spear`'s existing serialized override (`BaseWalkSpeed=100`) survived the move intact — this is a straightforward property hoist up the hierarchy, not the riskier USTRUCT-in-TArray-element case documented below.
- New `ARageInMageCharacterBase::UpdateMovementSpeed()` (`BlueprintCallable`): `MaxWalkSpeed = BaseWalkSpeed * GetRageInMageAttributeSet()->GetMovementSpeed()` — multiplicative now, not additive.
- New `ARageInMageCharacterBase::BindMovementSpeedDelegate()` + private `OnMovementSpeedAttributeChanged()`: binds `AbilitySystemComponent->GetGameplayAttributeValueChangeDelegate(GetMovementSpeedAttribute())` so `MaxWalkSpeed` updates **live** the instant any GE changes the attribute — mirrors `BindHeatGlowDelegate()`'s exact pattern (`AddUObject`, called after `AttributeSet` is assigned). This is the actual fix for the original gap: previously nothing would re-sync `MaxWalkSpeed` mid-fight at all, only at BeginPlay/hit-reaction-end.
- `RageInMageEnemyCharacter.cpp`: both old call sites replaced with `UpdateMovementSpeed()`; `BindMovementSpeedDelegate()` added alongside the other `Bind*Delegate()` calls in `BeginPlay()`.
- `RageInMagePlayerCharacter.cpp`: `BindMovementSpeedDelegate()` + initial `UpdateMovementSpeed()` added alongside the other binds in `InitPlayerAbilityActorInfo()` (after `InitializeDefaultAttributes()`, so the attribute's coefficient default is already set before the first read).
- New members on `ARageInMageCharacterBase` again — same risk category as the Hit Reaction Grace Period work, but **this one hot-patched fine via Live Coding** (confirmed via raw log: `"Live coding succeeded, data type changes may cause packaging to fail..."`, no C2065). Reinforces that this gotcha is genuinely inconsistent, not a hard rule — always check the raw log rather than assuming either outcome.
**Files:** `RageInMageCharacterBase.h/.cpp`, `RageInMageEnemyCharacter.h/.cpp`, `RageInMagePlayerCharacter.cpp`.
**Not yet PIE-tested**, but unblocks `SlowEffectClass` on the in-progress Sparking Sphere zone (see below) and any future slow/haste GE project-wide.

### Sparking Sphere (Lightning Secondary) — built this session (DONE, not yet PIE-tested)
**Design (user-directed):** standard lightning damage+Charge tick, glides through (not explodes on) enemies, lingers until lifetime runs out — needed a genuinely new shape, since every existing precedent was either a single-hit-then-die projectile (`ARageInMageProjectile`) or a stationary zone (`ARageInMageZone`). **No slow effect** — that was carried over by me from an older pre-session design note ("Sparking Sphere — slow zone...") without re-confirming it was still wanted; user explicitly said they never asked for a slow and had it removed (see below). Pure damage/Charge zone now, nothing else.
**New `ARageInMageSparkingSphereZone`** (`Source/RageInMage/{Public,Private}/Actor/RageInMageSparkingSphereZone.h/.cpp`), subclass of `ARageInMageZone` — adds a `UProjectileMovementComponent` (gravity-free, fixed speed) so the zone itself travels in a straight line instead of sitting still, reusing the base class's existing overlap-tracked damage-tick infrastructure unchanged (no `DamageTickEnemiesInside` override — the base class's behavior is exactly what's wanted, nothing to add on top of it). `PrimaryActorTick.bCanEverTick = true` (override of the base zone's default-off) + `Tick()` draws a debug sphere at the zone's current location/radius every frame (`bShowDebug` toggle, `#if ENABLE_DRAW_DEBUG`) so the actual moving hit-check volume is visible as it travels — added per user request, since the base zone's one-time `DrawDebugSphere` at `BeginPlay` only shows the *spawn* point and goes stale immediately once the zone starts moving.
**New `URageInMageSparkingSphereAbility`** (`Source/RageInMage/{Public,Private}/AbilitySystem/Abilities/RageInMageSparkingSphereAbility.h/.cpp`), subclass of `URageInMageDamageGameplayAbility` directly (mirrors Ceaseless Wrath's reasoning — no need for the Meteor/chain machinery). `BeginSparkingSphere(TargetLocation)`: computes a flattened (Z=0) direction from the avatar to `TargetLocation`, spawns the zone facing that direction via `SpawnActorDeferred`+`FinishSpawning` (rotation set so `ProjectileMovementComponent`'s `InitialSpeed` carries it forward — same convention `RageInMageProjectileSpell` already uses), builds the damage spec from `DamageEffectClass`/`DamageTypeTags` (mirrors `RageInMageMeteorAbility::MakeDamageSpec()`), and passes `ZoneRadius`/`ZoneDuration`/`DamageTickInterval`/`ZoneEffect`/`SphereSpeed`/`bShowDebug` through to the spawned zone instance. Does **not** call `EndAbility` internally — mirrors Flash And Awe, not Ceaseless Wrath, since this is a one-shot synchronous spawn-and-forget. The ability's existing `bShowDebug` (already used for the spawn-direction debug arrow) now also drives the zone's per-frame debug sphere via this passthrough — one toggle controls both.
**Discovered + fixed a real gap while initially designing a slow:** see the MovementSpeed section above — the attribute existed but nothing read it. Fixed project-wide this session regardless of the slow itself being cut — that fix stays, it's legitimate infrastructure independent of this ability.
**`GE_Slow` built then unused:** created at `/Game/Blueprints/AbilitySystem/Shared/Effects/Conditions/GE_Slow` (`HasDuration` 1.5s, `MovementSpeed` `MultiplyAdditive` -0.5, `AggregateByTarget`/refresh-on-application) while the slow was still in scope. Left in place (harmless, reusable by any future slow source) after the slow itself was cut from Sparking Sphere — not deleted, just currently unreferenced by anything. Flag for cleanup if it stays unused long-term.
**Tooling note:** `create_asset(path, 'GameplayEffect')` (no `ParentClass` option) silently created a different, incompatible asset shape than every existing `GE_*` in the project — `a.type` showed `"GameplayEffect"` directly instead of delegating to the Blueprint wrapper, and `help()` reported no asset-specific methods at all. Deleted it and recreated correctly via the proven path: `create_asset(path, 'Blueprint', {ParentClass='GameplayEffect'})` — matches every other GE in the codebase (`a.parent_class == 'GameplayEffect'`, delegates to `open_blueprint`). Don't use the bare `'GameplayEffect'` type alias for new GEs.
**Assets:** `BP_SparkingSphereZone` (ParentClass=`RageInMageSparkingSphereZone`), `GA_SparkingSphere` (ParentClass=`RageInMageSparkingSphereAbility`, `InputTag.RMB`/`Ability.Type.Secondary`, `ZoneRadius=200`/`ZoneDuration=5s`/`DamageTickInterval=0.5s`/`SphereSpeed=400`, `bShowDebug=true` (left on for active testing), `DamageTypeTags` Electric=6/Charge=8 per tick (lower than Lightning Flash's per-rod values since this ticks repeatedly over 5s instead of hitting once), `ActivationOwnedTags` mirroring those two tags again — same mandatory step as every other Lightning damage ability, learned the hard way earlier this session), `GE_Cost_SparkingSphere`/`GE_Cooldown_SparkingSphere` (duplicated from `GE_Cost_SwirlingYoyo`/`GE_Cooldown_SwirlingYoyo`, Water's Secondary tier — chosen because its cost modifier has a properly-wired `CurveTable` reference, unlike some other templates flagged earlier), new `CT_ManaCost` row `"SparkingSphere"` (25→37 across levels 1-5). EventGraph mirrors Flash And Awe's skeleton exactly (Aim(Line, length=`GetAimIndicatorLength`) → Cost → Branch → `BeginSparkingSphere` → Cooldown → EndAbility). Compiled clean (confirmed via raw log, twice — once with the slow, once after removing it), registered as LightningMage's 4th startup ability.
**Same manual GEComponent tag step will be needed:** `GE_Cooldown_SparkingSphere` still grants the duplicated-from-source `Cooldown.Water.SwirlingYoyo` tag instead of `Cooldown.Lightning.SparkingSphere`.
**Not yet PIE-tested** — no confirmation yet that the zone actually travels correctly, that the debug sphere tracks it visibly, or that damage/Charge ticks land as expected. `bShowDebug=true` is already on, ready for a test pass.

### BLOCKING — Live Coding layout corruption, full rebuild required before anything else (2026-06-27)
**Symptom (user-reported):** PIE message log showed 26 repeated `Blueprint Runtime Error: "Attempted to access missing property 'OnHealthChanged'/'OnMaxHealthChanged'"` errors from `WBP_EnemyHealthBar` trying to bind delegates on `ARageInMageEnemyCharacter` — and separately, every enemy hit by Sparking Sphere was instantly killed instead of taking the small per-tick damage it's configured for (Electric=6/Charge=8).
**Root cause (diagnosis, not yet independently re-verified post-rebuild):** this session ran **three separate rounds of Live Coding compiles that each changed class layout** on the same heavily-subclassed hierarchy, back to back, with no real editor restart between them:
1. Added `BaseWalkSpeed`/`UpdateMovementSpeed()`/`BindMovementSpeedDelegate()`/`OnMovementSpeedAttributeChanged()` to `ARageInMageCharacterBase` (the shared parent).
2. **Removed** `BaseWalkSpeed` from `ARageInMageEnemyCharacter` (a child of the class just changed in step 1) in the same session, right next to the `OnHealthChanged`/`OnMaxHealthChanged` `UPROPERTY` delegates that are now reportedly "missing" at runtime.
3. Two more rounds of add/remove on `ARageInMageSparkingSphereZone`/`URageInMageSparkingSphereAbility` (removing `SlowEffectClass`, adding `bShowDebug`/`Tick()`).
Each individual compile reported `"Live coding succeeded"` in the raw log with no errors — but stacking multiple layout-changing patches across a parent+child pair in one live session, without ever reloading from a real compiled DLL, is exactly the failure category already documented below ("Live Coding cannot hot-patch new class members") — this time it manifested as **silent reflection corruption** (a property that genuinely exists in source reads as missing at runtime) rather than a hard `C2065`, and probably explains the instakill too: if `ARageInMageEnemyCharacter`'s live memory layout drifted out of sync with its actual compiled size/offsets, reads of `Health`/`MaxHealth`/the `AttributeSet` pointer could land on garbage.
**Fix:** full editor restart + `Build.bat`/UBT rebuild (not another Live Coding compile) — this resets every loaded class back to a single consistent compiled state. The C++ source itself is believed correct (no header changes were made to `OnHealthChanged`/`OnMaxHealthChanged` themselves, only to neighboring members), but **this must be confirmed after the rebuild**, not assumed.
**After the rebuild, verify in this order:**
1. PIE with an enemy present — confirm `WBP_EnemyHealthBar`'s "Attempted to access missing property" errors are gone from the PIE message log (`read_log('message', {log='PIE'})`).
2. Cast Sparking Sphere at an enemy — confirm damage lands as a small per-tick amount (not instakill), and the debug sphere (`bShowDebug=true` on `GA_SparkingSphere`) visibly follows the travelling zone.
3. Re-confirm Lightning Flash chain-lightning and the stun-immunity fix still work (both were last confirmed working *before* this session's Live Coding stacking — low risk since neither's classes were touched again after that confirmation, but cheap to recheck).
4. PIE-test Flash And Awe and Ceaseless Wrath for the first time (still never confirmed working at all).
**No data-wipe risk this time** — no `UDataAsset`/struct-array changes were involved in this session's batch, so nothing needs to be un-saved before closing the editor.

### Manual GEComponent cooldown-tag fixes still outstanding
Same tool limitation as documented below (NeoStack can't write `TargetTagsGameplayEffectComponent`'s tag field) — each of these still grants the tag duplicated from its template source instead of its own:
(All four prior offenders — `GE_Cooldown_LightningFlash`, `GE_Cooldown_FlashAndAwe`, `GE_Cooldown_CeaselessWrath`, `GE_Cooldown_SparkingSphere` — are now fixed, confirmed by the user 2026-06-28. No open items here currently; ZipNZap has no cooldown GE at all (charges replace it, see below) and Static Megasurge's hasn't been built yet, so check its cooldown tag once it exists.)

## Engine Upgrade: UE 5.6 -> 5.8 (2026-06-20)
Project directory moved from `RageInMage5.6` to `RageInMage5.8`. `.uproject` EngineAssociation and both Target.cs files (`IncludeOrderVersion::Unreal5_8`) were already updated by the user before this session. Updated `CLAUDE.md` (Project Overview + Build Commands + Module Configuration) to match — old text referenced 5.6 paths/version. NeoStack Lua bridge confirmed working against the 5.8 editor. No other breakage found yet; user says "everything still works like it did before" — flag if any node names/APIs turn out to have changed in 5.8 during future sessions.

---

## Pending Tasks

### ZipNZap + Static Megasurge — BLUEPRINT ASSETS BUILT 2026-07-02 (the last 2 of 6 Lightning abilities; C++ already compiled)
**2026-07-02 UPDATE — assets authored this session (no rebuild needed; the C++ below had already landed in a prior rebuild — verified `RageInMageZipNZapAbility`/`RageInMageStaticMegaSurgeAbility` both loaded with all members, plus `ARageInMageCharacterBase::Get/SetBaseWalkSpeed`).**
- **ZipNZap — FULLY DONE, compiles clean, registered LightningMage 5th.** `GA_ZipNZap` (ParentClass=`RageInMageZipNZapAbility`) at `/Game/Blueprints/AbilitySystem/Player/Abilities/Lightning/ZipNZap/`. Defaults: `InputTag.1`/`Ability.Type.Movement`, `bUseHoldToAim=false`, `InstancingPolicy=InstancedPerActor`, `MaxCharges=2`, `RechargeTime=4`, `CharacterSpeed=(Value=3000)`, `DashDistance=(Value=600)`, `DashWidth=150`, `DamageEffectClass=GE_Damage`, `DamageTypeTags={Electric=8,Charge=10}`, `ActivationOwnedTags` mirrors those two (mandatory — else ExecCalc no-ops). `GE_Cost_ZipNZap` created fresh (Blueprint/GameplayEffect), Mana modifier → new `CT_ManaCost` row `ZipNZap` (20→28). **No cooldown GE** (charges replace it). EventGraph: ActivateAbility→CommitAbilityCost→Branch(RV)→[True]BeginZipNZap→EndAbility; [False]/OnEndAbility→their own EndAbility. Compiled clean, saved.
- **Static Megasurge — BUILT, ONE expected compile error pending a by-hand GEComponent.** `GA_StaticMegaSurge` (ParentClass=`RageInMageStaticMegaSurgeAbility`) at `.../StaticMegaSurge/`. Defaults: `InputTag.2`/`Ability.Type.Defensive`, `bUseHoldToAim=false`, `Duration=(Value=4)`, `BuffEffectClass=GE_StaticMegaSurge_Buff`, `DamageEffectClass=GE_Damage`, `DamageTypeTags={Electric=40,Charge=10}` (reflect payload), `ActivationOwnedTags` mirrors those two, `ReflectEffect=NS_LightningShock`, Cost/Cooldown GE refs set. `GE_StaticMegaSurge_Buff` (Infinite, `Resistance_Damage` Additive +50). `GE_Cost_StaticMegaSurge` (Mana → new `CT_ManaCost` row `StaticMegaSurge` 30→42). `GE_Cooldown_StaticMegaSurge` (HasDuration 12s). EventGraph: ActivateAbility→CommitAbilityCost→Branch(RV)→[True]ApplyStaticMegaSurge→CommitAbilityCooldown (deliberately **no EndAbility on success path** — C++ ends itself when the buff expires); [False]/OnEndAbility→EndAbility. Registered LightningMage 6th.
- **TWO BY-HAND GEComponent STEPS REMAIN (tool can't write `TargetTagsGameplayEffectComponent` — same limitation as every prior ability):**
  1. `GE_Cooldown_StaticMegaSurge` → add "Grant Tags to Target Actor" component granting `Cooldown.Lightning.StaticMegaSurge`. **This is what's causing GA_StaticMegaSurge's current compile ERROR** ("CooldownGameplayEffectClass grants no tags") — GAS refuses a tagless cooldown GE. Adding the tag + recompiling GE + GA clears it. (Unlike prior abilities whose cooldown GEs were *duplicated* and thus already had a component granting the WRONG tag — those compiled-but-wrong; this fresh one has NO component so it hard-errors instead.)
  2. `GE_StaticMegaSurge_Buff` → add "Grant Tags to Target Actor" component granting `Status.Reflecting` (the buff modifier is set, but without this tag `PostGameplayEffectExecute`'s reflect check never fires).
- **Still inert until separate work lands:** the "halve incoming damage" half needs Reaper's `Resistance_Damage` wiring in `ExecCalc_Damage` (per prior entry). Neither spell PIE-tested. `ReflectEffect`/`ChainImpactEffect`/audio are placeholder VFX picks.
- **Tooling notes this session:** (a) `duplicate_asset(src, dest)` treats the 2nd arg as a NAME-only and pops a blocking modal if it contains `/` ("name cannot contain a /") — froze the whole Lua bridge until Reaper dismissed it. **Avoid `duplicate_asset`; instead `create_asset(path,'Blueprint',{ParentClass='GameplayEffect'})` then stamp `Modifiers` via the export-text string** (round-trips fine, fully non-interactive — this is how both cost GEs + the buff were made). (b) Setting a class-ref property via `set()` returns `true` even when the path is wrong and resolves to `None` — always `get()` it back to confirm it resolved (caught a wrong `GE_Damage` path this way: real path is `/Game/Blueprints/AbilitySystem/Shared/Effects/Damage/GE_Damage`, note the `/Damage/` subfolder). (c) `DamageTypeTags` TMap export format: `(((TagName="X"), (Value=8.000000)),((TagName="Y"), (Value=10.000000)))`. (d) DA StartupAbilities append: read the map value, `string.find(marker,1,true)` (PLAIN find — pattern-escaping the anchor silently 0-matched), splice, `map_set`+`save`.

### ZipNZap + Static Megasurge — C++ written (already compiled; superseded by the build entry above)
**User request:** build the final two Lightning-school abilities. ZipNZap design was revised mid-session from the original "instant teleport-dash" concept (Flash And Awe's `TeleportTo` pattern) to an actual physical dash that goes through the existing `BaseWalkSpeed`/`MovementSpeed`-coefficient pipeline (see "MovementSpeed attribute wired to actual movement" earlier in this file), specifically so a character with a higher `MovementSpeed` coefficient dashes both faster AND further automatically. Static Megasurge's damage-reduction mechanic was redirected from a proposed new `DamageTakenMultiplier` attribute (rejected by the user) to a `Resistance_Damage` secondary attribute the user had already half-implemented themselves (see next entry) — the actual "halve incoming damage" formula isn't wired into `ExecCalc_Damage` yet; the user is doing that themselves.

**ZipNZap (`URageInMageZipNZapAbility : URageInMageLightningDamageAbility`, NEW, `Source/RageInMage/{Public,Private}/AbilitySystem/Abilities/RageInMageZipNZapAbility.h/.cpp`):**
- **First ability-charge system in this project** (2 independently-recharging uses instead of a single cooldown GE). Implemented as plain ability-instance state, not a stacking Cooldown GE — `InstancingPolicy` forced to `InstancedPerActor` in the C++ constructor (so charge state survives between casts; every other ability in the project defaults to `InstancedPerExecution`, confirmed via reflection on `GA_LightningFlash`/`GA_SparkingSphere`). `MaxCharges`/`RechargeTime` (EditDefaultsOnly), `CurrentCharges` initialized in `OnGiveAbility()`. One `RechargeTimerHandle` re-arms itself sequentially each time a charge regenerates (until back at max) — behaviorally identical to N independent timers at a fixed interval, far less bookkeeping. `CanActivateAbility()` overridden to block at 0 charges (same gating role a cooldown tag would normally play). **No cooldown GE exists for this ability at all** — charges fully replace it. `GetChargesRemaining()`/`GetMaxCharges()` exposed `BlueprintPure` for whenever UI wants to show charge count (not built this session — the project's `UWaitCooldownChange` async task is GE/tag-based and won't show anything for this ability out of the box; flagged as a known gap, not addressed).
- **Dash mechanic (per user correction — NOT a teleport):** `BeginZipNZap()` saves `OriginalBaseWalkSpeed`, then calls the character's `SetBaseWalkSpeed(CharacterSpeed.GetValueAtLevel(...))` (new public wrapper, see below) so `MaxWalkSpeed` becomes `CharacterSpeed * (current MovementSpeed coefficient)` — same multiplier every other speed change in the game already goes through, so a generally-faster character automatically dashes faster too. `ActualDashDistance = DashDistance.GetValueAtLevel(...) * GetMovementSpeed()` — same coefficient applied a second time to the distance, per the user's explicit request ("because of the faster dash it should also dash further"), so total distance scales independently from speed rather than just falling out of a fixed-duration formula. A 60Hz timer (`ZipTick()`, mirrors `URageInMageSurfAbility::SurfTick`'s exact convention) sets `CharacterMovementComponent::Velocity` directly every tick (bypasses acceleration ramp-up) until `ActualDashDistance` has been travelled (`FVector::Dist2D` from the start point) or a `MaxDashSafetyDuration` (2s default) hard cap is hit — the safety cap exists so a wall-blocked dash can't get stuck forever and never free its charge. `EndZip()` reverts `BaseWalkSpeed` and zeroes velocity. Re-entrant casts (second charge fired while the first dash is still in flight) call `EndZip()` first so `OriginalBaseWalkSpeed` is always captured from a clean resting state.
- **Zap mechanic (user confirmed this part of the design as-is):** one upfront `SweepMultiByChannel` (`ECC_Pawn`, sphere radius `DashWidth/2`) along the full computed dash path at cast time — same sweep-along-a-line pattern already used by `URageInMageBeamSpell::BeamTickDamage`. Every unique non-friendly actor hit gets the inherited `CauseChainDamage()` (so it also jumps onward like every other Lightning spell), gated `HasAuthority()`.
- **New public API added to `ARageInMageCharacterBase`** (`RageInMageCharacterBase.h/.cpp`): `GetBaseWalkSpeed()`/`SetBaseWalkSpeed(float)` — `BaseWalkSpeed`/`UpdateMovementSpeed()` were `protected`, unreachable from an ability class. `SetBaseWalkSpeed` sets the value and immediately calls `UpdateMovementSpeed()` in one call. **New members on this already-loaded, heavily-subclassed class** — same risk category as the "BLOCKING — Live Coding layout corruption" incident two sessions ago; needs the full rebuild below, not Live Coding.

**Static Megasurge (`URageInMageStaticMegaSurgeAbility : URageInMageDamageGameplayAbility`, NEW, `Source/RageInMage/{Public,Private}/AbilitySystem/Abilities/RageInMageStaticMegaSurgeAbility.h/.cpp`):**
- Lifetime pattern mirrors `URageInMageShieldAbility` exactly (read directly for the convention): `ApplyStaticMegaSurge()` applies a self-buff GE (`BuffEffectClass`, not yet built — needs `Resistance_Damage` Additive modifier + `Status.Reflecting` TargetTags GEComponent, the latter needing the same by-hand GEComponent step every tag-granting GE in this project needs), binds to the AttributeSet's new `OnDamageReflected` delegate, and starts a `Duration`-length expiration timer. The ability stays alive (not `EndAbility`'d immediately) for the buff's whole duration so the bound delegate handle stays valid, then `HandleBuffExpired()` removes the GE + unbinds + ends the ability — exactly `RemoveShieldEffect`/`HandleShieldExpired`'s shape.
- **New `Status.Reflecting` tag + `OnDamageReflected` delegate on `URageInMageAttributeSet`** (`RageInMageAttributeSet.h/.cpp`, `RageInMageGameplayTag.h/.cpp`) — same shape as the existing `OnShieldAbsorbedDamage`/`Status.Shielded` pair. Wired into `PostGameplayEffectExecute`'s incoming-damage block, right after the Shield check: if the target has `Status.Reflecting` (and isn't hitting itself), broadcasts the attacker actor. Unlike Shield, this does **not** return early — the target still takes its (eventually Resistance_Damage-reduced) damage normally; reflecting is additive on top.
- `HandleDamageReflected(Attacker)` calls the inherited `CauseDamage(Attacker)` for a flat, non-scaling, non-chaining Electric+Charge hit (deliberately `URageInMageDamageGameplayAbility`, not the chain-lightning subclass, since a reflect zap shouldn't also jump onward to nearby enemies — flag if that turns out to be wrong). Also spawns `ReflectEffect` Niagara at the attacker and fires a `BlueprintImplementableEvent OnReflectProc(Attacker)` for BP-side VFX/SFX.
- **The "halve incoming damage" half of the design isn't functional yet** — `Resistance_Damage` isn't read anywhere in `ExecCalc_Damage`'s actual formula yet (the user is wiring that in themselves, see next entry). Once that's in, the buff GE's `Resistance_Damage` modifier value should follow this project's established resistance convention: flat percentage points 0-100, applied as `Damage *= 1 - Resistance/100` (confirmed by reading `Resistance_PhysicalDamage`'s usage in `ExecCalc_Damage.cpp`) — so "halve damage" means an Additive +50 modifier on the buff GE, not 0.5.

**`Resistance_Damage` attribute — finished this session (user had started it, left it half-wired):**
Found via `git status`/reading the files that the user had already added the `FGameplayAttributeData Resistance_Damage` UPROPERTY (with `OnRep_Resistance_Damage`, replication wired) to `RageInMageAttributeSet.h/.cpp`, and a `DEFINE_ATTRIBUTE_CAPTUREDEF(URageInMageAttributeSet, Resistance_Damage, Target, false)` to `ExecCalc_Damage.cpp` — but **no matching `DECLARE_ATTRIBUTE_CAPTUREDEF(Resistance_Damage)`**, which would not have compiled (`DEFINE_` references a struct member only `DECLARE_` creates). Added the missing `DECLARE_` line — this is a pure compile-correctness fix, not the actual damage-reduction logic, which the user is doing themselves (per their explicit "leave ExecCalc_Damage as I will sort that out" instruction — did not touch `Execute_Implementation` or the `ResistanceTagToCaptureDef`/`DamageTypeToResistance` maps). Also added the missing `ATTRIBUTE_ACCESSORS(URageInMageAttributeSet, Resistance_Damage)` macro call (every other attribute has one; this one didn't, so no `GetResistance_Damage()`/`SetResistance_Damage()`/`GetResistance_DamageAttribute()` existed yet), and a new native tag `Resistance.Damage` (`RageInMageGameplayTag.h/.cpp`) since none existed. Deliberately did **not** add it to `FRageInMageGameplayTag::DamageTypeToResistance` (that map is keyed by specific damage-type tags; Resistance_Damage is meant to apply universally, which is the user's call to wire) or to `URageInMageAttributeSet::TagsToAttributes` (no other Resistance_* attribute is in that map either, so this matches existing convention, not an oversight). No default-value initializer needed — confirmed the `URageInMageAttributeSet` constructor is empty and every other Resistance_* attribute already relies on `FGameplayAttributeData`'s implicit 0 default (none are touched by `GE_SecondaryAttributes_Enemy`/`_Player`), so `Resistance_Damage` defaulting to 0 (no reduction) needs no extra wiring — this is NOT the same bug shape as the MovementSpeed-defaults-to-0 issue below (that one needed a multiplicative baseline of 1.0; this one is an additive-from-zero reduction, where 0 is already the correct neutral default).

**BLOCKED — needs a full rebuild, not Live Coding, before the Blueprint assets can be made:** this batch touches `ARageInMageCharacterBase` (new public methods) and `URageInMageAttributeSet` (new delegate member) — both already-loaded, heavily-subclassed classes, the exact category that caused the "BLOCKING — Live Coding layout corruption" incident two sessions ago. Plus two brand-new UCLASSes (`URageInMageZipNZapAbility`, `URageInMageStaticMegaSurgeAbility`), which have inconsistently succeeded/failed via Live Coding in past sessions (Flash And Awe/Ceaseless Wrath's new classes both outright failed). Did not attempt Live Coding at all this session — went straight to asking the user to close the editor for a full `Build.bat`/UBT rebuild, per their standing "rebuilds for correctness are fine" preference.

**Next steps once the rebuild lands:**
1. Confirm `RageInMageEditor` builds clean — no header/usage errors expected, but unverified by an actual compile yet.
2. Create `GA_ZipNZap` (ParentClass=`RageInMageZipNZapAbility`, `InstancingPolicy=InstancedPerActor` — must be set in the BP defaults too, not just trusted from the C++ constructor) under a new `/Game/Blueprints/AbilitySystem/Player/Abilities/Lightning/ZipNZap/` folder, `bUseHoldToAim=false` (instant on press, no aim mode — this is a dash, not an aimed spell), `GE_Cost_ZipNZap` + a `CT_ManaCost` row (no cooldown GE — charges replace it), EventGraph `ActivateAbility → CommitAbilityCost → Branch → BeginZipNZap → EndAbility`, register as LightningMage's 5th startup ability. Suggested defaults (from the earlier design discussion): `MaxCharges=2`, `RechargeTime=4s`, `CharacterSpeed≈3000`, `DashDistance=600`, `DashWidth=150`, `DamageTypeTags={Electric=8, Charge=10}` (inherited Lightning fields).
3. Build `GE_StaticMegaSurge_Buff` (Infinite duration, `Resistance_Damage` Additive +50, `Status.Reflecting` TargetTags GEComponent — the GEComponent part needs the by-hand editor step). Create `GA_StaticMegaSurge` (ParentClass=`RageInMageStaticMegaSurgeAbility`, `InputTag.2`/`Ability.Type.Defensive`) under `/Game/Blueprints/AbilitySystem/Player/Abilities/Lightning/StaticMegaSurge/`, `GE_Cost_StaticMegaSurge`/`GE_Cooldown_StaticMegaSurge` + `CT_ManaCost` row, EventGraph `ActivateAbility → CommitAbilityCost → Branch → ApplyStaticMegaSurge → CommitAbilityCooldown` (no `EndAbility` node here — same reasoning as Ceaseless Wrath, the C++ side ends itself once the buff expires), register as LightningMage's 6th (and final) startup ability. Suggested defaults: `Duration=4s`, reflect `DamageTypeTags={Electric=40, Charge=10}`.
4. New cooldown GEs for both will need the same by-hand `TargetTagsGameplayEffectComponent` tag fix every prior ability has needed (ZipNZap has none, since it has no cooldown GE).
5. PIE-test both — neither has been run even once. For ZipNZap specifically: confirm the dash actually covers ~`ActualDashDistance`, confirm a second charge can be spent mid-dash from the first (re-entrant `EndZip()` path), confirm charges visibly regenerate one at a time. For Static Megasurge: confirm the reflect fires (note the damage-reduction half won't visibly do anything until the user finishes the `ExecCalc_Damage` `Resistance_Damage` wiring).
**Files:** `RageInMageZipNZapAbility.h/.cpp` (NEW), `RageInMageStaticMegaSurgeAbility.h/.cpp` (NEW), `RageInMageCharacterBase.h/.cpp`, `RageInMageAttributeSet.h/.cpp`, `RageInMageGameplayTag.h/.cpp`, `ExecCalc_Damage.cpp`.

### Enemies don't move toward player — MovementSpeed attribute evaluates to 0 (Coefficient bug FIXED 2026-07-01, but speed issue still persists — root cause not fully closed)
**Symptom (user, 2026-06-27, post-rebuild PIE test):** Sparking Sphere confirmed working (user says too powerful, will retune `GA_SparkingSphere` themselves). Separately: enemies stand completely still until hit, then will attack if the player is already in range, but never walk toward the player at all.
**Root cause confirmed by reading assets directly (not yet fixed):** `DA_CharacterClassInfo.SecondaryAttributes` is a single shared `TSubclassOf<UGameplayEffect>` applied to literally every character on spawn (player AND all enemies — confirmed via `CharacterClassInfo.h`: `SecondaryAttributes` is a top-level field, not per-`ECharacterClass`; `URageInMageAbilitySystemLibrary::InitializeDefaultAttributes` applies it unconditionally). That field currently points at `GE_SecondaryAttributes_Enemy` (confirmed via `a:get("SecondaryAttributes")` on the DA). Its `MovementSpeed` modifier:
```
ModifierOp=MultiplyAdditive, MagnitudeCalculationType=AttributeBased,
AttributeBasedMagnitude=(BackingAttribute=Agility)   <- no Coefficient field at all
```
Every other modifier in this same GE explicitly sets `Coefficient=(Value=...)`; this one doesn't, so it silently defaults to `FScalableFloat()` = 0. The `MovementSpeed` attribute itself also has no initializer (defaults to 0 base). Net: `GetMovementSpeed()` evaluates to 0 for every character → `ARageInMageCharacterBase::UpdateMovementSpeed()` (`MaxWalkSpeed = BaseWalkSpeed * GetMovementSpeed()`, see "MovementSpeed attribute wired to actual movement" entry in Completed Work) computes `MaxWalkSpeed = 0`. The C++ formula itself is correct and not the bug (confirmed this explicitly with the user, who initially questioned whether `UpdateMovementSpeed()` was wrong — it isn't). The BT's `BTS_FindNearestPlayer` service computes `DistanceToTarget` independently of actual movement (just `GetDistanceTo`), so the melee-attack decorator (`DistanceToTarget < 300`) can still fire whenever the player walks into a stationary enemy — explains the "attacks if in range but never approaches" symptom exactly.
**Likely cause of the bug itself:** this modifier was almost certainly hand-edited during the recent "MovementSpeed wired to actual movement" session (the user said they'd "already converted MovementSpeed's base-value GE to a multiplicative coefficient, default 1.0") — probably switched `ModifierOp` from the old `Override` to `MultiplyAdditive` but didn't clear the leftover `AttributeBased`/`BackingAttribute=Agility` wiring from the old `25*Agility` Override formula, and never set a `Coefficient`, so it defaults to 0 instead of the intended 1.0 baseline.
**Also note — separate sibling assets exist but are NOT live:** `GE_SecondaryAttributes_Player`, `GE_SecondaryAttributes_Ghoul`, `GE_SecondaryAttributes_Player_TEST` all exist on disk with the OLD `Override, Coefficient=25, BackingAttribute=Agility` formula for MovementSpeed (large Agility-scaled value, not a 1.0 coefficient) — only `GE_SecondaryAttributes_Enemy` is actually wired into `DA_CharacterClassInfo` and therefore the only one that matters at runtime. Since it's shared, this bug likely also affects the player's own `MaxWalkSpeed`, even though the user only reported it for enemies (not yet confirmed either way — worth asking).
**Fix applied and confirmed (2026-07-01, by Reaper directly, outside any agent session):** `GE_SecondaryAttributes_Enemy`'s `MovementSpeed` modifier Coefficient issue described above — "We fixed the coefficient bug. That has been sorted." Not verified by this agent which exact change was made (Reaper did it themselves) — if revisiting, confirm current state of `GE_SecondaryAttributes_Enemy`'s `MovementSpeed` modifier directly rather than assuming the originally-proposed `Override 1.0` shape was what was used.

**2026-07-01 — bug persists despite the coefficient fix, so there's a second/separate cause. Added temporary debug instrumentation to isolate it:** Reaper confirms the speed bug is "still all over the place" even after the above fix landed — meaning the zeroed-Coefficient issue was real but was not the whole story. Added `ARageInMageCharacterBase::LogMovementSpeedDebug()` — a 0.5s repeating `FTimerHandle` (started in `BeginPlay()`, NOT `Tick()`, since `PrimaryActorTick.bCanEverTick = false` is intentionally set project-wide for perf and wasn't touched) that prints one persistent on-screen line per actor (keyed by `GetUniqueID()` so it updates in place instead of stacking): `[Speed] {ActorName} | Vel={actual velocity} | MaxWalkSpeed={CharacterMovement value} | MovementSpeedAttr={raw GAS attribute}` — red if either MaxWalkSpeed or the attribute is <= 0, green otherwise. Lets you see, per enemy simultaneously, whether the attribute itself is 0 again (regression / a different modifier still broken), whether MaxWalkSpeed lags the attribute (update-delegate bug), or whether MaxWalkSpeed is healthy but Velocity stays 0 anyway (would point to an AI/pathing bug instead of a speed-value bug at all — worth checking `BTS_FindNearestPlayer`/movement tasks in the behavior tree next if the readout shows healthy speed values with zero actual velocity). New members on `ARageInMageCharacterBase` — Live Coding failed (confirmed via raw log grep, `Live coding failed`), needs the full rebuild, which Reaper is about to do (closing editor).
**Files this session:** `RageInMageCharacterBase.h/.cpp` (new `MovementSpeedDebugTimerHandle` + `LogMovementSpeedDebug()`).

### RidingTide — Drag Enemies Along the Wave (C++ ✅ COMPILED & LIVE — `DragSpeed`+`DamageTickInterval` verified on `RageInMageWaveBox` 2026-07-26 session 3. NO LONGER BLOCKED — only the PIE test below is outstanding.)
**User request:** Enemies hit by RidingTide (Water Movement) should be dragged along with the surfing character — currently the player just passes through them. User explicitly clarified the design: hit-detection must come from the **WaveBox's own overlap** (think of it as a moving wall), NOT a radius check around the caster. The pre-existing `SurfDamageCheck()` did a sphere-radius check around the avatar (`GetLivePlayersWithinRadius`), which is the wrong source of truth per the user's correction.

**Design implemented (code written, NOT yet compiled successfully):**
- `URageInMageSurfAbility::SurfDamageCheck()` reverted to its original one-shot-pushback radius-check behavior, but now only runs as a **fallback** when `!ActiveWaveBox` (e.g. a future ground-slide ability with no wave box VFX) — gated in `SurfTick()`.
- `ARageInMageWaveBox` is now the real hit-detector. Added `Tick()` (`PrimaryActorTick.bCanEverTick = true`) and a new private `ProcessEnemyOverlaps()` that polls `BoxCollision->GetOverlappingActors()` every tick, filters via `ICombatInterface` + `IsDead` + `IsBothEnemy`, then:
  - **Continuous drag every tick (ungated):** `TargetChar->LaunchCharacter(FVector(Forward.X, Forward.Y, 0) * DragSpeed, true, false)` — `Forward` is the box's own forward vector, which the ability already keeps in sync with `SurfDirection` every tick via `SetActorRotation`. `bZOverride=false` so it doesn't fight gravity.
  - **Damage + on-hit-effect** gated by a new `DamageTickInterval` per-enemy timestamp map (`DamagedEnemyTimestamps` on the WaveBox), same pattern as the old ability-side check.
  - **Extra upward pop** (`PushbackStrength`) applied alongside each damage tick only (not continuous, so it can't rocket enemies upward).
- New `ARageInMageWaveBox` properties (populated by `URageInMageSurfAbility::BeginSurf` right after spawn): `DamageEffectSpecHandle`, `OnHitEffectSpecHandle`, `DragSpeed` (= `SurfSpeed.GetValueAtLevel()` — enemies move at the **same speed as the surfer**), `PushbackStrength`, `DamageTickInterval`.
- New `URageInMageSurfAbility::MakeSurfOnHitSpec()` helper (mirrors `MakeSurfDamageSpec`/`MakeTrailDamageSpec`) builds the on-hit GE spec once at `BeginSurf` instead of per-hit.

**BLOCKED — Live Coding limitation, not a code bug:** `exec_command('LiveCoding.Compile')` fails with `RageInMageWaveBox.cpp(168,46): error C2065: 'OnHitEffectSpecHandle': undeclared identifier`, even though it's correctly declared as a `UPROPERTY` in `RageInMageWaveBox.h` and UHT did run during the compile. See "Tooling Gotchas" below — Live Coding cannot hot-patch new class members (this header diff adds 5 new UPROPERTYs + a `Tick()` override + a `TMap` member to `ARageInMageWaveBox`), only function-body edits. Needs a full editor restart + normal `Build.bat`/UBT compile.

**Next steps after the rebuild:**
1. Confirm `RageInMageEditor` builds clean (the code itself should be correct — this is purely a Live Coding hot-patch ceiling).
2. Test in PIE on `/Game/Maps/Tournament_Forest_2` — user has placed 3 `BP_Goblin_Spear` enemies there near `(-1300,-2700,48)`, far from `PlayerStart` at `(-1801,168,92)`; move player or goblins closer to test conveniently.
3. Cast RidingTide (`InputTag.1`) through them; confirm (a) they're dragged at the surfer's speed/direction instead of being walked through, (b) damage ticks at `SurfDamageTickInterval` (0.2s on `GA_RidingTide`), (c) no upward-rocketing.
4. Current `GA_RidingTide` tuning: `SurfSpeed=600`, `PushbackStrength=150` (semantics changed — now an upward-pop magnitude, not a one-shot horizontal launch; likely needs retuning after playtest), `SurfDamageTickInterval=0.2`.
**Files (saved to disk, uncompiled):** `RageInMageSurfAbility.h/.cpp`, `RageInMageWaveBox.h/.cpp`.

### Debug Shapes/Logs to Remove Before Ship
- `ARageInMageCharacterBase::LogMovementSpeedDebug()` (2026-07-01) — per-actor on-screen speed readout via a 0.5s repeating timer started in `BeginPlay()`. Temporary, added specifically to chase the MovementSpeed=0 bug; remove (or comment out per the usual convention) once that bug is confirmed fixed.
- DropRippleBlast cone blast has a cyan debug cone (`DrawDebugCone` in `RageInMageConeBlastAbility.cpp`). Remove or gate behind a CVar before shipping.
- SwirlingYoyo investigation left heavy debug instrumentation in place (was essential for diagnosing the carry bug below, but needs cleanup before ship):
  - `RageInMageYoyoSpell.cpp` `LaunchYoyo()` — uncommitted `GEngine->AddOnScreenDebugMessage` block tagged "DEBUG — remove after fixing spawn issue"; `RecastYoyo()` has several more (Recast BLOCKED/FAILED/SUCCESS messages, committed).
  - `RageInMageYoyoProjectile.cpp` `Tick()` — draws a cyan `DrawDebugSphere` + velocity `DrawDebugLine` every frame; `BeginPlay`/`Destroyed`/`OnSphereOverlap` have several `UE_LOG(LogRageInMage, Warning, TEXT("[YOYO:...]"...))` calls.
  - `RageInMageAbilitySystemComponent.cpp` `AbilityInputTagHeld`/`Released` — verbose `[ASC:HELD]`/`[PC:HELD]`/`[SPEC DUMP]` logging on every input tick.

---

## Saved Plans (ready to implement when needed)

### Yoyo Spell Aim Indicator — Trajectory Prediction
**Status:** Plan complete, not started
**Summary:** Swirling Yoyo (water boomerang) needs trajectory prediction during aim mode. Adds `SimulateYoyoTrajectory()` to `URageInMageYoyoSpell` that replicates projectile tick physics in a pure loop, outputs array of world-space points. Aim lifecycle: `BeginAimMode()` / `UpdateYoyoAimIndicator()` / `EndAimMode()` with BlueprintImplementableEvents. Initial visual via debug lines, Niagara later.
**Files:** `RageInMageYoyoSpell.h/.cpp`, `GA_SwirlingYoyo` EventGraph
**Key GUIDs:** ActivateAbility `916D7EED4EF02C8D6700DDBE2DC8CCD4`, AimMode task `99A597084DDB1DD8DFF01DB4B8679D3E`, CommitCost `5EB83F6648672173649DA0BEA4F8D9F2`, EndAbility(cancel) `54FC7348420A69EA1CB3C0B302A556FE`

### SoothingBubble — Multi-Hit Shield + Heal
**Status:** DONE
**Summary:** Water school Defensive ability. See Completed Work below.

### Spread Aim Indicator for SpreadProjectileSpell
**Status:** DONE — already fully implemented. See Completed Work below.

---

## Completed Work (recent)

### Cooldown Reduction attribute (DONE 2026-07-27, C++ ✅ compiled via Live Coding "Live coding succeeded")
**Reaper's request:** "We need a Cooldown Reduction attribute" — an attribute he'd skipped when building the secondary set.
**New `CooldownReduction` secondary attribute** on `URageInMageAttributeSet` (replicated + `OnRep_CooldownReduction` + `DOREPLIFETIME_CONDITION_NOTIFY` + `TagsToAttributes` entry), native tag `Attributes.Secondary.CooldownReduction`. It is a **PERCENT** (0 = none, 50 = half-length cooldowns), defaults to 0 so nothing needs initialising — exactly the same "gear/passive grants it, base value 0" shape as `Poise`.
**Where it actually bites:** new `URageInMageGameplayAbility::ApplyCooldown` override (all 72 spells inherit it, incl. Blueprint children, because BP "Commit Ability" routes through the same virtual). It builds the cooldown spec itself via `MakeOutgoingGameplayEffectSpec(CooldownGE->GetClass(), ...)`, reads `Spec->GetDuration()` as the base, then `Spec->SetDuration(Reduced, /*bLockDuration=*/true)` and `ApplyGameplayEffectSpecToOwner`. **Deliberate design choice: zero `GE_Cooldown_*` assets were touched** — whatever duration each asset authors stays the "base" cooldown, so this scales to all 72 without asset churn. Falls back to `Super::ApplyCooldown` when reduction is ~0, when there's no spec, and skips scaling for non-finite durations (instant/infinite).
**Three EditDefaultsOnly knobs on the ability base** (per-ability tunable, same philosophy as the knockback constants): `bAffectedByCooldownReduction` (default true — turn off for ultimates whose cooldown is the design), `MaxCooldownReductionPercent` (80) and `MinCooldownAfterReduction` (0.5s floor). Reduced duration is also `FMath::Min`'d against the base so the floor can never *lengthen* a short cooldown. `GetEffectiveCooldownReduction()` is BlueprintPure and returns the already-clamped value.
**Reads clamped at the USE SITE, not in `PreAttributeChange`** — deliberate, matching the `FMath::Max(0.f, GetPoise())` precedent; avoids the classic GAS problem where PreAttributeChange clamping leaks unclamped base values back through the aggregator when buffs are removed.
**Cooldown UI needed no changes:** `UWaitCooldownChange::OnActiveEffectAdded` already reads `ASC->GetActiveEffectsTimeRemaining()` off the live effect, so it reports the reduced time for free.
**Same follow-ups as Poise, not done:** no GE/item grants `CooldownReduction` yet (0 for everyone today), and it's not in the `UAttributeInfo` DataAsset for Attribute-Menu display.
**Files:** `RageInMageAttributeSet.h/.cpp`, `RageInMageGameplayTag.h/.cpp`, `RageInMageGameplayAbility.h/.cpp`
**Caveat flagged to Reaper:** patched in via Live Coding, which warns on data-type changes — a new UPROPERTY on an AttributeSet changes class layout, so a full editor restart is advised before trusting it in PIE.

### Knockback Momentum Transfer + Bounce-Off-Immovable System (DONE)
**User request:** Pushback/knockback currently just stops dead the instant a launched character collides with another actor. Wanted: if the target can absorb it, transfer some momentum to the target and slow (not stop) the original; if the target can't be moved, bounce off it instead.
**New `Poise` secondary attribute** added to `URageInMageAttributeSet` (replicated, `OnRep_Poise`, gameplay tag `Attributes.Secondary.Poise`, added to `TagsToAttributes` map). Defaults to 0 for everyone — purely an additive bonus meant to come from gear (no GEs grant it yet; that's on the user to design/itemize later). Explicitly NOT the existing `ImmovableMass` attribute — user clarified that's a separate Earth-mage stacking/CC-overload mechanic (charges when standing still, decays when moving/dashing, breaks at 100% total slow) unrelated to general knockback resistance.
**New shared `ARageInMageCharacterBase::ApplyKnockbackImpulse(Impulse, bXYOverride, bZOverride)`** — scales the impulse down by `100/(100+TargetPoise)` then calls `LaunchCharacter`. **Refactored all 8 existing direct `LaunchCharacter` call sites to route through it** so the behavior is uniform everywhere: `RageInMageDamageGameplayAbility::ApplyKnockback`, `RageInMageConeBlastAbility::ApplyConeBlast`, `RageInMageLeapAbility::ApplyLandingExplosion`, `RageInMageSurfAbility::SurfDamageCheck`, `RageInMageProjectile.cpp`'s anonymous-namespace `ApplyKnockbackToActor`, `RageInMageYoyoProjectile::OnSphereOverlap`, and both call sites in `RageInMageWaveBox::ProcessEnemyOverlaps` (drag + vertical pop). Each site's `Cast<ACharacter>` was changed to `Cast<ARageInMageCharacterBase>` since the new function lives there, not on the engine base class.
**New `ARageInMageCharacterBase::NotifyHit` override** (fires for free on any blocking-hit during character movement — confirmed via engine source `UPrimitiveComponent::DispatchBlockingHit`/`AActor::DispatchBlockingHit`, no special collision flags needed for kinematic character sweeps). Logic, gated to `bSelfMoved && HasAuthority()` and a per-actor-pair 0.2s de-dupe (sliding/multi-sweep can fire `NotifyHit` several times per frame):
- Ignored entirely if horizontal speed < `MinKnockbackTransferSpeed` (default 300) — keeps normal AI-pathing bumps from triggering this.
- Target resistance = `BaseShoveResistance` (400) + `OtherPoise * PoiseResistancePerPoint` (5/pt) if the other actor is a character; **infinite** (`TNumericLimits<float>::Max()`) if it's anything else (walls/static geometry always "win").
- If mover's speed beats the resistance: target gets shoved via `ApplyKnockbackImpulse(FlatVelocity * KnockbackTransferRatio(0.6) * OvercomeRatio, true, false)`, and the mover's own velocity is scaled down (not zeroed) by `1 - KnockbackAbsorptionRatio(0.5) * (1-OvercomeRatio)` — slows but never stops.
- Otherwise (immovable): mover's horizontal velocity is reflected across the flattened hit normal and scaled by `KnockbackBounceRestitution` (0.4) — a real bounce-off, this was the part the user got most excited about ("if the target can't be moved then bounce off of it using some sort of applied physics!").
- All 6 constants (`MinKnockbackTransferSpeed`, `BaseShoveResistance`, `PoiseResistancePerPoint`, `KnockbackTransferRatio`, `KnockbackAbsorptionRatio`, `KnockbackBounceRestitution`) are `EditDefaultsOnly` on the base class, so each BP character subclass can retune without recompiling.
**Calibration data gathered before picking constants:** configured `KnockbackStrength`/`PushbackStrength` values across abilities range ~100-3000 (GA_BlazingFlame=200, GA_SimpleIce=400, GA_IgnitionLeap=800, GA_DropRippleBlast=3000, GA_RidingTide=150); base enemy walk speed ~100. The 300 transfer threshold deliberately filters out the weakest knockbacks (100-200) so they keep the old stop-dead feel, while anything ability-grade gets the new treatment.
**Build note:** this touched two widely-included headers (`RageInMageAttributeSet.h`, `RageInMageCharacterBase.h`), so it was a much bigger Live Coding recompile than a single-file body edit (~9 minutes, vs ~60-90s for earlier function-body-only fixes this session) — but it DID succeed via Live Coding despite adding new UPROPERTY/UFUNCTION members (contradicts the earlier-documented "Live Coding cannot hot-patch new class members" gotcha from the RidingTide session — that one hit a hard `C2065` error, this one only produced a soft warning: `"Live coding succeeded, data type changes may cause packaging to fail if assets reference the new or updated data types"`). Confirmed via `class_properties`/`class_methods` reflection that `Poise`, `ApplyKnockbackImpulse`, and the tunables are live. **Before packaging/shipping, do a full `Build.bat` rebuild** per that warning — Live Coding patches aren't guaranteed cooker-safe for new data types.
**Known follow-up not addressed:** no GE/item currently grants `Poise` (it's 0 for everyone by default, so today every character has identical base shove-resistance); not wired into the Attribute Menu UI (`UAttributeInfo` data asset) for display. Both are natural next steps whenever the user wants to itemize it.
**Files:** `RageInMageAttributeSet.h/.cpp`, `RageInMageGameplayTag.h/.cpp`, `RageInMageCharacterBase.h/.cpp`, `RageInMageDamageGameplayAbility.cpp`, `RageInMageConeBlastAbility.cpp`, `RageInMageLeapAbility.cpp`, `RageInMageSurfAbility.cpp`, `RageInMageProjectile.cpp`, `RageInMageYoyoProjectile.cpp`, `RageInMageWaveBox.cpp`

### TearsDoFall — AoE Zone Never Actually Dealt Damage (DONE)
**Symptom:** User reported TearsDoFall (Water Ultimate) is supposed to be an AoE zone damaging everything inside every X seconds, but it didn't seem to work that way.
**Root cause:** `GA_TearsDoFall`'s "Spawn Actor from Class" node (spawning `BP_IceStormZone`) had its `Damage Effect Spec Handle` pin completely unconnected — nothing in the BP graph ever built a `FGameplayEffectSpecHandle`. `ARageInMageZone::DamageTickEnemiesInside()` (`RageInMageZone.cpp:118`) bails out immediately if `!DamageEffectSpecHandle.IsValid()`, so the timer fired every `DamageTickInterval` (already correctly configured: `ZoneRadius=800`, `ZoneDuration=8`, `DamageTickInterval=1.0`) but always did nothing. Confirmed `GA_TearsDoFall`'s `DamageEffectClass` (`GE_Damage`) and `DamageTypeTags` (`DamageType.MagicalDamage.Ice=8`, `Attributes.Mechanics.Heat=-10`) were already correctly configured as class defaults — they were just never consumed, since `CauseDamage()` (the only function that reads them) was never called anywhere in the ability graph.
**Why this is BP-only (unlike every other zone ability):** Every other `ARageInMageZone` subclass user (`RageInMageMeteorAbility::MakeFireZoneDamageSpec()`, `RageInMageYoyoSpell::MakePuddleDamageSpec()`, `RageInMageSurfAbility`'s trail zone) is backed by a dedicated C++ ability subclass that builds the spec via `MakeOutgoingGameplayEffectSpec` + `AssignTagSetByCallerMagnitude` per `DamageTypeTags` entry, then assigns it directly to the spawned zone's `DamageEffectSpecHandle` in C++. `GA_TearsDoFall` has no such C++ subclass (parent is plain `RageInMageDamageGameplayAbility`), so it fell through the cracks.
**Fix (Blueprint-only, no C++/recompile needed):** Added the same pattern directly in `GA_TearsDoFall`'s EventGraph between the existing `Cast To Pawn` and `Spawn Actor from Class` nodes: `Get Ability Level` → `Make Outgoing Gameplay Effect Spec` (Class=`GE_Damage`) → `Assign Tag SetByCaller Magnitude` (`DamageType.MagicalDamage.Ice`=8.0) → `Assign Tag SetByCaller Magnitude` (`Attributes.Mechanics.Heat`=-10.0) → feeds the spawn node's `Damage Effect Spec Handle` pin. Exec chain re-spliced: `CastToPawn.then → Assign1.execute → Assign2.execute → Spawn.execute`. Compiled clean (0 errors/warnings) and saved.
**Known follow-up not addressed (separate from what was asked):** `BP_IceStormZone`'s `SlowEffectClass`/`FreezeEffectClass` (the progressive slow→freeze stacking mechanic in `ARageInMageIceZone::DamageTickEnemiesInside()`) are both still unset (`None`), so that secondary mechanic still no-ops — no `GE_Slow`/`GE_Freeze` assets exist anywhere in the project yet. User only asked about the core damage tick; flagged here for if/when they want the freeze stacking built out.
**Files:** `GA_TearsDoFall.uasset`

**Follow-up bug (same session, found after first fix shipped):** after the above fix, user reported the zone still only hit "a few" enemies, usually near the outer rim — not everyone inside. Root cause was in the shared base class `ARageInMageZone::BeginPlay()` (`RageInMageZone.cpp`), affecting **every** zone type (Fire/Ice/Water), not just TearsDoFall:
1. `ZoneCollision->SetSphereRadius(ZoneRadius)` (resizing from the constructor's default 300 up to the real radius, e.g. 800) was called *before* `ZoneCollision->OnComponentBeginOverlap/EndOverlap.AddDynamic(...)`. `USphereComponent::SetSphereRadius` internally calls `UpdateOverlaps()` when the radius changes, which fires fresh `BeginOverlap` events for anyone newly in range — but with the delegates not bound yet, those events fired into a void and `ActorsInside` never got populated from them.
2. Even fixing the ordering wouldn't fully solve it: overlap delegates only fire on **state changes**. Component registration (which happens before `AActor::BeginPlay`) already marks anyone within the small default-300 radius as "overlapping" internally — so even with delegates bound early, the engine wouldn't re-fire a BeginOverlap for them since it only broadcasts deltas, not a full state dump.
Net effect: anyone already standing in the AoE's footprint at cast time was silently invisible to `ActorsInside` forever; only enemies who wandered across the boundary *after* the zone spawned (naturally entering from the outer rim) ever got tracked/damaged. Exactly matches the reported symptom.
**Fix:** in `ARageInMageZone::BeginPlay()`, right after binding the overlap delegates (and after the radius/lifespan setup), explicitly snapshot `ZoneCollision->GetOverlappingActors(InitialOverlaps, APawn::StaticClass())` and seed `ActorsInside` with whoever's already there (same instigator/`IsBothEnemy` filtering as `OnZoneOverlap`), instead of relying solely on delta-based overlap events. Added `#include "GameFramework/Pawn.h"`. Pure function-body change (no new header members) — hot-patched via `exec_command('LiveCoding.Compile')` successfully, confirmed via `read_log('livecoding')` (`is_compiling: false`, no crash).
**Files:** `RageInMageZone.cpp`, `RageInMageZone.h` (include only)
**User confirmed fixed in PIE.**

### Tournament_Forest_2 — NavMeshBoundsVolume Fix (DONE)
**Symptom:** Enemies placed in `Tournament_Forest_2` didn't move or chase the player.
**Root cause:** No `NavMeshBoundsVolume` existed in the level (`navmesh_list_bounds()` → 0 volumes; `navmesh_info()` → `has_nav_data=false`, 0 tiles).
**Fix:** Added a `NavMeshBoundsVolume` actor (`NavMeshBoundsVolume_All`) sized to the landscape footprint + margin (`BoxExtent=(4000,5500,1000)` at world origin — landscape's own `GetActorBounds` was `Extent=(3750,5250,100)`). See "Tooling Gotchas" below for the non-obvious part of how this had to be done. Ran `navmesh_build()` → 324 tiles, `has_nav_data=true`; verified with `navmesh_test_path()` between `PlayerStart` and a far corner. Saved the level.
**Files:** `Tournament_Forest_2.umap`.

### SwirlingYoyo — Fixed Spawn Socket + "Carrying" Player Bug (DONE)
**Symptom:** Casting SwirlingYoyo (Water RMB) spawned the yo-yo underneath the player and visibly dragged the character along with it instead of flying off and exploding on impact.
**Root cause (two bugs):**
1. `BP_SwirlingYoyo`'s `WaterMesh` (StaticMeshComponent, visual-only, attached to the `Sphere` root) still had its default `BodyInstance` collision: `CollisionEnabled=QueryAndPhysics`, `CollisionProfileName="BlockAllDynamic"`, `bEnableGravity=True`, `bAutoWeld=True`. This made the cosmetic mesh a real physics body that blocked/welded against the player capsule — the actual gameplay collision is the dedicated `Sphere` component (`QueryOnly`, overlap-only), so `WaterMesh` should never have had physics collision at all.
2. `GA_SwirlingYoyo`'s `MakeLiteralGameplayTag` (feeding `LaunchYoyo`'s `SocketTag`) was set to `CombatSocket.RightHand` instead of `CombatSocket.Weapon`. `RightHand` resolves via the character mesh's hand bone (which on this character/skeleton wasn't placed correctly, resolving near ground level, Z≈4), instead of the weapon tip socket (`Weapon->GetSocketLocation(WeaponTipSocketName)` in `RageInMageCharacterBase.cpp`).
**Fix:**
- Set `WaterMesh`'s `BodyInstance` to `CollisionEnabled=NoCollision`, `CollisionProfileName="NoCollision"`, `bEnableGravity=False`, `bAutoWeld=False`.
- Changed the `MakeLiteralGameplayTag` pin value to `(TagName="CombatSocket.Weapon")` in `GA_SwirlingYoyo`.
- Verified live in PIE on `/Game/Maps/Dungeon`: spawn Z went from ~4 (ground) to ~106 (weapon tip), and the orb now flies independently of the character.
**Files:** `BP_SwirlingYoyo.uasset`, `GA_SwirlingYoyo.uasset`
**Note:** see Pending Tasks above — debug logging/draws added during this investigation (and an earlier session's) are still in place and should be cleaned up before ship.

### TearsDoFall — Null Instigator Crash in IsBothEnemy (DONE)
**Symptom:** Crash when casting TearsDoFall (Water Ultimate), inside `IsBothEnemy` with the first actor NULL.
**Root cause:** `GA_TearsDoFall`'s "Spawn Actor from Class" node (spawning `BP_IceStormZone`) never wired its `Owner`/`Instigator` pins, so the spawned ice zone's `GetInstigator()` was always null. `ARageInMageIceZone::DamageTickEnemiesInside()` (`RageInMageIceZone.cpp:25`) passes that null straight into `URageInMageAbilitySystemLibrary::IsBothEnemy()` (`RageInMageAbilitySystemLibrary.cpp:354`), which dereferenced it unconditionally.
**Fix:**
- Hardened `IsBothEnemy()` with a null check on both actors (defense-in-depth — same call pattern exists in `RageInMageZone.cpp`, `RageInMageFireWall.cpp`, `RageInMageYoyoProjectile.cpp`).
- Wired `GA_TearsDoFall`'s spawn node: added a `Cast To Pawn` of the avatar actor, feeding both `Owner` and `Instigator` pins (Instigator pin requires `Pawn`, not `Actor`).
**Files:** `RageInMageAbilitySystemLibrary.cpp`, `GA_TearsDoFall.uasset`
**Build note:** the C++ null-check change needs a module recompile before it's live in PIE/packaged builds (the Blueprint wiring fix is already live since it's data-only).

### ASC Debug Log Crash Fix (DONE)
- `RageInMageAbilitySystemComponent.cpp:143` — `UE_LOG` called `AbilitySpec.Ability->CheckCooldown()` with `GetCurrentActorInfo()` which is null when the ability isn't executing. Replaced with `AbilityActorInfo.Get()` (always valid on the ASC).

### RidingTide — Steering + Direction Fix (DONE)
- **Direction bug:** GA_RidingTide BP passed raw `AimPosition` (world point) as direction to `BeginSurf`. Added `GetAvatarActor→GetActorLocation` + subtraction to compute proper direction vector.
- **Steering:** Added `bCanSteer` and `SurfSteerRate` properties to `URageInMageSurfAbility`. `SurfTick` reads `GetCurrentAimWorldPosition()` from `ARageInMagePlayerController` and smoothly interpolates `SurfDirection` toward aim via `FMath::RInterpTo`. Works for both M&K (cursor) and controller (right thumbstick).
- Set `bCanSteer=true`, `SurfSteerRate=5.0` on GA_RidingTide.
- **Files:** `RageInMageSurfAbility.h/.cpp`, `GA_RidingTide.uasset`

### DropRippleBlast — Cone Height Limit + Debug Shape (DONE)
- Added `ConeHeightLimit` property to `URageInMageConeBlastAbility` (default 0 = unlimited).
- Gameplay filter skips targets with Z distance > `ConeHeightLimit` from caster.
- Debug `DrawDebugCone` renders flat wedge shape matching the height-limited cone (3s duration, cyan).
- Set `ConeHeightLimit=150` on GA_DropRippleBlast.
- **Files:** `RageInMageConeBlastAbility.h/.cpp`, `GA_DropRippleBlast.uasset`

### Enemy Pushback Fix (DONE)
- **Problem:** `LaunchCharacter` pushback was inconsistent — sometimes enemies just popped up slightly instead of flying backwards.
- **Root cause:** `HitReactionTagChanged` in `RageInMageEnemyCharacter.cpp` set `MaxWalkSpeed=0` during hit reactions. `CharacterMovementComponent` clamps velocity to `MaxWalkSpeed`, killing the horizontal impulse.
- **Fix:** Removed the `MaxWalkSpeed=0` on hit reaction start. Only restore walk speed when hit reaction ends. AI movement is still blocked by the BT's `HitReacting` blackboard key.
- Also changed `LaunchCharacter` `bXYOverride` from `true` to `false` so horizontal push adds to velocity instead of replacing it.
- **Files:** `RageInMageEnemyCharacter.cpp`, `RageInMageConeBlastAbility.cpp`

### Riding Tide — Wave Box System (DONE)
- Created `ARageInMageWaveBox` (new actor): invisible box collision under caster during surf
  - `UBoxComponent` root: overlaps `ECC_GameTraceChannel1` (projectiles) + `ECC_Pawn`
  - Absorbs enemy projectiles (destroys them harmlessly)
  - Lightning exception: if projectile has `DamageType_MagicalDamage_Electric` SetByCaller > 0, routes damage to rider's ASC
  - Friendly projectiles pass through (same pattern as `ARageInMageFireWall`)
- Modified `URageInMageSurfAbility` with wave box support:
  - New properties: `WaveBoxClass`, `MaxWaveHeight` (200), `WaveRiseDuration` (0.5s), `WaveDescendDuration` (0.8s), `WaveBoxHalfExtent` (100x30x10), `bInterruptOnHit`
  - 3-phase height interpolation in `CalculateCurrentWaveHeight()`: Rise (InterpEaseOut) → Cruise (constant) → Descend (InterpEaseIn)
  - `BeginSurf`: disables `bConstrainToPlane`, switches to `MOVE_Flying`, spawns wave box, registers `Effects_HitReaction` tag delegate
  - `SurfTick`: sets character Z via `SetActorLocation(SurfStartZ + CurrentHeight)`, updates wave box position/rotation
  - `EndSurf`: destroys wave box, restores `bConstrainToPlane=true` + `MOVE_Walking`, unregisters delegate
  - `OnCasterHitDuringSurf`: interrupts surf when hit reaction tag fires
- Modified `ARageInMageProjectile::OnProjectileOverlap` to ignore `ARageInMageWaveBox` (wave box handles its own overlap)
- Configured `GA_RidingTide`: `WaveBoxClass=ARageInMageWaveBox`, `bInterruptOnHit=true`
- **Files:**
  - `Source/RageInMage/Public/Actor/RageInMageWaveBox.h` (NEW)
  - `Source/RageInMage/Private/Actor/RageInMageWaveBox.cpp` (NEW)
  - `Source/RageInMage/Public/AbilitySystem/Abilities/RageInMageSurfAbility.h` (MODIFIED)
  - `Source/RageInMage/Private/AbilitySystem/Abilities/RageInMageSurfAbility.cpp` (MODIFIED)
  - `Source/RageInMage/Private/Actor/RageInMageProjectile.cpp` (MODIFIED)
  - `GA_RidingTide.uasset` (CONFIGURED)

### Dual-Ability Cross-Activation Bug Fix (DONE)
**Problem:** All ability inputs (LMB, RMB, 1-4) triggered the primary spell. Root cause: two bugs in the controller support input system.
**Bug 1:** `SetupInputComponent()` in `RageInMagePlayerController.cpp` called `BindAbilityActions()` twice — once for M&K config and once for Controller config. Both configs reference the SAME `IA_Ability_*` InputAction objects, so pressing LMB fired two callbacks with different InputTags (InputTag.LMB + InputTag.Controller.LT), matching two different abilities simultaneously.
**Fix:** Removed the controller `BindAbilityActions` call. Both IMCs share the same InputAction assets, so the M&K binding handles gamepad inputs too. Controller InputTags in ability specs are used only for UI display (key icons).
**Bug 2:** `InputTagToControllerInputTag` mapping in `RageInMageGameplayTag.cpp` had LMB→Controller.RT and RMB→Controller.LT, but `DA_ControllerInputConfig` maps Primary→Controller.LT and Secondary→Controller.RT. The code mapping was swapped.
**Fix:** Swapped the mapping to LMB→Controller.LT, RMB→Controller.RT to match the data asset.
**Files modified:**
- `Source/RageInMage/Private/Player/RageInMagePlayerController.cpp` (removed duplicate BindAbilityActions + debug logging)
- `Source/RageInMage/Private/RageInMageGameplayTag.cpp` (swapped LMB/RMB controller tag mapping)
- `Source/RageInMage/Private/AbilitySystem/RageInMageAbilitySystemComponent.cpp` (removed debug logging)
- `Source/RageInMage/Public/Input/RageInMageEInputComponent.h` (removed debug logging)

### Spell Globe Keybind Display — Reactive Updates (DONE)
- `FOnKeybindDisplayChanged` delegate on `RageInMagePlayerController.h:23`, broadcast via `BroadcastKeybindDisplayChanged()` (`.cpp:571-574`)
- Called from all 6 rebind functions in `ControlsWidgetController.cpp`: SetBinding, ResetBinding, ResetAllBindings, SetControllerBinding, ResetControllerBinding, ResetAllControllerBindings
- `WBP_SpellGlobeUpdate` EventGraph binds to delegate, filters by InputTag (both M&K and Controller), updates `Text_KeyBind`
- Also reacts to `OnInputDeviceChanged` for M&K ↔ gamepad switches

### Spread Aim Indicator for SpreadProjectileSpell (DONE)
- C++ functions in `URageInMageSpreadProjectileSpell`: `SpawnSpreadIndicators()`, `UpdateSpreadIndicators()`, `DestroySpreadIndicators()`, `HasActiveSpreadIndicators()`
- `GA_SimpleIce` BP fully wired: AimMode → OnAimUpdate spawns/updates N spread indicators, OnInputReleased destroys indicators then spawns spread projectiles, OnCancelled cleans up
- Uses `NS_Aim_Line_Water` Niagara system, class color from `GetClassProgressBarColor()`
- Cone angle: `ConeHalfAngle=15°` (30° total), `ProjectileCount` scales with ability level

### Controller Support Phase 3 — UI Updates (DONE)
- Toggle logic in `WBP_ControlsSettings` was already fully wired (M&K/Controller tab clicks, bShowingControllerBindings toggle, GetAllBindings/GetAllControllerBindings, ScrollBox repopulation, tab highlight colors, ResetAll branching)
- Added gamepad B-button (`Gamepad_FaceButton_Right`) close support to `WBP_TabbedMenu` OnKeyDown (OR'd with existing Escape key check)
- Added gamepad shoulder buttons: RB (`Gamepad_RightShoulder`) calls `IncreaseTabIndex`, LB (`Gamepad_LeftShoulder`) calls `DecreaseTabIndex` for tab switching
- Start/View button menu toggle already wired: `IA_TabbedMenu` mapped to `Gamepad_Special_Left` in `IMC_ControllerContext`, handled by `BP_RageInMagePlayerCharacter` EventGraph (`EnhancedInputAction IA_TabbedMenu` → Branch on MenuActive → Create/Remove widget)
- D-pad navigation works via UMG auto-focus: all interactive buttons have `IsFocusable=True`, ScrollBox has `NavigationDestination=IntoView` + `ScrollWhenFocusChanges=AnimatedScroll`, `WBP_TabbedMenu` has `bIsFocusable=True` and receives focus via `SetInputModeGameAndUI(InWidgetToFocus)`
- Compiled and saved `WBP_TabbedMenu`

### SoothingBubble — Multi-Hit Shield + Heal (DONE)
- C++ `URageInMageShieldAbility` already had full multi-hit support (MaxAbsorbCount, per-hit tracking, accumulated heal, heal conversion to self + nearby friendlies)
- `OnShieldAbsorbedDamage` delegate on `URageInMageAttributeSet` already wired (broadcasts in `PostGameplayEffectExecute` when `Status.Shielded` tag present)
- Fixed `GE_Water_Shield`: removed stale `Condition.Frozen` asset tag (was incorrectly on AssetTags component). Grants `Status.Shielded` tag correctly (Infinite duration).
- Fixed `GE_Water_Heal`: SetByCaller DataTag was empty — set to `Attributes.Vital.Health` to match `GA_SoothingBubble`'s `HealMagnitudeTag`
- Set `MaxAbsorbCount = 3` on `GA_SoothingBubble` (was 1)
- Wired `N_AquaShield` Niagara system into `Spawn System Attached` node in `GA_SoothingBubble` EventGraph (bAutoDestroy=false, manual cleanup in OnShieldAbsorbed/OnShieldExpired)
- BP flow: ActivateAbility -> CommitCost -> ActivateShield -> CommitCooldown. OnShieldActivated spawns N_AquaShield attached to character mesh. OnShieldAbsorbed/OnShieldExpired destroy VFX component.
- All assets compiled and saved

### Controller Support Phase 1 — C++ (DONE)
- `StartupControllerInputTag` on ability base class
- `InputTagToControllerInputTag` mapping in `FRageInMageGameplayTag`
- Dual-tag injection in `AddCharacterAbilities()`
- `GetControllerInputTagFromSpec()` + updated `GetInputTagFromSpec()`
- `ControllerInputTag` field in `FRageInMageAbilityInfo`, populated at all 3 broadcast sites
- Controller IMC/config properties on PlayerController
- Device detection delegate `OnInputDeviceChanged`, cursor management
- Controller keybinding persistence in save game
- `ControlsWidgetController` dual-mode support (M&K + Controller)
- `SettingsWidgetController` passes controller refs

### Controller Support Phase 2 — Assets (DONE)
- Created controller InputActions (IA_Controller_RT/LT/RB/A/B/X/Y/Start)
- Created `IMC_ControllerContext` with gamepad key mappings
- Created `DA_ControllerInputConfig` data asset
- Set BP defaults on PlayerController (ControllerMageContext + ControllerInputConfig)
- Set SettingsWidgetController defaults

### Input System Overhaul (DONE)
- Split monolithic `DA_RageInputConfig` into `DA_MAKInputConfig` + `DA_ControllerInputConfig`
- Split `IMC_RageContext` into `IMC_MAKContext` + `IMC_ControllerContext`
- Created individual InputActions for all ability/modifier/levelup/menu inputs
- Key icon data system (`FKeyIconData`, `UKeyIconRow`)

---

## Tooling Gotchas (NeoStack Lua bridge)

### Adding a field to a USTRUCT used inside a UDataAsset array wipes that array's data in the live session
**Recurring issue, confirmed by the user as something they've hit before** (not new to this session): adding a new `UPROPERTY` field to a `USTRUCT` that's used as the element type of a `TArray<Struct>` on a `UDataAsset` (e.g. adding `StunImmunityGraceSeconds` to `FRageInMageConditionInfo`, used by `UConditionInfo::ConditionInfos`), then recompiling (Live Coding in this instance, but the user says plain IDE recompiles have done this too), can wipe that array back to empty-default elements **in the live editor session** — the struct's old serialized data doesn't survive the layout change. Confirmed via `a:get('ConditionInfos')` going from real data (Burning/Frozen/OverCharged/Charged rows) to `((),(),(),...)` (all empty) immediately after the recompile, with no `save()` call made in between.
**Not necessarily true data loss** — as long as nothing calls `save()` on the affected asset before reloading, the on-disk `.uasset` should still have the original data; the wipe is only in the in-memory copy. User's existing workflow: expect this, and manually re-enter the affected data asset's rows after this kind of struct change (confirmed they've done this before and are fine doing it again).
**Mitigation for future sessions:** before adding a new field to any USTRUCT already used inside a `TArray`/`TMap` on a `UDataAsset`, warn the user this may wipe that asset's array data on next recompile, and confirm they're OK re-entering it (or get the current values first via `get()` so they can be manually restored) rather than assuming Live Coding will safely preserve it.
**Confirmed recovery path (validated this session, Flash And Awe stun-immunity work):** the wipe is in-memory only as long as the affected asset is never saved in that wiped state. Close the editor WITHOUT saving the wiped data asset, do a full `Build.bat`/restart rebuild (not Live Coding), reopen — the asset reloads from its still-intact on-disk `.uasset` with all original rows AND the new struct field both present. No manual re-entry needed if you catch it before saving.

### `load_level()` discards unsaved in-editor changes — silent data-loss risk
Calling `load_level(path)` reloads the level fresh from the saved `.umap` on disk, silently discarding any unsaved in-memory edits — it is NOT a safe "just open this to inspect it" operation if the level might already be open with unsaved work. This actually happened: the user had placed enemies in `Tournament_Forest_2` that were only ever captured in an editor autosave (`Saved/Autosaves/Game/Maps/Tournament_Forest_2_Auto1.umap`, newer + larger than the saved `.umap`), and a `load_level()` call (done to check nav bounds) wiped them from the editor. The autosave file still exists on disk and could have been used to recover (load it, then save-as over the real path) — user opted to skip recovery and re-place enemies manually instead. **Before calling `load_level()`, check `playtest_status()`/current level state first; prefer inspecting the already-open level directly (`select_all_actors()` + `get_selected_actors()`) over reloading it.**

### New native gameplay tags don't register until the editor restarts
Adding a new tag to `FRageInMageGameplayTag` (e.g. `Condition_StunImmune` this session) compiles fine via Live Coding — `class_properties`/`class_methods` on other new symbols in the same compile confirm the module genuinely hot-patched. But the new tag itself is **not usable** in the running session: `FRageInMageGameplayTag::Get()` almost certainly lazy-initializes via a `static bool bInitialized` guard that already ran once before the edit, so the new `AddNativeGameplayTag()` call never executes this session. Symptom: any editor-script attempt to set a `FGameplayTag`/`FGameplayTagContainer` property to the new tag's name silently produces an *empty* container (the string fails to parse against the tag manager) — easy to mistake for a tool/property-set bug rather than an unregistered tag. Confirmed by testing: setting the same struct shape with a pre-existing tag (`Condition.Frozen`) round-tripped fine on the same asset/property in the same script. **Fix: full editor restart** (this is a distinct flavor of the existing "Live Coding can't hot-patch new class members" gotcha below — same root cause category (static init not re-run), different symptom).

### GameplayEffect tag-granting uses GEComponents in UE 5.8, not the legacy struct fields — and the scripting tool can't add them
`InheritableGameplayEffectTags`/`InheritableOwnedTagsContainer` (the old `FInheritedTagContainer` struct fields on `UGameplayEffect`) can be *read* via `class_properties`/`a:get()` and even *appear* to `a:set()` successfully (the immediate `a:get()` echoes back the new value) — but the change is silently wiped the moment the asset is saved/compiled. Root cause: existing GE assets in this project (e.g. `GE_Frozen`) actually grant their tags via `GEComponents` sub-objects (`TargetTagsGameplayEffectComponent` for granted/owned tags, `AssetTagsGameplayEffectComponent` for asset tags) — confirmed by reading `GE_Frozen.GEComponents`, which contains exactly those two component instances. The legacy struct fields are vestigial and get reset on save/compile for assets that use the modern component model. **The NeoStack scripting tool has no way to add `GEComponents`** — `a:add_component(name, class)` only accepts actor/scene component types ("Class not found or not a component type" for `TargetTagsGameplayEffectComponent`), and there's no dotted-path or raw-export-text way to instantiate a new sub-object into the `GEComponents` array. **Workaround: add these two components by hand in the editor** (Details panel → Components → Add Component → "Target Tags" / "Asset Tags", then set the tag on each) for any new GameplayEffect Blueprint that needs to grant a tag. Worth reporting on the Betide Studio Discord as a gap in GameplayEffect authoring support.

### Live Coding cannot hot-patch new class members
`exec_command('LiveCoding.Compile')` (equivalent to Ctrl+Alt+F11 in-editor) reliably patches **function-body-only** changes (e.g. the `IsBothEnemy` null-check fix) but fails on changes that add/remove `UPROPERTY`/`UFUNCTION` members or change class layout/size — even though UHT reruns and the header is correct, you get `error C2065: '<new member>': undeclared identifier`. Adding a new property to an existing UCLASS needs a full editor restart + normal `Build.bat`/UBT compile. Also note: `Build.bat` itself refuses to run at all while the editor is open with Live Coding active ("Unable to build while Live Coding is active") — the editor must be closed first.

---

## Architecture Notes

### Controller Input Flow
Both IMCs active simultaneously but share the same `IA_Ability_*` InputAction assets. Only ONE `BindAbilityActions()` call is made (using M&K config/tags). Each ability spec carries both M&K and Controller tags via DynamicSpecSourceTags. The M&K tag is used for ability activation; controller tags exist only for UI display (key icons). `InputTagToControllerInputTag` mapping: LMB→Controller.LT, RMB→Controller.RT, Shift→Controller.RB, 1→A, 2→B, 3→X, 4→Y.

### Keybinding System
- M&K bindings: `IMC_MAKContext` -> `DA_MAKInputConfig` -> M&K InputTags
- Controller bindings: `IMC_ControllerContext` -> `DA_ControllerInputConfig` -> Controller InputTags
- Runtime remapping creates a duplicate IMC, modifies key mappings, swaps contexts
- Persistence via `RageInMageSettingsSaveGame` (separate maps for M&K and controller)
- `ControlsWidgetController` exposes `GetAllBindings()` / `GetAllControllerBindings()` for UI
